{
  "name": "joca-ui",
  "version": "1.0.0",
  "scripts": {
    "setup": "cd backend && npm install && cd node_modules/node-pty && npx node-gyp rebuild && cd ../../../frontend && npm install",
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
