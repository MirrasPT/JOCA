---
description: Create (or improve with --upgrade) a skill via a self-improving research pipeline.
---

# /create-skill — Create a new skill

`Read(".claude/skills/create-skill.md")` and follow the pipeline described there, passing `$ARGUMENTS` as the description of the skill to create (or `--upgrade <name>` to improve an existing one).
