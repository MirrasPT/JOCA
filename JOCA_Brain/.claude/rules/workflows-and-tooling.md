# Workflows & Tooling — MOVED (on-demand)

Dump of recurring gotchas (sub-agent briefs · Workflow tool · Windows environment: python≠python3, ports occupied on IPv4+IPv6, `sed -i` on `.bat`, Vite SPA-fallback proxy, atomic JSON writes, `__dirname ../../` data-dir, Git Bash `MSYS_NO_PATHCONV`, Sail+Vite on the host, robocopy `/XD`, embeddable ComfyUI, SDK `.d.ts`, plugin marketplace, asset readiness · CRLF/LF line endings between machines · Playwright visual QA (allowed roots, fullPage vs `position:fixed`, scrub) · macOS gotchas) **moved to `.claude/reference/workflows-and-tooling.md`** — it is no longer auto-loaded in every session.

**Before running a multi-agent workflow, writing scripts that touch credentials/binaries/paths on Windows, or debugging an environment gotcha → `Read(".claude/reference/workflows-and-tooling.md")`.** Referenced by `orchestration-patterns.md` and sub-agent briefs.
