# Workflows & Tooling — MOVIDA (on-demand)

Despejo de gotchas recorrentes (briefs de sub-agente · Workflow tool · ambiente Windows: python≠python3, portas ocupadas IPv4+IPv6, `sed -i` em `.bat`, proxy Vite SPA-fallback, escrita atómica JSON, `__dirname ../../` data-dir, Git Bash `MSYS_NO_PATHCONV`, Sail+Vite no host, robocopy `/XD`, ComfyUI embeddable, SDK `.d.ts`, plugin marketplace, asset readiness) **movido para `.claude/reference/workflows-and-tooling.md`** — deixou de ser auto-carregado em toda a sessão.

**Antes de correr um workflow multi-agente, escrever scripts que tocam credenciais/binários/paths no Windows, ou depurar um gotcha de ambiente → `Read(".claude/reference/workflows-and-tooling.md")`.** Referenciado por `orchestration-patterns.md` e briefs de sub-agente.
