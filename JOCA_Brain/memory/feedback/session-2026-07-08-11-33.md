---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: bigorna-2026
---

**Categoria:** discovery-gap | **Severidade:** high | **Descrição:** Após a migração PC→Mac (commit "full JOCA snapshot for PC transfer"), a memória do projecto Bigorna manteve o `directorio:` num path **Windows** (`D:\Mega\Bigorna\2026_Website_Bigorna`) e o repo real **não estava clonado** no Mac. O `/init-project` foi corrido contra `/Users/renatoferreira/MEGA/Bigorna`, que só tinha resíduo legado (tema WooCommerce, logo) — nada detectou automaticamente que o path de memória não existe nesta máquina nem que o repo tinha de ser re-clonado. Só foi apanhado por inspecção manual do disco + `gh repo view`. | **Componente afectado:** `/init-project`, `/resume`, processo de migração (`/migrate`) | **Fix sugerido:** (1) `/resume` e `/init-project` devem verificar `test -d <directorio:>` no arranque; se faltar, cruzar com o remote GitHub conhecido na memória e propor `git clone`. (2) `/migrate` (PC→Mac ou máquina nova) devia incluir um passo de "re-clonar todos os repos de projecto" + reescrever os `directorio:` das memórias com os paths da nova máquina (Windows `D:\` → macOS `/Users/...`). Sem isto, cada projecto fica com path órfão e é preciso reconciliar à mão.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** `graphify` (v0.7.18) falhou a gerar a viz HTML no `_rebuild_code` do projecto Bigorna: "Graph has 7540 nodes - too large for HTML viz". Indexou 720 ficheiros mas não produziu output porque a viz rebentou o limite — provavelmente por não excluir `vendor/`/`frontend/` na API directa. | **Componente afectado:** graphify integração (`/init-project` PASSO 1, `/save` PASSO 5) | **Fix sugerido:** passar `--no-viz` (ou o equivalente na API `_rebuild_code`) para projectos grandes, ou garantir que o scan exclui `vendor/`/`node_modules/`/`frontend/dist` antes de contar nós. O `/save` já documenta as exclusões por omissão; o `/init-project` (Branch A) chama `_rebuild_code` directo e não as garante.
