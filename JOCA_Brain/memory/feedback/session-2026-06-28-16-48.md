---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-28
project: livro-de-elogios
---

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** Workflow com ~12 agentes opus em paralelo disparou throttle Anthropic ("Server is temporarily limiting requests") e, numa corrida seguinte, bateu no cap de sessão de 5h ("You've hit your session limit · resets 4:30pm") — TODOS os 12 agentes de uma fase devolveram vazio, perdendo a run inteira (a fundação sequencial e o tour já tinham passado, mas a fase paralela falhou em bloco). | **Componente afectado:** Workflow tool (fan-out paralelo) | **Fix sugerido:** (1) distinguir erros de rate-limit/session-cap de falhas lógicas e NÃO marcar a run como "completed" com resultados vazios; (2) auto-retry com backoff para erros transientes de throttle; (3) cap por defeito mais baixo para agentes pesados (opus) — ou um campo `maxConcurrency` no workflow; (4) caminho limpo de "re-correr só os agentes rate-limited" (o resume pode cachear os `null` dos falhados — confirmar e documentar).

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Mitigação que funcionou: bater o fan-out em batches sequenciais (4+4+4 via `for` + `parallel` por batch) reduz o pico de tokens e contorna o throttle. Não está documentado como padrão no `rules/pipelines.md`/`orchestration-patterns.md`. | **Componente afectado:** rules/orchestration-patterns.md | **Fix sugerido:** Adicionar padrão "batched fan-out" (chunks sequenciais de N) à lista de padrões endorsed, com nota de que workflows grandes de agentes opus devem usá-lo para não bater rate-limit/session-cap.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** O parser de scripts do Workflow rejeitou `uploadImage<T>(...)` mesmo DENTRO de uma string de documentação (template literal) — "TypeScript generics fail to parse". O `<T>` estava num literal, não em código. | **Componente afectado:** Workflow tool (parser) | **Fix sugerido:** O parser não devia inspeccionar conteúdo de string literals para sintaxe TS; em alternativa, documentar que se deve evitar `<...>` em qualquer parte do script (mesmo em strings).

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** O `/save` e o `/resume` não detectam automaticamente que um projecto tem trabalho de sessão em `dev` LOCAL ainda não empurrado (ex.: `d90e373` em `dev` local, não em `setuptech/dev`), separado do branch de feature. Causa confusão sobre o que está onde. | **Componente afectado:** commands/save.md, commands/resume.md (PASSO 2b drift) | **Fix sugerido:** No drift-check, comparar também `local <branch>` vs `<remote>/<branch>` (ahead/behind) e reportar commits locais não-empurrados por branch.
