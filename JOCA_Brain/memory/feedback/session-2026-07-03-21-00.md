---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-03
project: JOCA
---

**Categoria:** workflow-gap
**Severidade:** medium
**Descrição:** 28 skills tinham descriptions auto-geradas lixo ("MUST be invoked when the user mentions: Behavioral, LLM." — palavras capitalizadas soltas do corpo, ou vírgulas duplas `,,` de um pack marketing). O lixo estava baked no frontmatter dos `.md`, não regenerado — veio de um extractor antigo (provavelmente uma versão passada do `create-skill` ou de import de packs). Corrigido manualmente nesta sessão, mas não há gate que impeça re-introdução.
**Componente afectado:** processo de criação/import de skills; `skill-lint.js` (PostToolUse) não valida qualidade da description.
**Fix sugerido:** `validate-skill.py`/`skill-lint.js` ganhar uma regra: WARN se a description casar padrões de lixo (`mentions: <palavras isoladas capitalizadas>`, `,,`, `t mention`, description < 40 chars úteis). Barato, apanha na origem.

**Categoria:** workflow-gap
**Severidade:** medium
**Descrição:** 24 skills (13 wp-*, anima, lottie-animator, shopify-*, video, google-analytics, etc.) referenciavam `references/*.md` e scripts `.mjs`/`.py` que NÃO existem — packs importados de estruturas skill-directory e achatados para o layout flat do JOCA sem trazer os assets. Chains que seguissem o pointer falhavam em silêncio. `google-analytics` era o pior caso: skill inteira = manual de um `ga_query.py` inexistente.
**Componente afectado:** processo de import de skills externas; `check-skill-paths.sh` (PostToolUse) só valida paths de skills, não referências internas a `references/`/scripts.
**Fix sugerido:** ao importar um pack externo, regra explícita — ou trazer os assets referenciados, ou cortar os pointers no mesmo passo (nunca deixar path morto). `check-skill-paths.sh` podia estender-se a avisar sobre `Read("references/...")`/`node ...mjs` cujo alvo não existe no disco.

**Categoria:** skill-improvement
**Severidade:** low
**Descrição:** Restam 9 skills grandes (>400 linhas) por dividir em multi-step (github, remotion, file-storage, horizon, social-content, blueprint, video, hyperframes, lottie-animator) e 4 fusões candidatas (agent-context→rules, karpathy-guidelines→yagni, html-review rename, transactional-email absorção). Já documentado em `projects/joca.md` Pendente. As 6 primeiras divisões desta sessão validaram o padrão (corpo curto + `.claude/reference/<skill>/`, zero perda).
**Componente afectado:** skills grandes auto-carregadas ao serem lidas (custo de contexto quando activadas).
**Fix sugerido:** próxima ronda de `/upgrade-joca` aplica o mesmo padrão de split às 9 restantes; as 4 fusões precisam de sign-off do Renato (mexem em nomes/routing).
