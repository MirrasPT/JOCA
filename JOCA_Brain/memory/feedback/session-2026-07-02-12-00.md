---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-02
project: livro-de-elogios
---

**Categoria:** doc-gap / skill-improvement | **Severidade:** medium | **Descrição:** O agente `tester-ui-ux` é descrito e invocado para "testar no browser" (fluxos, erros de consola, responsivo ao vivo), mas o seu frontmatter só declara `tools: Read, Write, Edit, Bash, Glob, Grep` — **sem ferramentas de browser** (nem claude-in-chrome nem playwright MCP). Resultado: quando a tarefa pediu teste no browser, o agente só pôde fazer auditoria ESTÁTICA (leu os .tsx) e o teste live teve de ser feito pelo main loop. Isto não está claro no brief do agente nem na doc — cria expectativa de que o agente conduz o browser. | **Componente afectado:** `.claude/agents/tester-ui-ux.md` | **Fix sugerido:** ou (a) documentar explicitamente que `tester-ui-ux` faz auditoria estática + a11y por código (e que o teste live de browser é do main loop / de um passo separado), ou (b) dar-lhe acesso a playwright MCP no campo `tools` e instruir o fallback (chrome extension → playwright → curl+tsc). Escolher (a) é mais fiável dado que os agentes muitas vezes não têm o MCP disponível.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** A extensão claude-in-chrome não estava conectada (`Browser extension is not connected`) no arranque do teste de browser; o fallback para o Playwright MCP funcionou. Confirma a regra de `workflows-and-tooling.md` (browser pode estar ausente) — o fallback canónico (chrome → playwright → curl/tsc → pedir confirmação visual) foi o que salvou o passo. | **Componente afectado:** MCP claude-in-chrome | **Fix sugerido:** nenhum código; a regra de fallback já existe e funcionou. Registar só como confirmação de que o fallback é necessário por defeito.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Numa review de trabalho feito por um worker concorrente (ecossistema JOCA_OS), o working tree cresceu de 36→47 ficheiros DURANTE a review — o worker da tarefa "Corrigir e melhorias LE" continuou a escrever. O brief de review não previa um alvo em movimento; o relatório cobriu o snapshot inicial. | **Componente afectado:** briefs de review (`tester-code`/`tester-ui-ux`) + `rules/workflows-and-tooling.md` (secção Workflow tool, "verify adversarial sem falsos positivos") | **Fix sugerido:** ao rever trabalho num repo potencialmente activo, snapshot o scope no início (`git diff --stat` guardado), reportar a review como point-in-time, e re-verificar o count no fim; se o tree cresceu, dizê-lo no relatório (feito nesta sessão manualmente — devia estar no brief padrão de review).
