---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: tcg
---

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** A skill `unity-gamedev` não cobre o caminho **headless** (sem abrir o Editor GUI) que esta sessão validou end-to-end: (1) criar projecto + build via `Unity.exe -batchmode -quit -projectPath … -executeMethod`; (2) consumir o motor por **DLL netstandard2.1 em `Assets/Plugins/`** (não por source); (3) demo jogável **sem montar cena** via `OnGUI` (IMGUI) + bootstrap `[RuntimeInitializeOnLoadMethod]` que injecta um GameObject — imune a input-backend/EventSystem/TMP. | **Componente afectado:** `.claude/skills/unity-gamedev.md` | **Fix sugerido:** acrescentar secção "Build & demo headless" com estes 3 padrões + os gotchas abaixo.

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** Unity batchmode (`-createProject` e `BuildPipeline.BuildPlayer`) **sai com exit-code ≠0 mesmo quando o build SUCEDE** (bee_backend faz retries com ExitCode 4 que depois resolvem). Confiar no exit code leva a falso-negativo "build falhou". A licença Unity **Personal funciona em batchmode** (resolve entitlement) — não é bloqueador. | **Componente afectado:** workflow de build Unity / `unity-gamedev` | **Fix sugerido:** verificar sempre pelo log (`Build Finished, Result: Success` / `[BuildScript] Succeeded`) e pelo artefacto no disco; o stub `.exe` não muda timestamp entre builds (o código vai em `*_Data/Managed/Assembly-CSharp.dll`).

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Capturar screenshot de uma janela de app (verificação visual) num setup **multi-monitor** falha com `SetForegroundWindow` (apanha a janela errada / coordenadas negativas do monitor secundário) — apanhou uma janela de browser do utilizador em vez da app. | **Componente afectado:** `rules/workflows-and-tooling.md` (verificação visual / fallback browser) | **Fix sugerido:** `MoveWindow` a janela do processo para o monitor principal (x,y conhecido) antes de `CopyFromScreen`; confirmar o conteúdo lido (não assumir que a captura é da app certa).
