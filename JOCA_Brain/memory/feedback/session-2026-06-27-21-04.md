---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: comfyui
---

**Categoria:** skill-improvement | **Severidade:** low | **Descrição:** Ao instalar o ANIMA 1.0, o download via mirror `Aitrepreneur/FLX` com `curl` ficou throttled a ~620 KB/s (ETA 94 min p/ o ficheiro de 4 GB). Trocar para `hf_hub_download` do `python_embeded` com `hf_xet`, a partir do repo **oficial** `circlestone-labs/Anima`, baixou os 4 GB em **55s**. A skill `comfy:model-registry` lista URLs de download mas não documenta a preferência por repo oficial + hf_xet sobre mirrors de terceiros (que podem estar throttled). | **Componente afectado:** skill `comfy:model-registry` (e por extensão qualquer fluxo de download de modelos ComfyUI). | **Fix sugerido:** adicionar nota na `model-registry`: preferir `hf_hub_download(..., local_dir=...)` com `hf_xet` (já presente no python_embeded) sobre `curl` de mirrors; usar o repo oficial quando existir; se o repo-path tiver prefixo (ex.: `split_files/`), baixar p/ staging + `shutil.move` para a pasta final do ComfyUI.
