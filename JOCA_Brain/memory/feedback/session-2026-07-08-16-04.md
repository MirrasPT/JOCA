---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: bigorna-2026
---

**Categoria:** discovery-gap | **Severidade:** medium | **Descrição:** O `/resume` do Bigorna não detectou que o projecto tinha um deploy LIVE a correr (`bigorna.jf.setuptech.dev`), apesar de a memória listar "decidir plataforma de deploy" como pendente. O deploy só foi descoberto porque o utilizador o mencionou. Um `curl -I` ao URL provável do projecto (derivável do nome/marca) no `/resume` teria revelado o estado real (site up, API a responder). | **Componente afectado:** commands/resume.md (PASSO 2b drift) | **Fix sugerido:** No drift-check do `/resume`, se a memória tiver um URL/domínio conhecido do projecto (ou derivável), fazer um `curl -sI` não-bloqueante e reportar se está live + o que serve (API/admin). Barato, apanha "já está deployado" sem depender do utilizador dizer.

**Categoria:** workflow-gap | **Severidade:** high | **Descrição:** Assets de seed não-versionados (48 fotos de produto em `storage/app/public/products/`) perderam-se na migração PC→Mac — nunca commitados, o `CatalogSeeder` assume-os já colocados no disco. O seed "funciona" (cria registos) mas as imagens dão 403 silenciosamente. Nada no fluxo de migração ou de `/save` sinaliza "este projecto tem assets de seed fora do git que vão desaparecer num re-clone". | **Componente afectado:** commands/save.md, commands/migrate.md | **Fix sugerido:** No `/save`, se um seeder referenciar ficheiros em `storage/app/public/**` que NÃO estão em git tracking, avisar ("N assets de seed não-versionados — perdem-se num re-clone; considerar committá-los ou documentar a origem"). Evita descobrir a perda só ao deployar.
