---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: JOCA
---

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** O header de `codex-provider.ts` afirmava que o padrão de registo do MCP bridge (`-c mcp_servers.*` envolto em aspas duplas) era "verified-working". Mas a verificação foi feita a correr `codex` **à mão no shell** — onde a própria shell remove as aspas — e NÃO via `spawn(shell:false)`, que é como o backend realmente lança em macOS/Linux. Nesse caminho real as aspas passam **literais**, o codex parseia mal o override, regista 0 mcp_servers e corre sem tools. O "verified" mascarou um bug que só aparece no caminho de produção. | **Componente afectado:** `JOCA_OS/backend/src/master/codex-provider.ts` (provider codex do Master); por arrasto `tasks/engine.ts` + `automations/runner.ts`. | **Fix sugerido:** ao documentar um padrão de `spawn` como "verificado", verificá-lo **pela mesma via de invocação do código** (spawn com o mesmo `shell` flag), não pela shell interactiva. Quoting de args de `spawn` deve ser sempre gated por `process.platform === 'win32'` (aspas só quando `shell:true`). Regra generalizável para qualquer provider CLI futuro (agy/antigravity, ollama-exec).

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** `timeout`/`gtimeout` não existem por default no macOS — comandos de teste que dependiam de `timeout` falharam com "command not found". | **Componente afectado:** scripts de teste ad-hoc / skills que assumem coreutils GNU. | **Fix sugerido:** em macOS, evitar `timeout`; usar backgrounding + kill ou um wrapper node/perl. Documentável em `reference/workflows-and-tooling.md` (gotchas de ambiente) junto ao já-existente `python≠python3`.
