---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-06
project: bigorna-2026
---

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** O `save.md` PASSO 5 (e o `/resume` PASSO 3) afirmam que o scan graphify "exclui `vendor/`, `node_modules/`, `storage/`, `out/`, `public/` por omissão". Isto NÃO se aplica ao `graphify.watch._rebuild_code(Path('.'))` cru — nesta sessão scaneou 14 995 ficheiros e produziu 181 380 nós / 319 007 arestas no Bigorna (Laravel+React), com o HTML viz sempre saltado (>5000 nós). Quem faz a exclusão dessas pastas é o `graphify-deps.py`, não o `_rebuild_code`. O texto dos comandos induz em erro: sugere que o comando principal já exclui, mas não exclui. | **Componente afectado:** `.claude/commands/save.md` (PASSO 5), `.claude/commands/resume.md` (PASSO 3), pipeline graphify | **Fix sugerido:** Ou (a) configurar as exclusões no `_rebuild_code` (ficheiro de config graphify por projecto: `.graphify.toml`/ignore) para projectos code-heavy, ou (b) corrigir a doc dos comandos para deixar claro que `_rebuild_code` NÃO exclui por omissão e que o graph resultante inclui `node_modules`/`vendor` (HTML viz não gerado); o mapa consultável é `graph.json` + `GRAPH_REPORT.md`, não o HTML. Impacto: o graph do Bigorna (186MB) é ruído maioritariamente de dependências — cara de gerar, HTML inutilizável.
