---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-26
project: livro-de-elogios
---

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** A sessão tentou montar Octane+FrankenPHP num projecto Laravel em Windows nativo e bateu em paredes definitivas só descobertas após muito retrabalho: (1) FrankenPHP só tem binários Linux/macOS (o instalador do Octane recusa Windows); (2) o `StartCommand` do Octane exige `pcntl`/`posix` (sinais POSIX) que não existem no Windows → nenhum runtime do Octane arranca nativamente. | **Componente afectado:** `rules/workflows-and-tooling.md` (secção "Ambiente local Windows-first") | **Fix sugerido:** acrescentar regra: "Octane (qualquer runtime, incl. FrankenPHP/RoadRunner) NÃO corre em Windows nativo — exige pcntl/posix. Para persistent-worker/multi-thread em Windows usar WSL2 ou Docker; senão `php artisan serve` + opcache é o tecto (single-thread → pedidos serializam)."

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** `composer install` em Windows com PHP portátil falha a criar symlinks de alguns pacotes (precisa de Developer Mode/admin), deixando o `vendor/` inconsistente. Com opcache ligado, essa inconsistência manifesta-se como **corrupção da tabela de funções** (um `mb_convert_encoding(...)` despacha para `intltz_create_default()` → ArgumentCountError no boot, mascarado por "Class config does not exist"). Custou muitos ciclos a diagnosticar. | **Componente afectado:** `rules/workflows-and-tooling.md` (PHP/Composer em Windows) | **Fix sugerido:** documentar o sintoma + fix: "vendor inconsistente em Windows → boot rebenta com erro de intl/config mascarado. Diagnosticar com opcache OFF (`php -d opcache.enable=0`); reparar com `rm -rf vendor && composer install --no-scripts --ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix` + `php -d opcache.enable_cli=0 artisan package:discover`." Também: PHP 8.4 portátil — JIT do opcache segfalha no Windows (exit 5), manter `opcache.jit=disable`.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Padrão validado (positivo): para "melhorar todo o design de um app", o workflow de **auditoria read-only fan-out (N agentes por superfície) → síntese → main loop aplica ficheiro-canónico-partilhado + N implementadores paralelos em ficheiros disjuntos** funcionou muito bem (6 audit + 4 apply, 56 findings, zero clobber). | **Componente afectado:** `rules/orchestration-patterns.md` / `rules/workflows-and-tooling.md` | **Fix sugerido:** registar como padrão nomeado "audit-then-apply (CSS-canónico no main loop, per-file disjunto em paralelo)" — reforça o "Upgrades grandes = 2 fases" já existente, aplicado a design de apps (não só ao próprio JOCA).
