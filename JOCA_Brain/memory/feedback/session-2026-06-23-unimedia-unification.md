---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-23
project: unimedia
---

# Feedback JOCA — sessão unificação UniMedia (Netflix redesign, 4 workflows multi-agente)

**Categoria:** skill-improvement | **Severidade:** high | **Descrição:** Tailwind v4 — variante arbitrária `[@media(hover:hover)and(pointer:fine)]:block` gera CSS **inválido** (`@media (hover:hover)and(pointer:fine)` → "Unexpected token Function(and)") e derruba a página INTEIRA (500 / ecrã branco). `tsc` E `next build` passam; só rebenta no dev/runtime. Pior: **Tailwind v4 faz scan do texto de `.md` e de COMENTÁRIOS** à procura de candidatos a classes → a mesma string partida num comentário ou num resumo `.joca/intermediate/*.md` regenera a classe e mantém o site branco mesmo depois de corrigir o `.tsx`. | **Componente afectado:** `.claude/skills/tailwind.md` (e `frontend.md` checklist anti-slop, que já tem o caso "adblock token branqueia site" — este é o irmão "CSS inválido branqueia site"). | **Fix sugerido:** Adicionar à skill `tailwind`: (1) para media queries de feature (`hover`/`pointer`/`prefers-*`) usar `@custom-variant nome (@media (...))` no globals.css, NUNCA arbitrary `[@media(...)and(...)]` (o `and` sem espaços = CSS inválido; `_and_` é frágil); (2) AVISO: Tailwind v4 detecta classes em `.md` e comentários — uma classe partida num comentário/nota branqueia tudo; sanitizar. Mesmo sintoma da regra adblock-safe do `frontend.md`.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Ficheiros `.joca/intermediate/*.md` (resumos dos workers, padrão endorsed em `orchestration-patterns.md` #4 "agentes escrevem para disco") ficam DENTRO do projecto → o content-scan do Tailwind v4 (e potencialmente outros scanners) apanha-os. Um worker que cita uma classe partida no resumo `.md` parte o build do projecto-alvo. | **Componente afectado:** `rules/orchestration-patterns.md` (#4) + `rules/workflows-and-tooling.md`. | **Fix sugerido:** Recomendar `.joca/intermediate/` em `.gitignore` E excluído do content-scan de Tailwind (`@source not`), OU escrever os resumos fora da árvore do projecto (ex.: scratchpad). Nota: o gate `tsc`/`build` NÃO apanha (build passa); só o dev runtime.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** O `api-design.md` já manda "verificar parser contra resposta real" — funcionou na perfeição aqui (o worker C descobriu por `curl` que `m3upt.com/pt/` está morto e achou a fonte raw real + headers anti-leech, em vez de assumir o formato do plano). Vale registar como **caso de sucesso do padrão**: a regra evitou entregar IPTV partido silenciosamente. | **Componente afectado:** `api-design.md` (secção "Consumir APIs de terceiros"). | **Fix sugerido:** Nenhum fix — a regra está correcta e provou-se. Opcional: citar este caso como exemplo.

## Nota de sucesso (não-gap)
O padrão 2-fases para upgrades grandes (workflows-and-tooling.md) generalizou bem para um build grande de projecto: **análise+plano (1 workflow read-only → `_unification/`) → foundation sequencial → fan-out paralelo → polish**, com gate `tsc+build+smoke autenticado` por fase e atribuição de ficheiros disjuntos por worker (anti-clobber). 15 agentes, zero clobber real (só 1 race transitório resolvido no gate de integração). Briefs com anti-fabricação + "validar contra resposta real" + convenções da foundation = workers não recriaram componentes partilhados.
