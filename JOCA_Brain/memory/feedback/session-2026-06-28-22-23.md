---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-28
project: meu-site-github
---

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Playwright MCP recusa `file://` (`Access to "file:" protocol is blocked`) — bloqueia screenshot/verificação visual de HTML estático local (ex.: pasta `portfolio-projetos/` gerada). Workaround usado: servir com `python -m http.server <porta> --bind 127.0.0.1` e navegar `http://127.0.0.1:<porta>`, depois matar o server por porta. | **Componente afectado:** `rules/workflows-and-tooling.md` (secção "Browser (Playwright) no main loop") + skill `browser-automate` | **Fix sugerido:** Documentar o bloqueio de `file://` e o fallback do static-server como passo canónico para verificar artefactos HTML estáticos locais.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** markitdown devolve output vazio para PDFs só-imagem (sem camada de texto) — silenciosamente, sem erro. Afetou 3 PDFs de portfólio (Braga Car Power, Two Wheels, Space Collection). | **Componente afectado:** skill/agent `knowledge-ingest` / uso de `mcp__markitdown` | **Fix sugerido:** Notar que markitdown não faz OCR; PDFs só-imagem precisam de fallback (gemini-brain/OCR) ou tratar output vazio como "sem texto extraível" em vez de assumir falha.
