---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-09
project: livro-de-elogios
---

**Categoria:** workflow-gap | **Severidade:** high | **Descrição:** Numa review read-only, os ficheiros centrais mudaram ENTRE dois `Read()` na mesma sessão (desenvolvimento concorrente / outra sessão a escrever no working tree). O relatório foi produzido sobre um snapshot obsoleto (`chargeForFirstActivation` c/ quota) que já tinha sido substituído no disco (modelo de slots `chargeForActivation`). O `/save` só apanhou o drift por acaso (ao ler a memória de hoje). Sem esse acaso, ter-se-ia guardado um review inválido como verdade. | **Componente afectado:** `tester-code`, `tester-ui-ux`, `/review-code`, agentes de review em geral | **Fix sugerido:** Passo final obrigatório nos agentes/comandos de review: antes de emitir o relatório, `git status --short` dos ficheiros revistos + re-`Read()` dos centrais; se algum aparecer `M`/mudou desde o Read inicial → assinalar "snapshot moveu-se, review point-in-time" e re-verificar. Ancorar no brief-padrão dos tester-* e em `rules/workflows-and-tooling.md` (já há nota point-in-time no umbrella LE; promover a regra de review).

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** A extensão Chrome do Claude estava desligada (`Browser extension is not connected`) — bloqueou o teste UI live via MCP `claude-in-chrome`. Fez-se fallback a `open` (abre mas não conduz/inspecciona). | **Componente afectado:** MCP `claude-in-chrome` / passo 5 de reviews de UI | **Fix sugerido:** Quando um review pede teste de fluxo UI e a extensão está off, oferecer Playwright MCP como alternativa — mas notar que activar/togglar em dados reais pode ser MUTAÇÃO (ex.: activar loja cobra créditos), logo incompatível com "read-only"; nesse caso limitar a navegação/observação sem disparar acções irreversíveis.
