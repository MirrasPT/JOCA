---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-25
project: JOCA
processed: true
processed_date: 2026-06-27
---

# Feedback JOCA — sessão 2026-06-25 (cont.): providers do Master (CLI-como-cérebro)

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** Conduzir um agente-CLI externo (codex, agy) COMO cérebro orquestrador (a chamar tools nossas) tem realidades muito diferentes por CLI, descobertas só por **sondagem real** (não doc): **codex** tem `exec --json` (JSONL em stdout) + suporta MCP servers externos via `-c mcp_servers.<n>.command/.args` MAS só EXECUTA tool-calls em `exec` não-interactivo sob `--dangerously-bypass-approvals-and-sandbox`; **agy** (`-p`/--print) **não escreve nada para um stdout em pipe** (resposta só vai p/ um sqlite interno) → inutilizável de um spawn de backend; sem registo MCP headless verificável. **ollama** é o mais fácil (HTTP `/api/chat` function-calling in-process; gemma4 suporta tools — verificado). | **Componente afectado:** doutrina de orquestração / agent-sdk skill / qualquer wiring futuro de "CLI como provider". | **Fix sugerido:** registar como referência: (1) tools in-process (Claude SDK, Ollama HTTP) são triviais de ligar; (2) CLIs externos (codex) precisam de **MCP server stdio standalone + endpoints de controlo HTTP no backend** (o subprocesso do CLI não alcança o SessionManager in-process) + secret partilhado; (3) **prompt por STDIN** evita o inferno de quoting no Windows (`shell:true` + `.cmd`); (4) **sondar a capacidade real do CLI antes de escrever** (--help + chamada real) — o agy parecia viável e estava bloqueado.

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** O trap `__dirname ../../` para data-dir (já em `workflows-and-tooling.md`) voltou a morder, agora num **subprocesso/bridge standalone** (`codex-master-bridge.mjs`): a bridge recomputou o path do segredo a partir do seu próprio `__dirname` (`backend/src/master` → `../../data` = `backend/data`) enquanto o backend escreve o segredo em `JOCA_OS/data` via `DATA_DIR` → ENOENT no arranque → bridge morre → o agente-CLI externo (Codex) ficou com 0 tools e fez o trabalho ele próprio no background em vez de orquestrar os terminais do JOCA UI. A regra existente fala de "exportar `DATA_DIR` e importá-lo", mas uma `.mjs` standalone executada por outro processo NÃO pode importar o módulo TS compilado. | **Componente afectado:** `rules/workflows-and-tooling.md` (gotcha `__dirname` data-dir). | **Fix sugerido:** estender a regra: quando o consumidor do data-dir é um **subprocesso/bridge externo** que não partilha o módulo, o pai PASSA o caminho absoluto (computado da fonte-única `DATA_DIR`) por **argv/env**; o filho lê de lá e falha alto (stderr+exit) se faltar — nunca recomputa com `../../`. (Fonte: JOCA_OS Codex bridge 2026-06-25.)

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Padrão de workflow validado nesta sessão (positivo, não-gap): **fundação sequencial** (refactor partilhado para registry neutro, com equivalência byte-a-byte do path existente) → **fan-out paralelo** com cada agente a escrever o SEU ficheiro novo (zero clobber) + brief a exigir **sondagem real do CLI/runtime** e a reportar `working: true/false` honesto → **integração nos ficheiros partilhados no main loop** + verificação real (tsc/boot/smoke). Resultou em 1 provider verificado a funcionar (Ollama), 1 construído-mas-por-confirmar (Codex) e 1 honestamente bloqueado (Antigravity) — sem fabricação. | **Componente afectado:** `rules/orchestration-patterns.md` / `workflows-and-tooling.md` (já cobrem fan-out + fundação; este caso confirma para builds de backend, não só research/frontend). | **Fix sugerido:** nenhum urgente — opcional citar como caso de sucesso de "workflow para implementação multi-ficheiro com sondagem real + integração sequencial".
