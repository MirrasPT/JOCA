---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: cpanel-hosting
processed: true
processed_date: 2026-06-27
---

**Categoria:** tool-reliability | **Severidade:** medium | **Descricao:** O shortcut `read <path>` do driver `.claude/scripts/cpanel.mjs` chama `Fileman/get_file_content` com `{ path: p }`, mas a função UAPI exige `dir` + `file` (ou `file` como caminho completo sem barras intermédias). Resultado: `read` falha sempre com "The parameter 'file' is required" / "filename contains invalid characters". Tive de contornar com SFTP para ler conteúdo de ficheiros. | **Componente afectado:** `.claude/scripts/cpanel.mjs` (case `read`) | **Fix sugerido:** dividir o path em `dir`+`file` (`const file=p.split('/').pop(); const dir=p.slice(0,-(file.length+1))`) e passar `{ dir, file }` a `get_file_content`. Adicionar atalho `write`/`save` equivalente com `save_file_content`. **✅ `read` FEITO + verificado ao vivo (2026-06-27)** — split `dir`+`file`. `write` não adicionado (mutador, fica para quando for preciso).

**Categoria:** skill-improvement | **Severidade:** low | **Descricao:** A skill `cpanel` listava `AddonDomain/addaddondomain` etc. como UAPI, mas neste host (cPanel 130/134, stableserver) esses módulos UAPI não carregam — só funcionam via API2. Já corrigido na skill nesta sessão (nota API2 + delete recursivo SFTP + dump BD via Remote MySQL). | **Componente afectado:** `.claude/skills/cpanel.md` | **Fix sugerido:** feito; manter como referência para a próxima conta cPanel.
