---
processed: true
processed_date: 2026-06-20
processed_by: /upgrade-joca + /sync-questionnaires
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-18
project: simao-sina
---

**Categoria:** command-improvement | **Severidade:** medium | **Descrição:** `/resume` e `/save` (e o `CLAUDE.md` raiz) hardcoded `python3` para invocar a API do graphify. No Windows desta máquina, `python3` é o stub da Microsoft Store (sem pacotes instalados) → `ModuleNotFoundError: No module named 'graphify'`. O `python` real (Python312) tem o graphify. O passo de rebuild do graph falhou silenciosamente no primeiro try e exigiu fallback manual. | **Componente afectado:** `.claude/commands/resume.md`, `.claude/commands/save.md`, `CLAUDE.md` (secção Navegação de Código) | **Fix sugerido:** Em Windows, preferir `python` em vez de `python3` (ou detectar: `command -v python || command -v python3`). Documentar o gotcha do stub da Store nos comandos que correm graphify/whisper.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** `/resume` reconstrói o graph "se >7 dias", mas a Python API indexa toda a árvore incluindo `node_modules` quando corrida na raiz do projecto Node — graph saltou de 163 → 5056 nós e o HTML foi saltado (limite 5000). Ruído alto, pouca utilidade. | **Componente afectado:** `.claude/scripts` / config graphify | **Fix sugerido:** Garantir ignore de `node_modules`/`out`/`public` no rebuild de projectos Node, ou documentar `GRAPHIFY_VIZ_NODE_LIMIT` / exclude patterns.
