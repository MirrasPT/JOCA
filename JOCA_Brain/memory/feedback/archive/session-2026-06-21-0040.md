---
processed: true
processed_date: 2026-06-23
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-21
project: joca / joca-os
---
processed: true
processed_date: 2026-06-23

**Categoria:** missing-skill | **Severidade:** medium | **Descrição:** O padrão "dev→test→fix em loop" (review read-only paralelo por módulo → agregar issues critical/major → 1 fixer cirúrgico sequencial → re-verificar com `tsc`/build → repetir até limpo ou N rondas) foi usado ad-hoc como Workflow para endurecer o JOCA_OS recém-escrito e foi muito eficaz (3 rondas, ~45 fixes reais incluindo um path-traversal). Não há comando/skill JOCA que formalize isto. É distinto de `/one-shot` (PRD→build do zero) e de `/review-code` (review único sem loop de correcção+re-verificação). | **Componente afectado:** falta um comando tipo `/harden` ou `/dev-loop` (ou modo do `/one-shot`) | **Fix sugerido:** criar comando que receba um path de codebase + comando de build/test, e corra o loop review→fix→verify até convergir, com cap de rondas e gate de build. Encapsular o script de Workflow já provado (review via `Explore` read-only com schema de findings; fixer sequencial para evitar clobber; verify corre o build real).

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Invocar a skill bundled `claude-api` para obter detalhes do SDK carregou a referência inteira (todas as linguagens) para o contexto (~50k tokens) quando só era preciso TypeScript. Caro. O agente `claude-code-guide` (mais leve, dirigido) deu a resposta precisa sobre a Agent SDK (auth, tools, streaming) de forma muito mais económica. | **Componente afectado:** hábito de workflow ao construir apps LLM (não a skill em si, que é externa) | **Fix sugerido:** documentar preferência: para detalhes pontuais de SDK/API Claude, consultar primeiro o agente `claude-code-guide`; reservar a skill `claude-api` para quando se vai escrever bastante código Claude e a linguagem do projecto já está fixada. Eventualmente nota na trigger map / regra.

**Categoria:** skill-improvement | **Severidade:** low | **Descrição:** A regra `workflows-and-tooling.md` (criada nesta mesma leva de upgrade) provou-se imediatamente útil — a sua própria regra "verificar parser/afirmação contra a realidade" apanhou uma nota falsa que eu ia deixar (graphify-deps ignora vendor). Sinal de que regras always-loaded curtas e accionáveis valem o custo de contexto. | **Componente afectado:** `.claude/rules/workflows-and-tooling.md` | **Fix sugerido:** manter; considerar adicionar 1 linha sobre "verificar a API de um SDK contra o pacote instalado (types/.d.ts) antes de escrever código, nunca inventar nomes de opções" — aplicou-se directamente aqui (Agent SDK).
