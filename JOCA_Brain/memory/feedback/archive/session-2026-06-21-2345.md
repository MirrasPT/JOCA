---
processed: true
processed_date: 2026-06-23
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-21
project: JOCA
---
processed: true
processed_date: 2026-06-23

**Categoria:** missing-skill | **Severidade:** medium | **Descrição:** A skill `claude-api` cobre o **Messages-API SDK** (`@anthropic-ai/sdk`), mas NÃO o **Claude Agent SDK** (`@anthropic-ai/claude-agent-sdk`) — pacote diferente que conduz o CLI `claude` logado na subscrição (o núcleo do JOCA_OS/Master). Ao invocar `claude-api` para trabalho de Agent SDK, não ajudou; tive de `npm view` + WebFetch da doc oficial (code.claude.com/docs/en/agent-sdk/typescript) + ler os `.d.ts` instalados para obter as assinaturas (`query`/`Options.env`/`createSdkMcpServer`/`tool`) sem fabricar. | **Componente afectado:** skill `claude-api` (e o roteamento do JOCA para trabalho de orquestração com Agent SDK) | **Fix sugerido:** criar uma skill `agent-sdk` (ou secção dedicada na `claude-api`) com as assinaturas verificadas do `@anthropic-ai/claude-agent-sdk`: `query({prompt,options}):Query`; **`Options.env` SUBSTITUI** o env (env-strip de `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` = subscrição/custo-zero; a chave no env VENCE e factura); `createSdkMcpServer({name,version,tools})` + `tool(name,desc,zodShape,handler)`; `SDKAssistantMessage.message`=BetaMessage, `SDKResultMessage` traz `result`/`is_error`/`total_cost_usd`. Padrão validado: instalar o pacote e ler os `.d.ts` como fonte de verdade (à prova de doc desactualizada). Útil para todo o trabalho futuro do Master/JOCA_OS.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Padrão validado nesta sessão para construir uma app v2 de raiz a partir de uma cópia: workflow de design (entende código+visão+pesquisa → escreve PRD/TECH_SPEC/TASKS/REPURPOSE) → `git init` próprio + baseline → fases mecânicas/refactor no main loop, tsc+boot-smoke gate por fase, commit por fase. Funcionou bem e é reutilizável. | **Componente afectado:** `/build-plan` / rules/workflows-and-tooling.md | **Fix sugerido:** opcional — referir este fluxo (design-workflow → repo próprio → fases tsc-gated com boot-smoke) como receita em `/build-plan` para apps novas derivadas de uma base existente.
