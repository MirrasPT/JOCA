---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: datalix-vps
processed: true
processed_date: 2026-06-27
---

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** A skill `deploy-vps` só documenta o padrão de **site estático** (`root` + `file_server`). Nesta sessão fez-se deploy de uma **app Docker (Laravel) atrás do Caddy do sistema** — um padrão recorrente (TryPost, e antes a media stack) que não está capturado. Faltou: vhost `reverse_proxy 127.0.0.1:<porta>` (app Docker privada em localhost, padrão *arr), blocos `handle /path*` para servir páginas estáticas (legal/verificação) coexistindo com o reverse_proxy, e o gotcha do **cert Let's Encrypt on-demand** (o 1º pedido via Cloudflare orange dá **HTTP 525 transitório** enquanto o Caddy emite o cert; resolve sozinho em segundos → não é erro). | **Componente afectado:** `.claude/skills/deploy-vps.md` | **Fix sugerido:** adicionar secção "App Docker atrás do Caddy" (reverse_proxy + bind localhost + handle de paths estáticos) e nota do 525-on-demand-cert ao bloco de gotchas.

**Categoria:** missing-skill | **Severidade:** low | **Descrição:** Setup de connectors OAuth de plataformas sociais (TikTok Developers, Meta/Instagram) para uma app self-hosted foi 100% manual e demorado (apps, redirects, scopes, verificação de domínio, tester roles, App Review vs dev mode). Reutilizável para qualquer cliente que queira automação social. | **Componente afectado:** (novo) skill tipo `social-connectors-setup` ou secção numa skill de automação social | **Fix sugerido:** capturar o playbook: criar app "without a use case" (Meta) p/ evitar o labirinto do App Review; `<PLAT>_CLIENT_REDIRECT` obrigatório; Instagram standalone vs via-Facebook; "cargo insuficiente" = tester role + aceitar convite; `..._content_publish` "Ready for testing" basta em dev.
