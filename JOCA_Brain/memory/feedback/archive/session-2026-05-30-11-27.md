---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-05-30
project: JOCA (toolkit self-development)
---

# JOCA Feedback — design-skills session

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** Legacy broken placeholder `Read(".claude/skills/SKILL.md")` em 19 ficheiros (6 skills + 13 agents/commands) — leftover de quando os skills eram `category/name/SKILL.md` antes do flatten. Routers e agentes não conseguiam carregar o skill certo (todos apontavam ao mesmo path inexistente). | **Componente afectado:** 6 skills (design-system, laravel-specialist, marketing, planning, queues, transactional-email) + 13 agents/commands | **Fix sugerido:** RESOLVIDO esta sessão (paths corrigidos). Preventivo: CI/hook que faz grep por `skills/SKILL\.md` e falha o commit.

**Categoria:** missing-skill | **Severidade:** high | **Descrição:** Não existia skill de design-review opinativo (gosto/AI-slop/composição). `tester-ui-ux`=QA+WCAG, `design-system-audit`=tokens, `frontend #10`=só geração. 3 de 7 skills externas analisadas eram review. | **Componente afectado:** cluster de design | **Fix sugerido:** RESOLVIDO — criado skill `design-review` (modos live/code + plan).

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** `html-review` estava citado no trigger map do CLAUDE.md e no Quality Gate do `frontend` como "review de UI vs web standards" — é na verdade um conversor markdown→HTML para stakeholders. Citação propagou-se e induziu em erro. | **Componente afectado:** frontend.md, CLAUDE.md, framing de html-review | **Fix sugerido:** RESOLVIDO (citações corrigidas, routing para design-review). Verificar se há outras referências stale.

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** `build-skill-index.py` só faz parse de triggers em formato lista YAML ou "Triggered by:"; todos os skills JOCA usam inline comma (`triggers: a, b, c`) → `SKILL_INDEX.json` tem `triggers: []` vazio para TODOS os skills. Lazy-loading depende só dos keywords do `description`. | **Componente afectado:** .claude/scripts/build-skill-index.py | **Fix sugerido:** estender `extract_triggers()` para fazer parse do formato inline comma-separated da frontmatter.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** O mirror `.agents/skills/` (Codex, committed) dessincroniza silenciosamente — edições a `.claude/skills/*` não propagam, não há auto-sync, e `compile-bridges.sh` tem de ser corrido à mão. Além disso o script regenera AGENTS.md/GEMINI.md de templates stale com paths errados (ex.: `.claude/skills/design/frontend-dev/SKILL.md`, estrutura que já não existe). | **Componente afectado:** .agents/skills/, .claude/scripts/compile-bridges.sh, GEMINI.md, AGENTS.md | **Fix sugerido:** ou abandonar o mirror (Claude-first), ou hook fiável de sync + corrigir os templates de GEMINI.md/AGENTS.md para a estrutura flat actual.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** `compile-bridges.sh` usa `((count++))` sob `set -e` — o post-increment devolve 0 (falsy) no primeiro ficheiro, abortando o script. | **Componente afectado:** .claude/scripts/compile-bridges.sh | **Fix sugerido:** trocar por `count=$((count+1))` ou `((count++)) || true`.
