---
processed: true
processed_date: 2026-06-20
processed_by: /upgrade-joca + /sync-questionnaires
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-05-31
project: JOCA
---

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** `JOCA_UI/stop.bat` não termina os servidores (portas 7371/7372 continuam LISTENING após correr). Em cada ciclo de rebuild/restart tive de matar os PIDs manualmente via `Get-NetTCPConnection ... | Stop-Process`. Causou retrabalho repetido durante o dev do JOCA_UI. | **Componente afectado:** `JOCA_UI/stop.bat` (e provavelmente `stop.sh`) | **Fix sugerido:** matar por porta em vez de por nome/janela — ex. `for /f "tokens=5" %a in ('netstat -ano ^| findstr ":7371 :7372" ^| findstr LISTENING') do taskkill /F /PID %a`. Validar que apanha os processos node/vite reais.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** A leitura de rate limits assumia ambiente macOS (Keychain) e binário `sqlite3` no PATH — ambos ausentes no Windows, que é o ambiente primário do Renato. O statusline falhava silenciosamente. Padrão recorrente (ver também [[laravel-sail-windows]]). | **Componente afectado:** `.claude/scripts/statusline-command.js` (corrigido cross-platform nesta sessão); regra geral de portabilidade. | **Fix sugerido:** ao escrever scripts/skills que tocam credenciais, binários de sistema ou paths, assumir Windows-first ou detectar plataforma — nunca só macOS. Considerar nota nas skills relevantes.
