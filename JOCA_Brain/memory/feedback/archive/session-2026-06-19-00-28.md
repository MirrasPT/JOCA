---
processed: true
processed_date: 2026-06-20
processed_by: /upgrade-joca + /sync-questionnaires
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-19
project: comfyui
---

# Feedback JOCA — sessão ComfyUI (install + Ideogram 4.0 + teste end-to-end)

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** A entrada de memória do projecto (gerada por `/init-project` em sessão anterior) registou setup ASPIRACIONAL como se estivesse feito — `.mcp.json` criado, "22 skills", MCP "88 tools" — mas em disco `D:\_Comfyui` só tinha um Git-for-Windows; nada instalado. Custou um passo de verificação + correcção da memória no arranque desta sessão. | **Componente afectado:** `/init-project` (PASSO de criação de memória) | **Fix sugerido:** `/init-project` deve marcar claramente estado real vs plano (ex. "PLANEADO" vs "INSTALADO/VERIFICADO") e, para projectos dev, validar que o directório contém o que descreve antes de afirmar "Instalado/Inicializado".

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** Instalar plugins de marketplace no Claude Code pode fazer-se por CLI (`claude plugin marketplace add <repo>` + `claude plugin install <plugin>@<marketplace>`), não só pelo `/plugin` TUI interactivo. O memory/docs do projecto assumia passo manual do utilizador via TUI. A via CLE permite ao agente instalar autonomamente. | **Componente afectado:** docs JOCA / fluxo de install de plugins | **Fix sugerido:** documentar `claude plugin ...` CLI como método preferido quando o agente precisa de instalar/gerir plugins; notar que plugins de marketplace são sempre user-scope (custo always-on em todas as sessões).

**Categoria:** missing-skill | **Severidade:** medium | **Descrição:** Disparar um workflow ComfyUI por código (sem o MCP activo, que só liga em sessões abertas dentro do dir do projecto) exigiu scripting Playwright ad-hoc: carregar template via `app.loadGraphData`, serializar com `app.graphToPrompt()` (necessário por causa de subgraphs) e POST `/api/prompt`. Não há skill JOCA para conduzir uma app web local (litegraph/subgraphs) e disparar/monitorizar um job. | **Componente afectado:** skills de automação (lacuna) | **Fix sugerido:** considerar skill `comfyui-local` ou helper genérico de browser-automation (Playwright headless via `channel:'chrome'`, sem download de chromium) com receita load-template→graphToPrompt→/api/prompt→poll /history. Reutilizável para outras apps litegraph.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Em Windows, detectar o processo do servidor com `Win32_Process | where CommandLine -like '*ComfyUI*main.py*'` dá falso positivo: a própria pwsh que corre a query contém essa string na linha de comando, parecendo "PIDs a reiniciar em loop". Causou diagnóstico errado temporário. | **Componente afectado:** receitas de gestão de processos locais (Windows) | **Fix sugerido:** documentar padrão correcto — filtrar `Name='python.exe'` + `CommandLine -like '*main.py*'`, nunca incluir o nome único da app no filtro Win32_Process.
