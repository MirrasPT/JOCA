---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-25
project: JOCA
processed: true
processed_date: 2026-06-25
---

# Feedback JOCA — sessão 2026-06-25 (JOCA_OS Master UX + /install)

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** o `install.md`/`update.md` do repo público `MirrasPT/JOCA` referem caminhos e branch desactualizados — `JOCA_Logic` (renomeado para `JOCA_Brain` em 2026-06-21), `python3` (no Windows é o stub da Store → usar `python`) e, no `update.md`, branch `master` quando o repo oficial usa `main`. Quem corre o bootstrap numa máquina nova segue passos partidos. | **Componente afectado:** `install.md`, `update.md` (repo distribuição `MirrasPT/JOCA@main`) | **Fix sugerido:** sincronizar esses dois ficheiros com o estado actual (`JOCA_Brain`, `python`, branch `main`); idealmente gerá-los a partir de uma fonte única para não voltarem a divergir.

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** o MCP `playwright` estava registado (user scope) com o pacote **`@anthropic-ai/mcp-server-playwright`, que não existe no npm (404)** → "Failed to connect" silencioso; browser automation morto. O pacote correcto e a funcionar é `@playwright/mcp`. O `/install` documenta "MCPs (playwright, markitdown)" mas não fixa o nome do pacote, e algo registou o errado. | **Componente afectado:** `/install` (FASE 3 Browser Automation / setup de MCPs), `memory/tools/mcps.md` | **Fix sugerido:** no `/install` registar playwright via `claude mcp add playwright -s user -- npx -y @playwright/mcp@latest` e documentar o pacote canónico em `mcps.md`; nunca `@anthropic-ai/mcp-server-playwright`.

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** mesmo com o MCP playwright **Connected**, as suas tools (`browser_navigate`/`browser_take_screenshot`/…) **não apareceram via ToolSearch** no main loop — impossível conduzir o browser para verificação visual programática nesta sessão. Reforça a nota já existente em `workflows-and-tooling.md` (playwright pode estar ausente/indisponível no main loop). | **Componente afectado:** verificação visual de frontend (skill `frontend`, regra `workflows-and-tooling.md`) | **Fix sugerido:** manter o fallback canónico (build/tsc como proxy + pedir confirmação visual ao user) como caminho primário em vez de assumir playwright disponível; documentar que "Connected" no `claude mcp list` ≠ tools acessíveis via ToolSearch.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** o `/save` não distingue commits do repo-toolkit (`JOCA_FINAL`) dos do app `JOCA_OS` (repo git próprio, local-only) — a sessão acumulou trabalho por commitar nos dois e o estado tem de ser registado à mão na memória. | **Componente afectado:** `/save` (PASSO 2/8) | **Fix sugerido:** opcional — `/save` detectar sub-repos git e reportar pendências de commit por repo no relatório.
