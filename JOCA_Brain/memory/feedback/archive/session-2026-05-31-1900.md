---
processed: true
processed_date: 2026-06-20
processed_by: /upgrade-joca + /sync-questionnaires
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-05-31
project: bigorna-2026
---

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** O rebuild do knowledge graph (em `/resume` e `/save`) corre sobre TODO o projecto, incluindo `vendor/` e `node_modules/`. No Bigorna isso gera ~95.964 nós / 127.938 arestas — ruído que ultrapassa o limite de visualização HTML (5.000) e torna o graph lento e de baixo sinal (o código real da app são <1% dos nós). **Componente afectado:** `.claude/scripts/graphify-deps.py` / passo de graph em `resume.md` e `save.md`. **Fix sugerido:** excluir `vendor/`, `node_modules/`, `storage/`, `bootstrap/cache/` do scan (ignore patterns) por omissão para projectos PHP/JS; ou documentar a flag de exclusão. Resultado: graph focado na app + rebuild muito mais rápido no bind-mount Mega.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** O passo de graph do `/save` e `/resume` usa `python3 -c "...graphify..."`, mas no ambiente do Renato (Windows) o módulo `graphify` só existe no interpretador `python` (Windows), não no `python3` do Bash/Git. O comando documentado falha silenciosamente; só funciona via PowerShell `python`. **Componente afectado:** `save.md` PASSO 5, `resume.md` PASSO 3. **Fix sugerido:** documentar/usar `python` (não `python3`) em ambiente Windows, ou detectar o interpretador correcto.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Em builds multi-fase grandes (este teve 8 fases planeadas, 4 executadas), o padrão "workflow desenha plano → implemento faseado com testes + restart por fase" funcionou muito bem, mas não há um comando/skill que formalize isto (persistir plano em `docs/`, criar tasks por fase, checkpoint por fase). **Componente afectado:** falta um `/build-plan` ou extensão do `/one-shot` para modo faseado supervisionado. **Fix sugerido:** considerar uma skill que pegue no output de um workflow de planeamento e gere tasks + doc de plano + loop faseado com gate de testes.
