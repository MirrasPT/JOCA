---
processed: true
processed_date: 2026-06-23
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-22
project: JOCA (JOCA_OS app dev)
---
processed: true
processed_date: 2026-06-23

# Feedback JOCA — sessão JOCA_OS Fase 1a + memória

Sessão de dev do app JOCA_OS (não do toolkit), mas revelou lições de ambiente/workflow que pertencem ao Brain. As duas primeiras **já foram aplicadas** a `rules/workflows-and-tooling.md` nesta sessão (via `/save` com o argumento "atualiza o brain").

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** Em Windows, arrancar um 2º servidor node numa porta já ocupada NÃO falha — o 1º agarra IPv4 (`127.0.0.1`), o 2º agarra IPv6 (`::`), ambos "LISTENING". Pedidos batem ora num ora noutro → estado dividido (UI "marada"). Aconteceu por correr `start.bat` por cima de um backend relançado pelo Claude. | **Componente afectado:** `rules/workflows-and-tooling.md` (Ambiente Windows); `JOCA_OS/stop.bat` | **Fix sugerido:** [APLICADO] regra "nunca arrancar em porta ocupada — verificar `Get-NetTCPConnection -LocalPort` antes; 2 linhas = 2 PIDs = double-bind; parar primeiro". Pendente: confirmar que `stop.bat` apanha IPv4+IPv6.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Módulo em `src/sub/x.ts` (`dist/sub/x.js`) resolve `__dirname/../../data` para um dir DIFERENTE de um módulo em `src/y.ts` — um nível a mais. Causou a memória do Master a escrever em `backend/data` em vez de `JOCA_OS/data`. Bug silencioso (tsc passa). | **Componente afectado:** `rules/workflows-and-tooling.md` | **Fix sugerido:** [APLICADO] regra "paths de dados partilhados via constante `DATA_DIR` exportada de um módulo, nunca recomputar `../../` por ficheiro".

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** Lições reutilizáveis do Master que podiam virar skill/nota do Brain para futuros providers-agnósticos: (a) Agent SDK repete o último bloco de texto no `result` → de-duplicar contra a última mensagem do assistant; (b) submit fiável a TUI Claude via PTY = **bracketed-paste** (`ESC[200~…201~`) + CR atrasada (newlines raw submetem cedo); (c) sessões spawnadas por API programática precisam de emitir evento para a UI difundir (`session_created`). | **Componente afectado:** futura skill `agent-sdk` / `pty-tui` no Brain | **Fix sugerido:** considerar uma skill curta "driving Claude Code via PTY/Agent SDK" se o padrão se repetir noutros projectos.
