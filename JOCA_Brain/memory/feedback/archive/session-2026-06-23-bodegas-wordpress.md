---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-23
project: bodegas-do-campo
processed: true
processed_date: 2026-06-25
---

**Categoria:** missing-skill | **Severidade:** high | **Descrição:** Build completo de um site WordPress + Elementor (Free) + WooCommerce + HFE feito sem nenhuma skill dedicada de Elementor. Todo o pipeline (gerar `_elementor_data` JSON → `wp post meta update` → flush; classes editáveis sem "blocos de código") + gotchas críticos foram descobertos por tentativa/erro e gravados em memória (`elementor-json-pipeline`, `projects/bodegas-do-campo`). Sem skill, qualquer build Elementor futuro repete a descoberta. | **Componente afectado:** `.claude/skills/` (falta `elementor-builder` ou secção no `wordpress-router`) | **Fix sugerido:** criar skill `elementor-builder` com: pipeline JSON via post meta; **`css_classes` (containers) vs `_css_classes` (widgets)**; widget types (core + HFE `navigation-menu`/`site-logo`/`hfe-cart`; `wpforms`; `video` youtube; `image-gallery` control = `wp_gallery`); HFE CPT `elementor-hf` + `ehf_template_type`/`ehf_target_include_locations`; estratégia "child-theme CSS faz o look, Elementor segura conteúdo editável"; `body:has(.bd-hero)` para hero full-bleed; bump de versão de assets para cache-bust.

**Categoria:** discovery-gap | **Severidade:** high | **Descrição:** WooCommerce recente activa `woocommerce_coming_soon=yes` por defeito no onboarding → a loja inteira serve o placeholder "Great things are on the horizon" e o loop de produtos vem vazio no front-end (apesar de a query devolver os produtos). Custou diagnóstico (página sem Elementor, produtos publicados/instock, query OK, mas nada renderiza). O setup WooCommerce do `/init-project` (ou de uma skill `woocommerce`) devia desligar isto à cabeça. | **Componente afectado:** `commands/init-project.md` (setup WooCommerce) | **Fix sugerido:** no passo de config WooCommerce, adicionar `wp option update woocommerce_coming_soon no` (+ `woocommerce_store_pages_only no`) e registar a gotcha. Aplica-se sempre que se cria uma loja WooCommerce nova.

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** O MCP `playwright` falha health-check (`npx @anthropic-ai/mcp-server-playwright` → Failed to connect), pelo que NÃO está exposto nem ao main loop nem aos sub-agentes. Toda a verificação visual desta sessão (≈10 screenshots/medições) foi feita conduzindo o Playwright local via `node` + `NODE_PATH=JOCA_FINAL/node_modules`. | **Componente afectado:** MCP playwright (config) + `rules/workflows-and-tooling.md` (diz "pode não estar no main loop"; na prática está DOWN) | **Fix sugerido:** reparar/reinstalar o MCP playwright, ou documentar o fallback canónico (Playwright local via Node) como método de QA visual. Medição via Node DOM é mais fiável que a leitura de screenshot do agente (um agente reportou 64px onde eram 12px).
