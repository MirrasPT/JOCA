---
processed: true
processed_date: 2026-06-23
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-21
project: JOCA
---
processed: true
processed_date: 2026-06-23

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Renomear a pasta-raiz do projecto (`JOCA_Logic`→`JOCA_Brain`) falhou de dentro da sessão — o cwd do processo Claude segura o directório (Windows: `Permission denied` no `git mv`). Não há forma de un-anchor os shells persistentes in-session; o rename físico tem de ser feito com o Claude fechado, partindo a continuidade da tarefa. | **Componente afectado:** Bash/PowerShell tools (cwd anchor) + qualquer fluxo de rename de directório-raiz | **Fix sugerido:** Já documentado em `rules/workflows-and-tooling.md` (padrão refs-in-session + `.bat` externo). Considerar um comando/skill `/rename-project-dir` que automatize: stop apps → sed refs → gerar `.bat` → instruções de reabertura.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** O conceito "perfil base do utilizador" (contexto estático tipo `user.md`/`soul.md` do paradigma Agentic OS) não tinha um comando/skill dedicado — foi feito ad-hoc via "interview mode" pedido pelo Renato. Valida-se como padrão recorrente (o vídeo Simon Scrapes recomenda a entrevista de 15 perguntas como passo 1 de qualquer Agentic OS). | **Componente afectado:** comandos de setup (`/install`, `/init-project`) | **Fix sugerido:** Skill/comando `/interview` ou `/profile` que conduz a entrevista estruturada e gera `memory/profile.md` + auto-memória `user`. Integrar no bootstrap.
