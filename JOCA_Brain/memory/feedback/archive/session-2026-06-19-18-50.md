---
processed: true
processed_date: 2026-06-20
processed_by: /upgrade-joca + /sync-questionnaires
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-19
project: simao-sina
---

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** O `Workflow` foi lançado com `args` (objeto JSON `{lyrics:[...]}`) passado no parâmetro `args` do tool, mas dentro do script `args` chegou como `undefined` → `const L = args.lyrics` rebentou com "undefined is not an object" e o workflow falhou logo no arranque (0 agentes). Fix: embeber os dados diretamente no script (array literal) em vez de depender de `args`. | **Componente afectado:** tool `Workflow` (passagem de `args` para o sandbox do script) | **Fix sugerido:** investigar porque `args` não chega ao global do script quando passado como objeto JSON; entretanto documentar no guidance do Workflow que dados devem ser inline no script (não via `args`) até estar fiável, ou validar `args` no arranque com erro claro.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** O agente `gemini-brain` foi usado para obter onsets de voz precisos (±0.4s) numa música; em vez de análise multimodal de áudio do Gemini, caiu para `faster-whisper` modelo `tiny` local, que só dá timestamps a nível de segmento (3-12s) e transcrição degradada — inútil para sync de letra. Reportou honestamente "não tem precisão suficiente". | **Componente afectado:** agente `gemini-brain` (pipeline de áudio) | **Fix sugerido:** documentar que `gemini-brain`/`watch` não servem para forced-alignment fino; para onset preciso usar WhisperX (forced align, modelo medium/large-v3) via `whisper-sync.py`. Considerar uma skill/agente dedicado a "lyric forced-alignment" que use o venv WhisperX existente.
