---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-24
project: gerador-rotulos
processed: true
processed_date: 2026-06-25
---

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** Git Bash (MSYS) converte automaticamente um argumento de linha-de-comando iniciado por `/` num path Windows (ex.: `/labels/2_Linhas/...` chega ao script como `D:/.../Git/labels/2_Linhas/...`). Custou um ciclo de debug ("as imagens desapareceram" — `src` na BD ficou com prefixo lixo). O `workflows-and-tooling.md` tem vários gotchas Windows-first (sed CRLF, `python` vs `python3`, taskkill por porta) mas não este. | **Componente afectado:** `.claude/rules/workflows-and-tooling.md` (secção Ambiente local Windows-first) | **Fix sugerido:** Adicionar bullet: "Git Bash converte arg iniciado por `/` em path Windows → usar `MSYS_NO_PATHCONV=1 cmd` ou correr via PowerShell; em scripts próprios, normalizar (procurar o segmento conhecido, ex.: `indexOf('/labels/')`)."

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** Padrão repetível e valioso desta sessão: ao depurar discrepâncias de cor/render entre "o ficheiro" e "o output", **medir os pixels reais** (PIL + canvas getImageData + SHA-256) em vez de teorizar. Resolveu 2 bugs distintos (CMYK source vs canvas color-management do Chrome). Poderia ser um mini-padrão na doutrina de debugging visual (existe `asset readiness` no workflows-and-tooling mas não cobre "medir pixel para localizar onde a cor muda"). | **Componente afectado:** `rules/workflows-and-tooling.md` (Asset readiness) ou nova nota | **Fix sugerido:** Nota curta: "Discrepância de cor entre fonte e export → extrair pixels de ambos (PIL/getImageData) e comparar por hash ANTES de mudar código; localiza se a perda é na fonte, no canvas (color-management → `createImageBitmap colorSpaceConversion:none`) ou no encoder."
