---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-05-30
project: bracaris-brasil-2026
---

# Feedback JOCA — sessão geração de imagem (Bracaris)

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** O agente `img-gen-google` documentava a invocação como `agy -p "PROMPT"`. Em `agy` 1.0.3, `-p`/`--print`/`--prompt` são flags de modo (não recebem o prompt como valor); o prompt é lido do **stdin**. Passar o prompt como argumento deixa o stdin vazio → o processo **pendura indefinidamente** (0 CPU, log 0 bytes), sem erro. Causou ~30 min de debug. | **Componente afectado:** `.claude/agents/img-gen-google.md` | **Fix sugerido:** ✅ APLICADO — doc corrigido para `Get-Content -Raw prompt.txt | agy --print --dangerously-skip-permissions` (pipe via stdin) + nota de que o stdout TUI não é capturável (verificar pelo ficheiro gravado).

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** O agente `img-gen-openai` documentava `codex exec "PROMPT"`. No Windows isto corre em `sandbox: read-only` e falha ao gerar/gravar com `windows sandbox: spawn setup refresh`. Além disso, o flag `-i` (imagens de referência) é variádico e **engole o prompt posicional** seguinte → codex fica em "Reading prompt from stdin..." pendurado. | **Componente afectado:** `.claude/agents/img-gen-openai.md` | **Fix sugerido:** ✅ APLICADO — doc agora exige `--dangerously-bypass-approvals-and-sandbox` e, quando há referências, prompt via **stdin** + `-i` repetido por ficheiro.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Os subagentes `img-gen-openai`/`img-gen-google` (spawned via Agent tool) **não executaram** os CLIs — construíram o comando e devolveram-no como texto ("não tenho acesso a shell"), apesar de terem a tool Bash. O trabalho real foi feito manualmente pelo orquestrador via PowerShell. | **Componente afectado:** subagentes img-gen (Bash tool em ambiente Windows/pwsh) | **Fix sugerido:** Investigar porque o subagente acha que não tem shell no Windows; ou documentar que, em Windows, a geração corre melhor inline via PowerShell em vez de delegada ao subagente.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Em background, canalizar output de CLI agêntico por `... | Select-Object -Last 40` faz o pipeline **bufferizar tudo até o processo terminar** — durante a execução o ficheiro de output fica a 0 bytes, dando falsa impressão de "preso". | **Componente afectado:** padrão de lançamento de background tasks | **Fix sugerido:** Para tarefas longas, redirigir output cru (sem `Select-Object`) para poder monitorizar progresso em tempo real.

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** Em ~1/3 das execuções `codex exec` (img-gen), o codex **descarrila**: em vez de chamar a tool `imagegen`, vai fazer *web search* ("how to save generated image from API") e nunca produz a imagem. Também emitiu aviso de parsing "carriage return must be followed by newline" (prompt file com CRLF). | **Componente afectado:** `img-gen-openai` / fiabilidade do `codex exec` para geração | **Fix sugerido:** (1) Escrever prompt files com LF, não CRLF (no PowerShell, `Set-Content` mete CRLF — usar `-NoNewline` ou normalizar); (2) No prompt, instruir explicitamente "use the imagegen tool directly, do NOT web search"; (3) lançar N+1 tentativas para compensar descarrilamentos.

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** `compile-bridges.sh` **abortava silenciosamente** no primeiro ficheiro alterado. Causa: `set -euo pipefail` + `((count++))` — quando `count` vai de 0→1, a expressão aritmética retorna o valor pré-incremento (0), cujo exit status é 1, accionando `set -e`. Resultado: `/save` PASSO 6 (recompilar bridges) falhava sempre que havia mudanças a propagar, deixando AGENTS.md / GEMINI.md / .codex/agents desactualizados sem aviso. | **Componente afectado:** `.claude/scripts/compile-bridges.sh` (linhas dos 3 `((count++))`) | **Fix sugerido:** ✅ APLICADO — substituído `((count++))` por `count=$((count + 1))` (não devolve exit 1). Compiler agora corre limpo: 11 skills, 26 codex agents, índices regenerados.

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** `agy`/Gemini (nano_banana) **só gera imagens 1:1 (1024×1024)** — ignora completamente pedidos de aspect ratio no prompt (16:9 devolveu quadrado em 2 tentativas). O doc de `img-gen-google` não avisava, levando a retrabalho (gerar → descobrir 1:1 → estender com codex, o que suaviza o texto dos rótulos). | **Componente afectado:** `.claude/agents/img-gen-google.md` + skill `img-gen` (regra de routing) | **Fix sugerido:** ✅ APLICADO (doc agente) — nota de que agy = só 1:1; para ratios não-quadrados ou texto nítido em wide, usar `img-gen-openai` (gpt-image-2 faz 16:9 nativo ~1672×941; upscale 2K via `ffmpeg scale=2048:1152:flags=lanczos`). Falta espelhar na skill `img-gen` (router deve preferir OpenAI quando o ratio ≠ 1:1).

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** gpt-image-2 (codex) não produz "2K" — sai ~1672×941 (16:9 correcto mas sub-2K). Entrega de "2K" exige upscale externo. | **Componente afectado:** `img-gen-openai` / expectativa de resolução | **Fix sugerido:** Documentar resolução nativa (~1.6MP) e o passo de upscale ffmpeg lanczos para 2048×1152 quando o cliente pede 2K. ✅ nota adicionada ao doc img-gen-google.
