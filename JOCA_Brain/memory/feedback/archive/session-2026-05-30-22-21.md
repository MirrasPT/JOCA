---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-05-30
project: bigorna-2026
---

Sessão: construção completa do admin Filament/Laravel de uma loja online (10 módulos) para o Bigorna 2026. Gaps do toolkit JOCA revelados:

**Categoria:** missing-skill | **Severidade:** high | **Descrição:** Não existe cobertura para gateways de pagamento portugueses (ifthenpay: Multibanco/MB WAY/Cartão) nem para faturação certificada PT (Moloni, Vendus, InvoiceXpress). O agente `payment-integration` é Stripe-only. Construí ifthenpay + Moloni do zero a partir das docs públicas. | **Componente afectado:** `payment-integration` (agent), trigger map | **Fix sugerido:** Skill `portugal-payments` (ifthenpay + MB WAY/Multibanco) e `portugal-invoicing` (Moloni/InvoiceXpress/Vendus, comunicação AT, notas de crédito). Ou estender `payment-integration` com secção PT.

**Categoria:** skill-improvement | **Severidade:** high | **Descrição:** A skill `filament` não documenta gotchas críticos do v5 que dão fatal só em runtime: (1) `$navigationGroup` em Page/Resource exige tipo `string|UnitEnum|null` (não `?string`); (2) enum `getIcon()` deve devolver o Heroicon enum CASE, não `->value` ("Svg not found"); (3) estrutura delegada gerada (Schemas/Tables/RelationManagers). Perdi tempo a apanhar estes em runtime. | **Componente afectado:** `.claude/skills/filament.md` | **Fix sugerido:** Adicionar secção "v5 type gotchas" + nota sobre enums HasIcon. (Já guardado em memory/feedback/filament-v5-gotchas.md.)

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Instalar Filament Shield (RBAC): `shield:install` NÃO migra a tabela do spatie → `shield:generate` falha ("permissions doesn't exist"). E as policies geradas partem TODOS os testes Filament (403) a menos que se use `Gate::before` super_admin. Não documentado em nenhuma skill. | **Componente afectado:** `filament` ou `auth` skill | **Fix sugerido:** Documentar sequência Shield: install → publish spatie migration → migrate → generate → super-admin; + padrão `Gate::before(super_admin)` para testes. (Guardado em memory/feedback/filament-shield-testing.md.)

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** WebFetch não consegue ler docs de API renderizadas por JavaScript (Swagger/Redoc/Scalar): falhou em api.fiz.co, ifthenpay.com/docs e moloni.pt/dev (conteúdo só no cliente). Tive de usar o contrato conhecido da API. Repetiu-se em 3 fornecedores. | **Componente afectado:** WebFetch / research workflow | **Fix sugerido:** Quando WebFetch devolve página vazia/título-só, tentar automaticamente paths de spec OpenAPI (/openapi.json, /swagger.json, /v1/openapi.json) e, se falhar, pedir ao utilizador o spec via DevTools. Ou usar firecrawl (render JS) para docs de API.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Laravel em Windows sem PHP no host (só Docker/Sail): o script `./vendor/bin/sail` é bash e não corre em PowerShell; é preciso `docker compose` directo + `$env:WWWUSER/WWWGROUP` + fix de perms storage (PHP 8.5 tempnam 500). | **Componente afectado:** `laravel-specialist` / docs de setup | **Fix sugerido:** Nota no laravel-specialist sobre Sail-em-Windows-via-docker-compose. (Guardado em memory/feedback/laravel-sail-windows.md.)
