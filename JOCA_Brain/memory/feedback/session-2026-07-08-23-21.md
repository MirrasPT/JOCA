---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: bodegas-do-campo
---

**Categoria:** missing-skill | **Severidade:** medium | **Descrição:** Não existe playbook para OPERAR/EDITAR um WordPress remoto que só tem acesso **FTP + wp-admin** (sem token cPanel/UAPI, sem SSH/WP-CLI, DB não acessível de fora). A skill `cpanel` assume token UAPI; `deploy-cpanel`/`deploy-vps` são deploy, não operação. | **Componente afectado:** skills/cpanel, skills/deploy-cpanel | **Fix sugerido:** documentar o padrão provado esta sessão — subir script PHP one-shot que faz `require wp-load.php` + `$wpdb` (search-replace/queries/plugin-mgmt), disparar via HTTPS com `?key=<token>&mode=dry|run`, **apagar no fim**; guardar via UAPI-less. Candidato a skill `wp-remote-ops` (ou secção na `cpanel`).

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Upload FTPS (explícito) de ficheiro ~30KB via `curl --ssl-reqd` falha com **451** no canal de dados TLS e **trunca o destino a 0 bytes** — partiu o CSS de um site live momentaneamente. Ficheiros pequenos passam. | **Componente afectado:** qualquer workflow de deploy por FTP (deploy-cpanel, scripts ad-hoc) | **Fix sugerido:** documentar `curl --ftp-ssl-control` (TLS só no controlo) para ficheiros grandes + regra "verificar tamanho no servidor após cada upload FTP". Já no Brain.
