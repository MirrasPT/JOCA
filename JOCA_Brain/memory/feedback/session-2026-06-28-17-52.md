---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-28
project: joca (toolkit)
---

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** O Brain log (`joca-brain.mjs`) e os checkpoints (`joca-checkpoint.mjs`) chaveiam o "projecto" por `basename(git rev-parse --show-toplevel)` (`currentSlug`, joca-brain.mjs:52-58). Quando se trabalha num projecto cujo repo real está fora do workspace JOCA (ex.: TCG em `C:\Users\renat\Projetos\tcg`) mas o cwd dos comandos reverte para `JOCA_Brain`, o toplevel resolvido é `JOCA_FINAL` → decisões/aprendizagens/checkpoints de **vários sub-projectos caem todos no mesmo balde `JOCA_FINAL.*`**. Resultado: o recall no arranque (`session-intake` hook → `joca-brain recall`) mistura TCG + JOCA_OS + toolkit numa só lista, e o utilizador vê "ele fala dos dois ao mesmo tempo". Os ficheiros de prosa (`memory/projects/*.md`) NÃO sofrem disto (o `/save` resolve o nome do projecto correctamente). | **Componente afectado:** `.claude/scripts/joca-brain.mjs` (`currentSlug`), `.claude/scripts/joca-checkpoint.mjs` (mesmo padrão), `.claude/commands/save.md` (PASSO 2c chama `decide`/`learn`/checkpoint SEM `--slug`) | **Fix sugerido:** (1) `/save` (PASSO 2c) detecta o nome do projecto (já o faz para `memory/projects/<nome>.md`) e passa-o como `--slug <nome>` a TODOS os comandos `joca-brain decide/learn` e `joca-checkpoint save`. (2) Melhorar o fallback de `currentSlug`: quando o git toplevel é o próprio workspace JOCA (`JOCA_FINAL`/`JOCA_Brain`) mas a tarefa é de outro projecto, exigir `--slug` explícito em vez de assumir `JOCA_FINAL`. (3) Migração one-off: separar as entradas TCG do balde `JOCA_FINAL` para um balde `tcg` (script com backup dos `.jsonl`). **Estado:** diagnosticado e classificado nesta sessão; migração aprovada pelo Renato ("Separar TCG agora + corrigir raiz") mas ainda por executar.
