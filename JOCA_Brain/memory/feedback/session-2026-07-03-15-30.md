---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-03
project: comfyui
---

**Categoria:** tool-reliability | **Severidade:** high | **Descricao:** `save_workflow` (MCP `comfyui`) grava JSON verbatim sem validar formato — se lhe passares um workflow em `format:"api"` (achatado, sem `nodes`/`links`/`definitions.subgraphs`) para um workflow que originalmente tinha subgrafo, o ficheiro é aceite e gravado, mas abre **vazio no canvas do browser** (a UI não consegue desenhar um grafo API-format). Não há aviso nem erro — só se descobre ao abrir no browser. | **Componente afectado:** plugin `comfy@comfyui-mcp`, tool `save_workflow` | **Fix sugerido:** o tool podia detectar (chaves top-level `nodes`+`links` vs `class_type` por node-id) e avisar/recusar quando o formato não bate com o que o browser precisa (UI-format), ou pelo menos documentar isto explicitamente na description da tool.

**Categoria:** tool-reliability | **Severidade:** medium | **Descricao:** `enqueue_workflow` (MCP `comfyui`) falhou com erro genérico "ComfyUI returned a non-OK status" para um workflow que o `validate_workflow` tinha validado sem problemas e que o POST directo a `/prompt` (mesmo payload, envelope `{"prompt":{...}}`) aceitou sem erro. Falha intermitente/silenciosa — perdeu-se tempo a assumir que o workflow estava mal (chegou a gerar imagem errada de um teste anterior por confusão de qual prompt tinha corrido). | **Componente afectado:** plugin `comfy@comfyui-mcp`, tool `enqueue_workflow` | **Fix sugerido:** tool devia propagar o corpo do erro HTTP real do ComfyUI (não só "non-OK status"), para diferenciar erro de workflow vs falha de transporte/timeout do próprio tool.

**Categoria:** tool-reliability | **Severidade:** medium | **Descricao:** `restart_comfyui` (MCP `comfyui`) pára o processo mas falha sistematicamente a relançar quando o processo original foi arrancado via `run_nvidia_gpu.bat` (não via `start_comfyui`) — erro `spawn ComfyUI\main.py ENOENT` (path relativo errado, falta o `cwd`/base correcto do portable). Já é um gotcha conhecido (documentado em `memory/projects/comfyui.md` desde 2026-06-21), continua a reproduzir-se 2026-07-03. | **Componente afectado:** plugin `comfy@comfyui-mcp`, tool `restart_comfyui`/`start_comfyui` | **Fix sugerido:** guardar o `cwd` absoluto do processo original (não só o comando) ao detectar/adoptar um ComfyUI já em execução, para o restart conseguir replicar o spawn correctamente.

**Categoria:** tool-reliability | **Severidade:** low | **Descricao:** `install_custom_node` (MCP `comfyui`, via ComfyUI-Manager API) devolveu `405 Method Not Allowed` em `/v2/manager/queue/task` ao tentar instalar um repo git que não está na registry oficial (`nova452/ComfyUI-ConditioningKrea2Rebalance`). Contornado com `git clone` manual para `custom_nodes/`. | **Componente afectado:** plugin `comfy@comfyui-mcp`, tool `install_custom_node`, ou a própria API do ComfyUI-Manager V3.41 instalado | **Fix sugerido:** a tool podia detectar o 405 e fazer fallback automático a `git clone` directo (já tem `source:"git"` como opção — só falta o fallback quando a API do Manager rejeita o método).

**Categoria:** skill-improvement | **Severidade:** medium | **Descricao:** Nenhuma skill/agente `comfy:*` documenta o gotcha "workflows com subgrafo têm de ser editados em `format:"ui"`, nunca `"api"`, e os links do grafo exterior são arrays posicionais `[id,origin_id,origin_slot,target_id,target_slot,type]` enquanto os links DENTRO do subgrafo são objectos `{id,origin_id,...}`" — descoberto por tentativa-erro nesta sessão (2ª vez que acontece: já tinha acontecido com o `Ideogram4_t2i_gemma` a 2026-06-21, mas ficou só em `memory/projects/comfyui.md`, não numa skill reutilizável). Qualquer workflow novo do `comfyui-workflow-templates` (Krea2, Ideogram4, e provavelmente futuros) usa subgrafos — este é um padrão recorrente, não um caso único. | **Componente afectado:** skill `comfy:comfyui-core` (ou nova skill `comfy:subgraph-editing`) | **Fix sugerido:** extrair a receita completa (já escrita em `memory/projects/comfyui.md` secção "Krea 2 — variante direct + fix NSFW") para uma skill/reference reutilizável do plugin `comfy@comfyui-mcp`, para não ter de ser redescoberta a cada projecto/sessão.
