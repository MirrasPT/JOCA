---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-07
project: livro-de-elogios
---

**Categoria:** workflow-gap | **Severidade:** high | **Descrição:** O workflow `analisar-plataforma` (8 lentes + verificação adversarial por Critical/High = 20-40 agentes, ~2M tokens/run) bateu no rate-limit/créditos nas 3 execuções ANTES da fase de síntese automática correr — perdendo o relatório final apesar de 20+ agentes terem concluído. Tive de consolidar o relatório à mão a partir do `journal.jsonl`. Gap: workflows pesados não têm mecanismo para persistir resultados parciais nem uma síntese resiliente que corra com o que já existe quando o orçamento acaba. | **Componente afectado:** template de workflows pesados (analisar-plataforma.js) + doutrina de orquestração (rules/pipelines.md, rules/orchestration-patterns.md) | **Fix sugerido:** (1) padrão de workflow pesado deve escrever cada findings-set para disco (`.joca/intermediate/`) à medida que os agentes terminam, não só no fim; (2) fase de síntese defensiva — se o agente de síntese falhar por créditos, o script cai num fallback que gera um relatório mínimo (dump estruturado do journal) em vez de devolver `resumo:null`; (3) documentar o padrão "consolidar do journal.jsonl à mão" em rules/pipelines.md como recuperação canónica quando a síntese não corre.

**Categoria:** command-improvement | **Severidade:** medium | **Descrição:** O workflow custom criado em `.claude/workflows/` não é invocável por `{name:'x'}` — a resolução por nome só cobre os built-ins (deep-research, code-review). Só funciona por `{scriptPath:'<abs>'}`. Além disso o runtime entregou o `args` como string JSON em vez de objecto, rebentando o script. Ambos são contrato do Workflow tool não-óbvio. | **Componente afectado:** convenção de autoria de workflows (INDEX.md secção Workflows) | **Fix sugerido:** documentar na doutrina de workflows que (a) workflows de `.claude/workflows/` se invocam por `scriptPath`, não por `name`; (b) todo o script deve normalizar `const A = typeof args==='string'?JSON.parse(args):(args??{})` no topo. (Já registado no Brain como aprendizagem; falta anexar à referência de autoria.)
