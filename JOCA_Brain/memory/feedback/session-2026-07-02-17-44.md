---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-02
project: livro-de-elogios (Pacote Cartaz)
---

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** A skill `hyperframes` não menciona que em Windows os comandos `inspect`/`validate`/`render` falham com `spawn UNKNOWN` porque tentam descarregar/executar o `chrome-headless-shell`. Perdi vários ciclos a testar `CHROME_PATH`/`PUPPETEER_EXECUTABLE_PATH`/`PRODUCER_HEADLESS_SHELL_PATH` (nenhum funciona) até descobrir por leitura do `cli.js` que a var certa é `HYPERFRAMES_BROWSER_PATH`. | **Componente afectado:** `.claude/skills/hyperframes.md` (secção CLI Workflow) | **Fix sugerido:** adicionar nota Windows: `set HYPERFRAMES_BROWSER_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"` (PowerShell `$env:HYPERFRAMES_BROWSER_PATH=...`) antes de `inspect`/`validate`/`render`; sintoma = `✗ ... failed: spawn UNKNOWN`.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** A skill `deploy-vps` não avisa que após criar o registo A no Cloudflare o resolver local demora a propagar, dando `curl` com HTTP `000` (parece deploy falhado quando está OK). | **Componente afectado:** `.claude/skills/deploy-vps.md` (secção 7 DNS / verificação) | **Fix sugerido:** nota — verificar o site pelo edge directamente enquanto o DNS propaga: `curl --resolve <host>:443:<cf-edge-ip> https://<host>/` (IP de edge via `nslookup <host> 1.1.1.1`).

**Categoria:** missing-skill | **Severidade:** low | **Descrição:** Não existe regra/skill que capture o padrão "não usar nomes `ad-*`/`anuncios`/`sponsor`/`banner-ad` em markup de páginas próprias" — ad-blockers (uBlock/Ghostery) escondem secções legítimas por filtros cosméticos. Aconteceu no `apresentacao.html` (secção de campanhas invisível). | **Componente afectado:** skill `frontend`/`html-review` ou `landing-page` | **Fix sugerido:** adicionar anti-pattern à checklist de `frontend`/`html-review`: evitar identificadores que casem filtros de EasyList; usar prefixos neutros (`mkt-`, `promo-` não, `campaign-`).
