---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: joca
---

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** Após a migração Windows→Mac (`3067e6d`), os 12 comandos de hook em `.claude/settings.json` continham paths absolutos Windows (`C:/Users/renat/Desktop/JOCA_FINAL/JOCA_Brain/...`). Em macOS o `node` trata `C:/...` como caminho **relativo** e junta-o ao cwd → `MODULE_NOT_FOUND` em SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/Stop, ruidoso a CADA mensagem. | **Componente afectado:** `.claude/settings.json` (gerado/migrado); `/install` (PASSO dos hooks usa exemplo de path Windows) e `/migrate`. | **Fix sugerido:** o `/install` e `/migrate` deviam **reescrever os paths dos hooks para o path absoluto da máquina actual** (detectar `pwd` do JOCA_Brain) em vez de manter o path da máquina de origem. Alternativa mais robusta: usar `$CLAUDE_PROJECT_DIR` OU um resolver relativo-ao-settings — mas há a nota histórica de que no Windows `$VAR` pode não expandir em cmd (ver joca.md 2026-07-03 cont.2). Mínimo: passo de "reparar paths dos hooks" no `/migrate`.

**Categoria:** doc-gap | **Severidade:** high | **Descrição:** O MCP `playwright` estava documentado em `memory/tools/mcps.md` e registado com o pacote `@anthropic-ai/mcp-server-playwright`, que **não existe no npm (404)** — nunca existiu. Isto explica o "pode falhar a ligar" documentado (Windows incluído): não era flakiness, era um pacote fantasma. O pacote oficial de browser-MCP é `@playwright/mcp` (Microsoft). | **Componente afectado:** `memory/tools/mcps.md` (corrigido nesta sessão), `skills/browser-automate.md` (referência), `/install` (setup de MCPs). | **Fix sugerido:** garantir que `/install` regista `@playwright/mcp@latest`; auditar outras referências ao pacote fantasma no toolkit.

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** A lista "CLIs externos" no `~/CLAUDE.md` e a checklist do `/install` estavam **incompletas** — faltavam CLIs que o Renato usa de facto (documentados só em `memory/projects/*`/`feedback/*`): `ntn` (Notion), `wix`, `wp-cli`, `zmail-cli`, e o plugin `last30days` (+ Digg CLI). Resultado: numa migração não há registo único do que reinstalar. | **Componente afectado:** `~/CLAUDE.md` (linha CLIs externos), `/install` (FASE 3 CLIs Externos). | **Fix sugerido:** criado `memory/tools/clis.md` como fonte de verdade; o `/install` e `/save` deviam sincronizar contra esse ficheiro. Considerar um passo no `/install` que deteta CLIs instalados e reconcilia com `clis.md`.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** No Mac, registar o MCP `markitdown` com `python -m markitdown_mcp` (como o `install.md` manda) falha silenciosamente porque o pip do homebrew python é externally-managed (PEP 668). O comando correcto no Mac é `uvx markitdown-mcp`. | **Componente afectado:** `/install` (setup markitdown), `memory/tools/mcps.md` (corrigido). | **Fix sugerido:** `/install` detetar macOS+homebrew-python e usar `uvx`/`uv tool` por omissão para MCPs/CLIs Python.
