---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-29
project: simao-sina
---

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** `mcp__trypost__list-posts-tool` (status=scheduled, limit=100) estoura sempre o limite de tokens (~55-61k chars) e cai em ficheiro, obrigando a jq externo a cada listagem. Aconteceu 3x nesta sessão. O payload inclui caption completa + avatares + objecto social_account inteiro por plataforma por post — verboso demais para uma listagem. | **Componente afectado:** MCP TryPost (`list-posts-tool`) + skill `social-scheduler` | **Fix sugerido:** Documentar na skill `social-scheduler` o padrão "list-posts grande → ler do ficheiro guardado com jq" (já parcialmente em memória) E preferir filtros/limites pequenos. Idealmente pedir upstream ao TryPost um modo compacto (campos id/scheduled_at/status/platforms.enabled) ou filtro por plataforma. Workaround validado: `jq` sobre o ficheiro tool-results (`any(.platforms[]; .platform=="tiktok" and .enabled==true)`).

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** A skill `social-scheduler` cobre só o fluxo Direct Post do TryPost. Esta sessão provou que (a) o TryPost não expõe o caminho inbox/draft do TikTok (`video.upload`), e (b) o token social está ENCRIPTADO na BD (Laravel cast `encrypted`) — qualquer chamada raw à API TikTok por fora do TryPost exige decriptar via `artisan tinker`, não `select` directo. | **Componente afectado:** `.claude/skills/social-scheduler.md` | **Fix sugerido:** Adicionar secção "TikTok além do TryPost": Direct Post vs inbox/draft (scopes `video.publish` vs `video.upload`); sandbox não entrega inbox/draft (só produção); restrição unaudited = SELF_ONLY + conta privada; token encriptado → `artisan tinker` na VPS, nunca `select`. (Detalhe completo em `memory/projects/simao-sina.md`.)
