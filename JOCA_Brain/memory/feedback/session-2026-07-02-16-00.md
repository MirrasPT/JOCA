---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-02
project: livro-de-elogios
---

**Categoria:** doc-gap | **Severidade:** medium | **Descricao:** `rules/workflows-and-tooling.md` documenta o gotcha MEGAsync conflict-copy só no contexto de "workflows que escrevem ficheiros em paralelo" (múltiplos agentes). Nesta sessão o mesmo bug (`ficheiro.ts` → `ficheiro(1).ts`, apagando o canónico) aconteceu a meio de uma sessão **single-agent sequencial** (só eu a editar, um ficheiro de cada vez) — o MEGAsync interceptou um save normal do editor/Write tool. A causa real parece ser qualquer escrita a um ficheiro que o MEGAsync esteja a sincronizar ao mesmo tempo (ex.: sync de outro dispositivo, ou o próprio MEGAsync a re-verificar o ficheiro), não especificamente concorrência entre agentes. | **Componente afectado:** `rules/workflows-and-tooling.md` (secção "Workflow tool" / gotcha MEGAsync) | **Fix sugerido:** alargar a nota para cobrir também sessões single-agent — sugerir pausar o MEGAsync (ou mover o projecto para fora da pasta sincronizada) durante QUALQUER sessão longa de edição activa neste tipo de pasta, não só workflows paralelos. Detecção rápida: erro de build do bundler ("Failed to resolve import ... Does the file exist?") a meio de uma sessão que estava a correr bem — verificar `find . -iname "*(1)*"` antes de assumir um bug de código.

**Categoria:** tool-reliability | **Severidade:** medium | **Descricao:** MCP Playwright falhou com `Error: Browser is already in use for <profile>, use --isolated to run multiple instances` no primeiro `browser_navigate` da sessão. Causa: processo `chrome.exe` órfão de uma sessão anterior, ainda a segurar o profile (`--user-data-dir=...\ms-playwright-mcp\mcp-chrome-<id>`, visível via `Get-CimInstance Win32_Process -Filter "Name='chrome.exe'"` filtrando `CommandLine -like '*mcp-chrome*'`). Resolvido matando esse PID + o `crashpad-handler` filho (mesmo user-data-dir) e repetindo o navigate — funcionou de imediato. | **Componente afectado:** MCP Playwright (harness), possivelmente skill `browser-automate` | **Fix sugerido:** documentar este diagnóstico+fix no skill `browser-automate` (ou nas instruções gerais de Playwright) como primeiro passo de recuperação quando `browser_navigate` falha com "already in use" — evita perder um ciclo a tentar Chrome-in-browser/outros fallbacks antes de perceber que é só um processo preso.

**Categoria:** tool-reliability | **Severidade:** low | **Descricao:** A extensão Chrome-in-browser (`mcp__claude-in-chrome__*`) esteve indisponível ("Browser extension is not connected") em 3 tentativas ao longo da sessão (início, e ao testar o widget). O fallback documentado (`rules/workflows-and-tooling.md` — Playwright, depois `Start-Process`) funcionou bem de cada vez, mas representa custo de ciclos repetido. | **Componente afectado:** extensão Chrome-in-browser (ambiente local) | **Fix sugerido:** nenhuma acção de código — apontar para o Renato que a extensão parece desligar/não reconectar entre sessões; pode valer a pena verificar se fica sempre activa no perfil Chrome usado.

**Categoria:** workflow-gap | **Severidade:** low | **Descricao:** Tarefa de 16 pontos cross-stack (claramente via D — workflow, pelos thresholds de `rules/task-intake.md`) foi conduzida directamente pelo main loop, sequencialmente, sem `Workflow`/fan-out — decisão deliberada dado (a) o utilizador pediu explicitamente "trabalha ponto a ponto" (sequencial), e (b) o risco de MEGAsync com escrita paralela em ficheiros partilhados (ver primeiro item acima) tornava fan-out arriscado nesta pasta especificamente. Não é um gap de ferramenta, mas vale registar como padrão válido: tarefa grande + pasta MEGAsync + pedido explícito de sequência → preferir main loop sequencial a workflow paralelo, mesmo quando os thresholds apontam para D. | **Componente afectado:** `rules/task-intake.md` / `rules/orchestration-patterns.md` | **Fix sugerido:** nenhuma mudança de código necessária; considerar acrescentar esta excepção ("pasta MEGAsync + preferência de sequência explícita → sequencial mesmo em via D") como nota em `orchestration-patterns.md` se o padrão se repetir noutras sessões.
