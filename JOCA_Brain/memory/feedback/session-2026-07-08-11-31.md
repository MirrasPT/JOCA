---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: JOCA
---

**Categoria:** discovery-gap | **Severidade:** medium | **Descrição:** O gate `/resume` vs `/init-project` do JOCA_OS decide por `existsSync(<pathRegistado>/CLAUDE.md)`, mas o path registado (`projects.json`) pode apontar para a pasta-mãe enquanto o `CLAUDE.md` vive numa subpasta → `/init` perpétuo. Agravado por migração Windows→Mac (paths stale). | **Componente afectado:** `JOCA_OS/backend/session-manager.ts` (spawn) + `data/projects.json` | **Fix sugerido:** (feito parcial) detecção agora aceita `CLAUDE.md`/`AGENTS.md`/`.claude/`. Melhor ainda: ao registar/editar um projecto na UI, validar que o path contém um marcador de init OU avisar; ou fazer o `/init-project` gravar um `.claude/` sentinela no path exacto que o backend verifica.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** `/init-project.md` diz "corre a partir da pasta do projecto", mas no JOCA_OS o Claude corre SEMPRE em `JOCA_Brain` (cwd) e recebe o path por argumento. Se o comando criar o `CLAUDE.md` relativo ao cwd em vez do arg, cria-o no sítio errado. | **Componente afectado:** `.claude/commands/init-project.md` | **Fix sugerido:** tornar explícito que o alvo é o path do argumento (`$ARGUMENTS`), não o cwd.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** Detecção de prontidão de TUIs (Claude Code) por marcadores adivinhados falha silenciosamente quando a versão muda (marcadores errados → cai no cap de timeout). | **Componente afectado:** `JOCA_OS/backend/session-manager.ts` (waitForClaudeReady) | **Fix sugerido:** documentar o padrão "capturar output real via node-pty antes de fixar marcadores"; considerar marcador robusto por crescimento-de-buffer+quiet como fallback.
