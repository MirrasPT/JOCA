---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-03
project: elite-imagens-db
---

**Categoria:** tool-reliability | **Severidade:** high | **Descricao:** Sub-agentes gerais (general-purpose) que lançam um processo CLI de longa duração em background (ex. `agy` para geração de imagem) e depois tentam "esperar pela conclusão" terminando o próprio turno ficam presos num ciclo de falso-progresso: reportam `status: completed` com texto tipo "a aguardar em silêncio, sem acção necessária" repetidamente, sem nunca bloquear de facto nem verificar o resultado real. Cada ciclo destes gasta ~100-180k tokens de subagente sem produzir progresso. Aconteceu 3× nesta sessão (Alkimia Rosé, Bracaris Rosé, Royal Douro redo) antes de eu intervir manualmente instruindo um loop bloqueante numa única chamada Bash (`sleep`/poll até hora alvo). | **Componente afectado:** Agent tool / padrão de brief para sub-agentes que dependem de processos externos assíncronos. | **Fix sugerido:** Documentar em `rules/workflows-and-tooling.md` (ou onde vivem os gotchas de sub-agente) o padrão correcto: quando um sub-agente precisa de esperar por um processo externo (quota reset, build, render), o brief deve instruir explicitamente "corre UMA chamada Bash com loop bloqueante (sleep até hora alvo ou condição), nunca termines o turno para 'esperar em silêncio' — não recebes notificações como o main loop recebe."

**Categoria:** skill-improvement | **Severidade:** high | **Descricao:** Os playbooks de marca deste projecto (`alkimia.md`, `bracaris.md`) descrevem elementos visuais críticos (estrutura de cor do rótulo) em linguagem vaga ("giant stylised B with floral emblems") em vez de uma descrição literal/estrutural derivada de olhar para a imagem de referência real. Isto levou o Gemini a inverter a estrutura de cor do rótulo Bracaris (gerou fundo branco + "B" em contorno, quando a garrafa real tem um painel sólido rosa em forma de B) — e o próprio sub-agente "verificou contra a referência" e aceitou como correcto. Só foi apanhado porque o cliente reconheceu o produto real e reclamou. | **Componente afectado:** `.claude/agents/{alkimia,bracaris}.md` no projecto `elite-imagens-db` (não uma skill global do JOCA, mas o padrão é generalizável a qualquer playbook de geração de imagem de marca). | **Fix sugerido:** Regra geral para playbooks de brand/produto: descrições de elementos gráficos críticos (logos, rótulos, padrões) devem ser transcrições estruturais literais (cor sólida vs. outline, inset vs. floating, posição relativa exacta) escritas depois de o autor do playbook efectivamente ter olhado para a imagem — não paráfrases genéricas do tipo "stylised X with Y". Já corrigido nos 2 ficheiros afectados nesta sessão.

**Categoria:** doc-gap | **Severidade:** medium | **Descricao:** `royal-douro.md` continha uma lista de "ambientes rotativos" (iate, jato privado, paddock F1, penthouse, Michelin, galeria, lounge de charutos) que tinha sido testada e abandonada em Junho 2026 a favor de um estilo diferente (editorial/candid, 3 conceitos: Pour/Jantar/Editorial), documentado apenas no `CLAUDE.md` do projecto numa secção "briefing corrigido". O agente nunca foi actualizado para reflectir a correcção — um sub-agente que leia só `royal-douro.md` (sem cruzar com `CLAUDE.md`) teria usado a lista abandonada. | **Componente afectado:** Padrão de correcção de playbook em `.claude/agents/*.md` de projecto. | **Fix sugerido:** Quando uma correcção de comportamento é descoberta a meio de um ciclo e documentada num ficheiro separado (CLAUDE.md, nota solta), fundir a correcção directamente no playbook/agente afectado no mesmo momento — não deixar como "patch externo" que depende de o próximo executor cruzar dois ficheiros. Já corrigido nesta sessão.

**Categoria:** tool-reliability | **Severidade:** low | **Descricao:** Quota do Gemini (`gemini-3.1-flash-image` via `agy`) esgotou a meio do ciclo mensal (429 RESOURCE_EXHAUSTED), bloqueando 2 gerações simultaneamente com uma janela de reset de ~30-90 min. Não há forma de estimar isto antecipadamente nem de distribuir a carga ao longo do tempo dentro da mesma sessão. | **Componente afectado:** `img-gen-google` / dependência externa (quota da conta Gemini via agy). | **Fix sugerido:** Nenhum fix de ferramenta directo (é uma quota de terceiros) — mas vale considerar espaçar os disparos de geração de imagem em ciclos de 8 imagens (não disparar as 8 em paralelo total) para reduzir a probabilidade de esgotar a quota a meio, ou documentar o padrão de "retry com sleep bloqueante até `quotaResetTimeStamp`" como resposta padrão a 429 nos playbooks de img-gen.
