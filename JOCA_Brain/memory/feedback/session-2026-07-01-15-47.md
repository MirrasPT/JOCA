---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-01
project: livro-de-elogios
---

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Um workflow multi-agente de *wiring* (ligar frontend a backend real) validou tudo por código + curl + tsc + Pest e reportou "verde", mas deixou passar um bug de runtime que só aparece no browser: a página Créditos ficava presa em "A carregar…" por um `GET /subscriptions/current` a dar 403 (policy). Os agentes do Workflow **não navegam** (sem Playwright/Chrome), por isso curl como admin passava mas o browser como outro utilizador (ownership/policy diferente) falhava. | **Componente afectado:** `rules/pipelines.md` / `rules/orchestration-patterns.md` (pipelines de wiring/frontend) + doutrina do Workflow tool. | **Fix sugerido:** Adicionar às pipelines de wiring/frontend um passo final OBRIGATÓRIO de **smoke no browser conduzido pelo main loop** (não por agente), logado como o utilizador-alvo REAL, varrendo cada página por erros de consola (403/404/hang), DEPOIS do workflow. Documentar que "workflow verde (tsc/pest/curl) ≠ funcional" para trabalho com UI. Encadear como `chain:` do master-orchestrator quando o output tem frontend.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** O auto-runner de loop-until-dry parou no cap de rondas (MAX_ROUNDS=3) com a última ronda ainda a corrigir 9 ficheiros — a contagem de "corrigíveis novos" não convergia a 0 (28→8→9) porque cada ronda destapa itens mais profundos. Sem browser-smoke, o critério de paragem "dry" é sobre findings de código, não sobre funcionalidade real. | **Componente afectado:** desenho de workflows de loop (`rules/pipelines.md`). | **Fix sugerido:** Para loops de correcção com UI, o critério de convergência deve incluir um sinal funcional (smoke verde no browser), não só "0 findings de código novos". O main loop faz o smoke e decide continuar/parar — os agentes não têm esse sinal.
