---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-29
project: bodegas-do-campo
---

**Categoria:** command-improvement | **Severidade:** medium | **Descrição:** `/init-project` não verifica se o projecto já está inicializado no JOCA antes de correr o fluxo completo. Foi invocado sobre Bodegas do Campo — que já tem `memory/projects/bodegas-do-campo.md`, `CLAUDE.md` próprio e entrada na tabela de `~/CLAUDE.md` — e o comando arrancaria as fases todas (Q1 natureza, stack, skills, CLIs, regenerar memória), trabalho redundante. A detecção de "já inicializado" e a oferta refresh-vs-re-init foi feita manualmente, não pelo comando. | **Componente afectado:** `.claude/commands/init-project.md` | **Fix sugerido:** Adicionar um passo 0 antes da Fase 1: verificar se existe `memory/projects/<nome>.md` (resolvido pelo `directorio:` no frontmatter) OU `CLAUDE.md` na pasta-alvo. Se sim, `AskUserQuestion` com 4 vias (refrescar estado / re-init completo / trabalhar no projecto / cancelar) e só correr o fluxo completo no caminho re-init. Espelha o que `/save` já faz (resolver projecto por path) mas na entrada.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** `/init-project` corre `git` no directório-alvo assumindo repo na raiz; aqui o repo git vive num subdir (`website_wordpress/`) e a raiz (`D:\Mega\Bodegas do Campo (Espanhol)`) não é repo → `git` falhou com exit 128. O comando não antecipa projectos cujo código/repo está num subdir do path passado. | **Componente afectado:** `.claude/commands/init-project.md` (Branch [A] scan) | **Fix sugerido:** No scan inicial, se `git rev-parse` falhar na raiz, procurar 1 nível abaixo por `.git`/`CLAUDE.md`/`composer.json`/`package.json` e tratar esse subdir como a raiz efectiva do projecto (alinha com o `directorio:` que aponta para `website_wordpress`).
