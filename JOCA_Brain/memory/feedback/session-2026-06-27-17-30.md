---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: royal-douro
---

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** `gws gmail +send` recusa anexos fora do **cwd** (`--attach ... resolves to ... which is outside the current directory`, validationError 400). Não é óbvio na ajuda do `+send` (que só diz "total size limit 25MB"). Custou um ciclo: tentei anexar um PDF em `G:\...` e falhou; contornei com `( cd <pasta> && gws +send ... -a <nome-relativo> )`. | **Componente afectado:** agente `personal-comms` + `install.md` (linha `gws`) — qualquer fluxo que anexe ficheiros. | **Fix sugerido:** documentar no `personal-comms`/skill gws: anexos têm de estar no cwd → correr o `+send` a partir da pasta dos ficheiros (subshell `cd`), ou copiar o anexo para cwd primeiro. Body HTML grande passa bem por `--body "$(cat file.html)" --html` (aceita documento HTML completo, não só fragmentos apesar da nota da ajuda).

**Categoria:** skill-improvement | **Severidade:** medium | **Descrição:** Ao hospedar assets novos num site atrás de **Cloudflare** (cPanel SFTP), os URLs novos serviram o `index.html` do site (HTTP 200 mas `content-type: text/html`, `Cf-Cache-Status: HIT`) — o CF tinha cacheado o `404→index.html` (via `.htaccess` SPA-rewrite) ANTES do ficheiro existir, com `max-age=14400` (4h). Diagnóstico não-óbvio (parece upload falhado, mas o ficheiro está lá). | **Componente afectado:** skills `cpanel` / `deploy-cpanel` (e `deploy-vps` por analogia Cloudflare). | **Fix sugerido:** acrescentar gotcha "Cloudflare + assets novos": após upload, se `curl` devolver `text/html` num asset binário → é cache CF de um 404 antigo; confirmar origin com `curl '<url>?cb=RANDOM'`; servir via URL versionado `?v=N` (ou purgar cache CF). Aplica-se sempre que o docroot tem `.htaccess` com fallback `index.html` (sites SPA/static).

**Categoria:** missing-skill | **Severidade:** low | **Descrição:** Para construir um email HTML estático *sendable* (cold-reach/newsletter, table-based, inline styles, client-safe) não há skill — a `react-email` é exclusivamente React/`.tsx` (precisa de build + render). Hand-codei o HTML table-based seguindo as regras client-safe da `react-email`, mas o output dela (componentes React) não serve para entregar um `.html` colável num ESP/gws sem toolchain. | **Componente afectado:** `react-email` (cobre só React) — falta cobertura de "static HTML email". | **Fix sugerido:** ou uma skill `html-email` (table-based, inline CSS, MSO conditionals, pré-visualização via Chrome `--screenshot`, hosting de imagens absolutas + `?v=`), ou uma secção "Static HTML (no-build)" na `react-email` com um skeleton table-based pronto a colar.
