---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-30
project: livro-de-elogios
---

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** Um `Workflow` com fan-out paralelo (7 agentes de área a escrever ficheiros em simultâneo) correu numa pasta sob **MEGAsync** (`D:\Mega\...`). O MEGAsync, ao sincronizar durante a escrita concorrente, criou **conflict-copies `X(1).tsx`** e **apagou os ficheiros canónicos** a meio do run → partiu o `tsc -b`/`npm run build` global (imports a apontar para ficheiros inexistentes). O verify agent recuperou renomeando `X(1).tsx`→`X.tsx`, mas foi sorte de timing. | **Componente afectado:** `Workflow` tool + `rules/orchestration-patterns.md` (padrão "agentes escrevem para disco") + `rules/workflows-and-tooling.md` (gotcha Git+MEGAsync já existe mas só p/ git, não p/ workflows paralelos). | **Fix sugerido:** Adicionar regra explícita: **antes de lançar um Workflow com fan-out paralelo que ESCREVE ficheiros, verificar se a pasta-alvo está sob serviço de sync (MEGAsync/Dropbox/OneDrive/iCloud)**. Se estiver → (a) pausar o sync durante o run, OU (b) usar `isolation: 'worktree'` (worktree fora da pasta sync), OU (c) serializar a escrita (sem 2 agentes no mesmo directório ao mesmo tempo). Documentar no brief de orquestração e no `workflows-and-tooling.md`. Sintoma de diagnóstico: ficheiros `*(1).*` a aparecer + build a falhar com "cannot find module ./X" para ficheiros que existem.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Após um Workflow instalar **deps npm novas** (`qrcode.react`, `react-easy-crop`), o dev server Vite que já estava a correr serviu **504 "Outdated Optimize Dep"** (cache de optimize-deps stale) — parecia bug de código mas não era. | **Componente afectado:** checklist pós-workflow / `joca-ui-windows` / runbook de validação. | **Fix sugerido:** Quando um workflow/agente corre `npm i <nova-dep>`, o passo de validação deve **reiniciar o dev server** (ou `rm -rf node_modules/.vite`) antes do smoke runtime — senão o smoke reporta 504 falsos. Adicionar à doutrina de validação pós-implementação.
