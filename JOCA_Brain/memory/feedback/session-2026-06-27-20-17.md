---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-27
project: tcg
---

**Categoria:** tool-reliability | **Severidade:** high | **Descrição:** `mcp__plugin_comfy_comfyui__enqueue_workflow` devolve erro `"ComfyUI is not running. Use start_comfyui to start it first."` mesmo com o ComfyUI 100% saudável e a responder — `health_check`, `get_queue`, `get_history` e POST HTTP directo a `/prompt` funcionam todos na mesma sessão. A probe de readiness do `enqueue_workflow` está partida (falso negativo). Risco: levar a `start_comfyui` desnecessário → double-bind de porta. | **Componente afectado:** plugin `comfy@comfyui-mcp`, ferramenta `enqueue_workflow` | **Fix sugerido:** documentar fallback canónico (POST `http://127.0.0.1:8188/prompt` com `{"prompt": <wf>}`) na skill `comfy-mcp-workarounds`; ou usar a mesma probe de `health_check`/`get_queue` no `enqueue_workflow`.

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** `mcp__plugin_comfy_comfyui__list_output_images` e `view_image`/output helpers falham com `COMFYUI_PATH is not configured` — a env var não está setada para o MCP. Obriga a ler os PNG directo do disco (`D:\_Comfyui\ComfyUI_windows_portable\ComfyUI\output\`). | **Componente afectado:** config do MCP comfyui (env `COMFYUI_PATH`) | **Fix sugerido:** registar `COMFYUI_PATH=D:\_Comfyui\ComfyUI_windows_portable\ComfyUI` na config do MCP; ou a skill `comfy-mcp-workarounds` notar o path de output directo como fallback.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** o batch (38 imagens) foi conduzido por script Python+HTTP fora do MCP por causa do bug acima — funcionou bem (POST todos → poll `/queue` até 0 → copiar `<id>_00001_.png`). Padrão reutilizável para geração em lote no ComfyUI. | **Componente afectado:** skill `comfy-mcp-workarounds` / `img-gen` | **Fix sugerido:** acrescentar o padrão "batch via HTTP /prompt + poll /queue + copiar por filename_prefix" à skill de workarounds do ComfyUI.
