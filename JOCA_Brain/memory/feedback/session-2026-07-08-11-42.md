---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: bodegas-do-campo
---

**Categoria:** command-improvement | **Severidade:** medium | **Descrição:** `/init-project` corrido num projecto **já ligado ao JOCA** cai no Branch [A] ("já tem código") e re-corre o questionário todo, o que arrisca clobber de memória fresca + CLAUDE.md a funcionar. Foi preciso override manual: detectar "já ligado" (memória existe + em INDEX) e oferecer opções cirúrgicas (refresh Mac / root CLAUDE.md / re-init / limpar dup) em vez de seguir o fluxo. Isto já aconteceu 2× neste projecto (refresh 2026-06-29 + esta sessão). | **Componente afectado:** `.claude/commands/init-project.md` (Branch [A]) | **Fix sugerido:** No arranque, se `memory/projects/<nome>.md` já existe E está no INDEX → ramo "JÁ LIGADO": apresentar menu de reconciliação (refresh de ambiente / adicionar root CLAUDE.md / re-init destrutivo com aviso / limpar duplicados) em vez de re-correr Q1-Q7. Nunca reescrever memória existente sem confirmação.

**Categoria:** workflow-gap | **Severidade:** medium | **Descrição:** Migração de máquina PC(Windows)→Mac deixa paths stale (`D:\Mega\…`), interpretador errado (`python` vs `python3`) e portas antigas espalhados por memória + CLAUDE.md do projecto + `~/CLAUDE.md`. Não há comando/skill que faça esta reconciliação de ambiente sistematicamente — foi feita à mão via grep+edits. O `/migrate` existente é v1→v2 do toolkit, não migração de máquina. | **Componente afectado:** falta um passo/skill de "machine-refresh" (ou uma flag em `/init-project`) | **Fix sugerido:** Passo reutilizável que faz grep de paths de outro OS (`[A-Z]:\`, `/home/`, portas JOCA antigas) em memória + CLAUDE.md do projecto + `~/CLAUDE.md` e propõe substituição para os paths/convenções da máquina actual. Encaixa no ramo "JÁ LIGADO" do fix acima.

**Categoria:** discovery-gap | **Severidade:** low | **Descrição:** MEGAsync gera duplicados de conflito `Nome (1)/` que podem conter árvores inteiras (aqui 6,4 GB, `.wpress` de 315 MB duplicado) sem serem óbvios no `ls` da raiz. Nenhum passo do JOCA os sinaliza. | **Componente afectado:** `/init-project` scan (Branch [A]) e `/save` PASSO 8 | **Fix sugerido:** No scan de projecto, detectar irmãos com sufixo ` (N)` idênticos por conteúdo (diff/hash do `.wpress` ou contagem de ficheiros) e sinalizar como duplicado de sync candidato a limpeza (com o ganho de espaço).
