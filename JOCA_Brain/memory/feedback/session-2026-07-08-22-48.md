---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: cpanel-hosting
---

**Categoria:** discovery-gap | **Severidade:** medium | **Descrição:** As skills `cpanel`/`deploy-vps` documentam acesso Cloudflare via token `~/.cloudflare/*.json`, mas a migração PC→Mac (JOCA snapshot 2026-07-08) NÃO restaurou esse token nem o plugin `cloudflare@cloudflare`. Ao receber "vê no meu Cloudflare X", só descobri a ausência do token depois de procurar — sem pre-check, arriscava-se assumir acesso e falhar (ou fabricar). | **Componente afectado:** skills `cpanel`, `deploy-vps` + `/migrate` | **Fix sugerido:** (1) `cpanel`/`deploy-vps` fazem pre-check da existência do token CF e, se ausente, avisam "restaura ~/.cloudflare/*.json ou dá novo API token" ANTES de operar. (2) `/migrate` com checklist de credenciais externas a restaurar manualmente (tokens fora do repo: ~/.cloudflare/, ~/.ssh/*, keychains) e reporta quais faltam.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** Regra cPanel+Cloudflare reutilizável descoberta ao vivo (webmail/cpanel/mail nunca proxied/orange — devolve 503/502) não estava na skill `cpanel`. | **Componente afectado:** skill `cpanel` | **Fix sugerido:** 1 linha na skill `cpanel`: "Registos de serviço cPanel (webmail/cpanel/whm/mail) = sempre DNS-only (grey) na Cloudflare; proxied devolve 503/502."
