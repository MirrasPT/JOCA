---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: stickers-premium
---

**Categoria:** tool-reliability | **Severidade:** medium | **Descrição:** O browser-tool Claude-in-Chrome no Mac não permite validar layouts mobile (390px): (1) `resize_window` é travado pela largura mínima de janela do Chrome (~500px), e (2) o screenshot da extensão captura sempre a ~1288px logical independente do tamanho da janela — o media query nunca dispara. Resultado: impossível confirmar visualmente breakpoints mobile de uma página, tendo de recorrer a paridade de breakpoints + `overflow-x:hidden` como argumento indirecto. | **Componente afectado:** mcp__claude-in-chrome (resize_window + computer screenshot) / skills `frontend`, `design-review`, `browser-automate` | **Fix sugerido:** documentar na skill `browser-automate`/`frontend` um método de verificação mobile fiável — device emulation via DevTools protocol (`Emulation.setDeviceMetricsOverride`) ou navegar com `?` a um viewport forçado, OU usar Playwright MCP (`browser_resize` respeita viewport CSS) para screenshots responsivos. Preferir Playwright para verificação de breakpoints.

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** A extensão Claude-in-Chrome bloqueia `navigate` para `file://` URLs ("Can't interact with browser-internal or unparseable URLs"), o que não é óbvio ao pré-visualizar HTML local (caso comum em propostas de design). Workaround: servir a pasta via `python3 -m http.server` e navegar para `http://127.0.0.1`. | **Componente afectado:** skills `frontend`/`design-html`/`browser-automate` | **Fix sugerido:** adicionar 1 linha à skill `browser-automate` (e/ou `design-html`): "para pré-visualizar HTML local no browser-tool, servir com `python3 -m http.server` — `file://` é bloqueado".
