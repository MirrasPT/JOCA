---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-07-08
project: JOCA
---

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** `JOCA_OS/CLAUDE.md` diz portas **7381/7382** e recomenda **`npm run setup`** — ambos stale. Portas reais 7491/7492; `npm run setup` força `npx node-gyp rebuild` que compila node-pty e parte no Python ≥3.12 (distutils removido) — em macOS o `npm install` por pasta usa o prebuild darwin-arm64 e chega. | **Componente afectado:** `JOCA_OS/CLAUDE.md` (secção Arranque) | **Fix sugerido:** portas 7491/7492 + trocar bloco "Primeira vez / npm run setup" por "npm install em backend/ e frontend/".

**Categoria:** doc-gap | **Severidade:** low | **Descrição:** `JOCA/CLAUDE.md` (raiz do repo) ainda mostra `JOCA_UI/` no diagrama de estrutura e no Quick Start (`bash JOCA_UI/start.sh`), mas a pasta é `JOCA_OS/`. Herdado do snapshot de migração. | **Componente afectado:** `JOCA/CLAUDE.md` | **Fix sugerido:** `JOCA_UI` → `JOCA_OS` no diagrama e no Quick Start.

**Categoria:** command-improvement | **Severidade:** medium | **Descrição:** `/install` (`commands/install.md`) ainda referencia `JOCA_UI` (FASE EXECUCAO 8/9, relatório final) e portas **7371/7372**. Numa máquina com a estrutura nova (`JOCA_OS`, portas 7491/7492) o passo de build/arranque do UI aponta para pastas/portas erradas. | **Componente afectado:** `.claude/commands/install.md` | **Fix sugerido:** actualizar refs `JOCA_UI`→`JOCA_OS` e 7371/7372→7491/7492; nota sobre node-pty prebuild vs `npm run setup` em macOS.

**Categoria:** workflow-gap | **Severidade:** low | **Descrição:** clone fresco / migração em macOS: os launchers (`start.sh`, `stop.sh`, `JOCA OS.command`) vêm do Windows sem bit executável → precisam `chmod +x`. O `spawn-helper` do node-pty também (mas o servidor já auto-corrige). | **Componente afectado:** setup macOS / `/install` FASE EXECUCAO 8 | **Fix sugerido:** o `/install` (ou o `start.sh`) fazer `chmod +x` dos launchers em macOS/Linux.
