---
type: feedback-joca
source: auto-extracted-by-save
session_date: 2026-06-28
project: tcg (OmniClash)
---

**Categoria:** doc-gap | **Severidade:** medium | **Descrição:** O deploy de `cards.html`/PNGs para `cartastcg.rfdev.pt` (e qualquer site `rfdev.pt`) está atrás de **Cloudflare**, mas a nota de re-deploy na memória do projecto (`memory/projects/tcg.md`) e a skill `deploy-vps` descrevem só `scp` para o VPS — **omitem a purga da cache Cloudflare**. Resultado: após o scp, o live continuou a servir as imagens antigas (`cf-cache-status: HIT`) e parecia que o deploy tinha falhado. Só `?v=<ts>` (cache-bust) revelava o ficheiro novo na origem. | **Componente afectado:** `.claude/skills/deploy-vps.md` (e nota de deploy em `memory/projects/datalix-vps.md`) | **Fix sugerido:** adicionar passo pós-`scp` na skill `deploy-vps`: se o domínio estiver atrás de Cloudflare, purgar a cache por URL — `POST https://api.cloudflare.com/client/v4/zones/<zone>/purge_cache` `{files:[urls]}` com Bearer de `~/.cloudflare/datalix.json` (`zone_rfdev_pt` + `api_token`); verificar com `curl -I` → `content-length` novo + `cf-cache-status: MISS`. Existe o plugin `cloudflare@cloudflare` que também resolve.

**Categoria:** tool-reliability | **Severidade:** low | **Descrição:** Confirma-se (já conhecido) que `enqueue_workflow` do MCP comfy é não-fiável; toda a geração ANIMA desta sessão correu por `POST /prompt` directo via PowerShell (`style_sweep.ps1`). Sem regressão nova — só reforço. | **Componente afectado:** `comfy-mcp-workarounds` skill | **Fix sugerido:** nenhum (workaround já documentado e a funcionar).
