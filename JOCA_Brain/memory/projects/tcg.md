---
name: tcg
description: OmniClash (OC) 1v1 (duelo de cartas) crossover de temas (histórico, mitologia, cultura pop) — Unity (C#), alvo Android
type: project
directorio: C:\Users\renat\Projetos\tcg
---

**Stack:** Unity (C#) · alvo **Android** · motor de regras C# puro (POCO) separado da apresentação
**Objectivo:** OmniClash (OC) é um TCG de duelo 1v1 onde cada jogador comanda um exército temático (civilizações históricas, super-heróis, futebolistas, etc.) liderado por um General. Construir deck ~50 cartas, gerir gold, combate ATK vs DEF, derrubar as 5 muralhas do general adversário.
**Directório:** `C:\Users\renat\Projetos\tcg`
**GitHub:** `MirrasPT/tcg` (PRIVADO) — `master` ≡ local; remote `origin` configurado. Inclui código + arte (`assets/cards/`) + `cards.html`.
**Catálogo online:** `cards.html` (Codex de cartas) **LIVE** em `https://cartastcg.rfdev.pt` (VPS Datalix, vhost estático Caddy: `/var/www/cartastcg/` = `index.html` + `assets/cards/*.png`). Re-deploy: scp `cards.html`→`index.html` + PNGs. Ver `memory/projects/datalix-vps.md`.
**Iniciado:** 2026-06-27
**PRD:** PRD.md existe (GDD com regras core em §5) — actualizar via skill `prd` no /save
**Why:** Projecto pessoal de jogo. Renato (designer/PM) quer um TCG temático próprio.
**How to apply:** Ler skill `unity-gamedev` ANTES de código de gameplay. **Separação dura motor↔UI** (regras = C# puro testável em EditMode, UI consome eventos). Arte de cartas → `img-gen` + `graphic-design`. Conteúdo histórico/mítico afirmado como facto → validar contra fonte real (não inventar lendas/nomes). NÃO confundir com `mediaval-chess` (jogo SEPARADO: grelha+React).

## Conceito (regras-chave — fonte: PRD.md §5)
- **1v1**, deck **~50 cartas**. **General** em campo desde o início, habilidade fixa única (ex.: Romano = +2 limite de gold e buffs de defesa).
- **Estrutura de Cartas:** Custo, ATK/DEF (se Unidade), Raridade (Recruta, Veterano, Campeão, General, Divindade), Habilidade, Facção e **Época** (usada para rotação de formato e evitar powercreep; todas atualmente na Época 1).
- **Gold** = recurso por turno para jogar cartas.
- **3 tipos de carta:**
  - **Unidade** — ATK + DEF. Combate: **ATK atacante ≥ DEF alvo → destrói**; senão nada acontece.
  - **Equipamento** — equipa numa unidade (+ATK / +DEF / habilidade). É destruído com a unidade.
  - **Evento** — uso único (dano, "pesar"/tap, ressuscitar…).
- **Vitória:** atacar o general. Qualquer unidade ataca uma unidade inimiga OU o general directamente. General tem **5 muralhas** (variável); 1 ataque = 1 muralha; **6º ataque mata**.
- **Habilidades:** ressuscitar (cemitério→campo), combo, buff condicional (se carta X em campo → +ATK/+DEF), block (bloquear ataque), quick attack (atacar no turno em que é jogada), reconstruir muralha.
- **Activações:** **on play** (1x), **on cost** (paga gold/sacrifício/destruir muralha → repetível), **on death**.

## Estado actual
**Gregos, Romanos, Vikings E Astecas sincronizados motor↔`cards.html` E auditados por correcção — 96/96 testes verdes** (`dotnet test`). Facção Astecas (20 cartas + General Montezuma) 100% nova no motor: primeira facção a exigir arquitectura nova (sacrifice-as-cost, discard-as-cost, killer-reference/retaliate, reactive OnField listeners) — auditoria confirmou 0 discrepâncias. 4 bugs pré-existentes noutras facções encontrados pela mesma auditoria e corrigidos (ver sessão p). **COMMITADO** (`750c1f3`, `9a8e02f`) — trabalho acumulado de sessões m/n/o/p todo integrado, working tree limpo. DLL Release + `TCG.exe` standalone reconstruído e desbloqueado (verificado por log+artefacto, `unity-build-runner`).
**Sessão p (2026-07-06):** **Facção Astecas implementada de raiz no motor C#, autoplan → build sozinho, sem paragens.** Auditoria prévia (`card-catalog-sync`) confirmou Astecas+Egípcios 100% ausentes do motor (não drift — build from zero). Decisão do user: Astecas nesta sessão, Egípcios na próxima; sem commit automático; primitivo #6 (listener sacrifice-scoped) implementado em versão estreita (só Quetzalcoatl, sem cause-tagging genérico). Fundação nova: `Faction.Aztecs`; `AbilityDefinition.CostSacrificeAlly`/`CostDiscardHand`/`AlwaysDeferGold`/`ReactsToAllyDeath`/`ReactsToAllySacrifice`; `GameState.DeathRecord`(killer)+`RecentSacrifices`+`Sacrifice()`; `CombatResolver` passa o atacante como killer ao `Destroy()`; `CardPlay.Validate/Play` ganham `costSacrifice`/`costDiscard` (distintos do `sacrifice` de field-full-discard, que NÃO dispara OnDeath); `GameEngine.ResolveDeaths` passa o killer como target do OnDeath (retaliate) e dispara reactive OnField listeners (ally-death/ally-sacrifice) via novo `AbilityContext.SourceEquipment`. 5 efeitos novos (`destroy_wall`, `buff_all_allies`, `huitzilopochtli_play`, `return_equipment_self`, `no_op`) — reaproveitados `buff_ally_auto`/`resurrect_filtered`/`buff_self`/`damage`/`damage_auto_enemy` sem alteração para as restantes 15 cartas. Unity `GameDemo.cs`: selector J1/J2 cicla agora Rom→Vik→Gre→Azt (refactor de ternários encadeados para switch-expressions `GeneralOf`/`DeckOf`/`FactionName`/`NextFaction`). **COMMITADO** (`750c1f3`) a pedido do user, junto com todo o trabalho acumulado de sessões m/n/o (arte + `CLAUDE.md` + PassiveSystem.cs que nunca tinha ido a git).
**Sessão o (2026-07-05):** **Auditoria, correção de mira/alvos e sincronização das habilidades gregas.** Corrigido o direcionamento de mira do Raio de Zeus (selecionável entre unidades ou General inimigo para quebra de muralhas) e Flecha de Apolo. Implementada a auto-ativação inteligente para Lira de Apolo (tapa unidade inimiga com maior ATK ao equipar). Realizada auditoria completa de cards e habilidades gregos, romanos e vikings contra o catálogo cards.html. Todos os 77 testes a passar, DLL Release atualizada e standalone Windows TCG.exe reconstruído e desbloqueado com sucesso.
**Sessão n (2026-07-05):** **Sincronização e implementação completa da facção dos Gregos no motor C# e interface Unity.** Definidas as 19 cartas gregas (incluindo General Péricles) em `src/Tcg.Core/Data/Cards.cs`, `Generals.cs` e `Decks.cs`. Registados os 11 efeitos gregos em `Effects.cs` e `EffectRegistry.cs`. Adicionada lógica de interceptação de cartas de Evento em `GameEngine.PlayCard` para acionar sinergias (Péricles gold, Pitónia draw, Hércules muralha, Escudo Hoplon charges). Implementada passiva de formação em `PassiveSystem.cs`. Adicionado botão seletor dinâmico J1/J2 em `GameDemo.cs` para rotacionar facções in-game (permitindo jogar Gregos vs Roma/Vikings/Gregos).
**Sessão m (2026-07-05):** **os 14 buffs Viking (sessão j) — que viviam SÓ no `cards.html` — foram sincronizados no motor C#** (`src/Tcg.Core/Data/Cards.cs`): stats base (Hirdman 3/2, Drengr 4/3, Skald 2/2, Shieldmaiden 3/3, Huscarl 4/4), custos (Úlfhéðnar 3, Völva 2, Odin 7) e amounts de habilidade (Boga/Bóndi/Hirdman/Skald/Berserker/Forjador/Huscarl/Jarl/Thor) — verbatim do HTML, textos incluídos. Romanos/equip/eventos já batiam certo (verificado). **73/73 verdes.** `Tcg.Core.dll` (Release) rebuildada + copiada p/ Plugins; **`.exe` Windows RECONSTRUÍDO** via `unity-build-runner` (assemblies managed frescas `TCG_Data\Managed\` @ 05/07 18:31; build verde 0 erros; `TCG.exe` stub mantém data antiga). `TCG.exe` **desbloqueado** (`Unblock-File`, MOTW removido); aviso SmartScreen residual = exe não-assinado (normal, "Executar mesmo assim" 1×/build). **NÃO committado.**
**Sessão k (2026-06-28):** 7 regras novas + 2 bugs corrigidos no motor + demo + `cards.html` (live) + `.exe` reconstruído — ver "Última sessão (k)" e os blocos ✅ FEITO no Pendente. Regras novas: combate **ATK≥DEF**, **campo 5** + descarte-por-espaço, **turnos conjuntos** (1=J1,2=J2,3=J1), **gold = nº do turno conjunto** (J2 começa com 2), **atacar tapa** + **só atacar tapados**, **tapados podem bloquear** + **bloqueio 1×/turno**, **OnDeath dispara em todas as mortes** (combate+efeito), **gold de "Ao Morrer" diferido** p/ o turno seguinte. Bugs: Ataque Rápido concedido anula summoning sickness; gold off-turn não se perde.
**[Histórico] Motor base — 63 testes verdes** (`dotnet test`). Construído como **biblioteca .NET standalone headless** (`src/Tcg.Core`, netstandard2.1 → consumível por Unity 6) + testes NUnit (`tests/Tcg.Core.Tests`, net8.0). .NET SDK 8 instalado (winget). Inclui partida completa determinística até `GeneralKilled`.
- **Domain:** GameState, Player (zonas+gold+TurnsTaken), General (muralhas), Card→Unit/Equipment/Event defs, Unit runtime (ATK/DEF efectivos, tap, sickness, keywords), shim `IsExternalInit` (records C# 9 em netstandard2.1).
- **Rules:** GoldSystem (gold = nº do turno conjunto `game.TurnNumber`, cap 10+cap-mod, +perTurn, +`PendingGold` diferido por cima, sem carryover normal), TurnStateMachine (Draw→Main→Attack→End, untap/draw/refill, reset `HasBlockedThisTurn`), CombatResolver (**ATK≥DEF**; atacar tapa o atacante; só se atacam unidades tapadas; muralhas; bloqueio = keyword Block, 1×/turno, **mesmo tapado**; mortes via `GameState.Destroy`), CardPlay (campo **5**/lado + descarte-por-espaço sem OnDeath, equip), GameEngine (play+triggers+combate, `ResolveDeaths` drena OnDeath de todas as mortes), GameSetup, DeckValidator.
- **Abilities:** EffectRegistry (id→IEffect, throw em id desconhecido) + 12 efeitos (damage/destroy/weaken/tap/buff_self/buff_target/conditional_buff/resurrect/rebuild_wall/gain_gold/gold_cap/gold_per_turn). Triggers OnPlay/Activated/OnDeath.
- **Data:** catálogo Romanos+Vikings (17 normais cada + legendários Júpiter/Marte/Thor/Odin + neutral Padeira de Aljubarrota), decks demo de 50.
- **Fase 4 FEITA:** event bus no Core (GameEvent + GameState.Log/Emit/Emitted, emitido pelo GameEngine/TurnStateMachine; 60 testes) + **projecto Unity 6** (`unity/`, editor 6000.5.1f1) com `Tcg.Core.dll` (netstandard2.1) em `Assets/Plugins`. **Demo jogável `GameDemo`** (MonoBehaviour OnGUI + bootstrap `RuntimeInitializeOnLoadMethod` → zero setup de cena): hot-seat 1v1, mão/campo/ataques/log. `BuildScript` (Editor) faz builds Windows standalone + Android AAB (IL2CPP/ARM64).
- **VERIFICADO:** build standalone Windows via batchmode → **`unity/Build/Windows/TCG.exe` (93 MB) gerado** (scripts compilam contra o motor). Demo testável por double-click OU abrir `unity/` no Unity Hub → Play.
- **Demo UI em tabuleiro** (sem sobreposições): cartas desenhadas (custo/nome/ATK-DEF/cor por tipo, borda dourada lendários), generais com muralhas em pips, log lateral, realce de selecção/alvo. Layout 100% calculated. Janela 1280×720 windowed. Substitui a lista de botões inicial.
- **Habilidades Activated (on-cost) JOGÁVEIS:** 4 cartas activáveis (Rom Engenheiro=dano-pago, Tribuno=reforçar-aliado; Vik Völva=tapar-pago, Forjador=reforçar-se), nos decks; barra `Activar: <efeito> (Ng)` no demo com modo de alvo pendente; `GameEngine.ActivateAbility` emite `AbilityActivated`. Só custo em **gold** (sacrifício/muralha por fazer). Flag `--showcase` (QA).
- **No GitHub:** `MirrasPT/tcg` (privado), 6 commits em `master` + README. `Build/`/`Library/` gitignored.
- **Arte das cartas — transição para estilo 5 (pintura semi-realista) em curso (2026-06-29):** `assets/cards/<id>.png` (nome = `Id` do catálogo, 1:1), 3:4. **65 em estilo 5** (21 Egípcios + 4 generais via **ANIMA**; 20 Romanos + 20 Vikings via **Krea 2 Turbo**, deployados) · **40 ainda manga** (Astecas + Gregos — por regenerar em Krea2). Recipe estilo-5 + drivers em `card-art-pipeline` (skill) e scratchpad (`gen_krea2_all.py`/`gen_estilo5.py`/`deploy_cards.py`). **Histórico (manga, 2026-06-28):** **38 em Krea 2 Turbo** (Rom+Vik+4 deuses iniciais) + **63 em ANIMA** (pipeline novo, ~5s/img vs 65s; Astecas+Gregos+Egípcios+restantes Vik/Rom). Estilo Anima afinado p/ casar com o manga punchy do Krea2 (preâmbulo bold lineart/saturado). Gerador reutilizável: scratchpad `anima_gen.ps1` + `batch_prompts*.json` (POST /prompt directo, não o MCP comfy). **Catálogo `cards.html` (100 cartas, 5 facções) LIVE em `cartastcg.rfdev.pt`.** Unidades/deuses=personagem · Equipamentos=objecto em spotlight (sem pessoas) · Eventos=cena de acção. `assets/` agora **commitado + no GitHub**. **Falta:** `leg_padeira` (neutra, não pedida); **integrar a arte na carta da UI** (Unity compõe moldura/nome/stats por cima da ilustração — ainda não feito; o demo desenha cartas planas).
- **Falta:** Fase 5 real (correr o build Android AAB — BuildScript pronto; precisa módulo Android + keystore). Balanceamento. UI gráfica a sério (UGUI/animada) sobre o event bus.
- ⚠ Unity batchmode createProject/build sai com exit-code enganador (≠0) mesmo quando SUCEDE — confirmar pelo log (`Build Finished, Result: Success`) e pelo artefacto no disco, não pelo exit code.

**Desvios ao plano (reversíveis):** dados das cartas em **catálogo C#** (não JSON) — evita dependência System.Text.Json no Core+Unity (YAGNI). Generais com 5 muralhas (Augusto = +2 cap gold + aura Bloquear +1 DEF; Ragnar = Activar (2) grant temp). Bloqueio de ataque ao general redirige combate p/ o blocker. Limitação MVP restante: conditional_buff é snapshot OnPlay (não aura contínua). **(Resolvido em k:** efeito-kills agora disparam OnDeath via `GameState.Destroy`+`ResolveDeaths`.)

## Decisões tomadas
- 2026-07-06 (p): **Astecas antes de Egípcios; sacrifice-cost ≠ field-full-discard; retaliate/reactive-listeners via `AbilityDefinition` flags, não novos `AbilityTrigger`.** Ordem escolhida por risco crescente (Astecas = 7 primitivos "cost extension" sobre efeitos já existentes; Egípcios = 2 primitivos arquitecturais novos que tocam o chokepoint `Destroy()`). Sacrifice-as-cost usa `GameState.Sacrifice()` (chama `Destroy()`, dispara OnDeath) — deliberadamente distinto do `sacrifice` de `CardPlay.cs` (field-full discard-for-space, que NUNCA dispara OnDeath); confundir os dois quebraria a sinergia de Arqueiro de Veneno/Quetzalcoatl com sacrifícios. Retaliate-to-killer e os 2 reactive-listeners (ally-death, ally-sacrifice-scoped) foram implementados como flags booleanas em `AbilityDefinition` + lógica em `GameEngine.ResolveDeaths`, evitando crescer o enum `AbilityTrigger` — reaproveita `damage`/`damage_auto_enemy` já existentes, zero classes de efeito novas para esses 2 primitivos. Primitivo #6 (sacrifice-scoped) implementado em versão ESTREITA (só Quetzalcoatl via `ReactsToAllySacrifice`), sem cause-tagging genérico de mortes — YAGNI, só 1 carta precisa.
- 2026-07-05 (n): **Sincronização completa da facção dos Gregos.** Todas as 19 cartas, General Péricles e o baralho grego de 50 cartas foram implementados no motor C# e mapeados na UI Unity com botões seletores interativos. 77/77 testes aprovados e build bem sucedida.
- 2026-07-05 (m): **14 buffs Viking sincronizados HTML→motor (fecha o gap da sessão j).** O balanço v1 (Romanos ganhavam sempre → 14 buffs Viking) estava só em `cards.html`; agora vive no motor C# (`Cards.cs`) verbatim do HTML — stats+custos+amounts+textos. Romanos NÃO tocados (já batiam certo). 73/73 verdes; DLL Release + `.exe` Windows rebuildados e verificados (`unity-build-runner`, log+artefacto). `Unblock-File` no exe; SmartScreen residual é o aviso de exe não-assinado (não é MOTW — só "Executar mesmo assim"/code-signing o silencia). **NÃO committado** (mistura-se com trabalho de motor+arte não committado de sessões anteriores — decisão de commit adiada ao user).
- 2026-06-29 (l): **Arte de combate unificada em estilo 5 com DOIS motores — Romanos+Vikings em Krea 2, deployados.** Pedido: regenerar a arte não-egípcia em estilo 5. Comecei ANIMA (default das sessões anteriores) mas o user pediu **Krea 2** explicitamente → recipe estilo-5 verbatim conduzido pelo **Krea 2 Turbo** (`krea2_turbo_fp8_scaled` + CLIPLoader tipo `krea2`/qwen3vl_4b + qwen VAE, KSampler **euler/simple/8 steps/cfg 1**, negativo via `ConditioningZeroOut`, 896×1152). **Enhancer LLM do template `image_krea2_turbo_t2i` CONTORNADO** — descritor estilo-5 alimentado directo ao CLIPTextEncode (o template tem TextGenerate+System/User-Prompt que reescreveria e quebraria a consistência). Krea2 dá pincelada a óleo mais rica que ANIMA. **20 Romanos + 20 Vikings geradas + deployadas** (scp + purga Cloudflare, verificado 200/MISS; excl. generais já estilo-5). **Astecas+Gregos NÃO feitos** — o user mandou parar a meio; RESTAUREI a arte manga original de Astecas/Gregos via `git checkout` (12 já regeneradas in Krea2 foram revertidas) + re-scp+purga p/ o live ficar determinístico. **Egípcios (21) + 4 generais continuam ANIMA estilo-5.** Estado de arte: **65 estilo-5** (21 egy ANIMA + 4 gen ANIMA + 20 rom Krea2 + 20 vik Krea2) · **40 manga** (Astecas+Gregos). Scripts reutilizáveis no scratchpad: `gen_krea2_all.py` (grafo Krea2 + 80 subjects fiéis), `gen_estilo5.py` (variante ANIMA), `deploy_cards.py` (scp ids [+`--html`] + purga). ⚠ **NADA committado** (40 PNGs no working tree).
- 2026-06-28 (j): **Balanço Rom vs Vik — buff aos Vikings (só `cards.html`, deployado live).** Playtest do user: Romanos **ganhavam sempre**. Análise (skill `game-balance`): causa-raiz = (1) ATK Viking baixo não fura DEF romana alta (combate ATK≥DEF; Romanos têm DEF 4-5 espalhada); (2) sem 1-drop / curva lenta numa facção aggro; (3) payoffs de cemitério/AR fracos cedo; (4) Romanos desfazem o plano Viking (reconstroem muralhas + ramp p/ lendários). **14 buffs Viking** (só custo/ATK/DEF/valores-de-bónus — **tipos, habilidades e keywords intactos; Romanos NÃO tocados**): Boga dano 1→2; Bóndi +1→+2 ATK; Hirdman 2/2→3/2 +1→+2 ATK; Drengr 3/3→4/3; Skald 1/2→2/2 máx+3→+4; Berserker morte 2→3 dano; Forjador +2→+3 ATK; Shieldmaiden 2/3→3/3; Huscarl 3/4→4/4 ress ≤2→≤3; Úlfhéðnar custo 4→3; Völva custo 3→2; Jarl +1/+1→+2/+2; Odin custo 8→7; Thor 4→5 dano. Saqueador (3/1 AR) e Portador de Escudo (1/3) deixados intactos. **Deployado** (scp→VPS + purga Cloudflare). É 1º passo a olho — afinar com `tcg-playtester` (self-play seeded) **depois** do motor estar sincronizado. ⚠ **Só no HTML — o motor C# continua com os stats antigos** (faz parte do gap da próxima sessão).

## Design de cartas — Facções

### Identidade das facções (5 facções × 20 cartas = 100, TODAS com arte)
- **Romanos:** lentos, boa defesa, ramp de economia (gold/turno), late game, formation buff (ATK/DEF por contagem de unidades).
- **Vikings:** aggro rápido + frágil (ATK alto/DEF baixo, Ataque Rápido), recursão de **tempo** (ressuscita p/ manter pressão) + escala por cemitério.
- **Astecas:** **sacrifício-ofensivo** — destruir aliadas/descartar mão → converter em buff/dano/quebrar-muralha. Combustível = corpos baratos (Vítima/Iniciado).
- **Gregos:** **motor de eventos** — quanto mais Eventos jogas/no cemitério, mais o board pumpa (comprar, DEF, muralha). Corpos fracos sem eventos.
- **Egípcios:** **imortalidade + ressurreição + cemitério-importa** (≠ sacrifício Asteca). Pilares: keyword **Imortal (descarta 1 carta)** = cheat-death; trigger **Julgamento** (quando carta entra no cemitério); ressuscitar como plano A; auto-mill + cartas gated pela contagem do cemitério. Deuses: **Osíris** (aura Imortal ao exército) · **Rá** (ressuscita tudo ≤3 do cemitério) · **Anúbis** (1×/turno traz de volta unidade que foi ao cemitério, descartando 1 carta).

### Eixo de diferenciação (3 facções "de morte" NÃO se confundem)
Vikings = cemitério→**tempo** · Astecas = sacrifício→**buff/ofensiva** · Egípcios = cemitério→**inevitabilidade/imortalidade** (não os matas, voltam sempre).

### 4 triggers de habilidade (estabelecidos 2026-06-28)
| Trigger | Quando activa |
|---|---|
| **Ao Jogar** | 1x quando a carta é colocada em campo |
| **Em Campo** | Passivo, enquanto em campo + condição cumprida |
| **Activar (N)** | Paga N gold (or other cost) → activa, repetível |
| **Ao Morrer** | 1x quando a unidade morre |

## Pendente

### ⚠ PRÓXIMA SESSÃO — Egípcios (última facção a alinhar)
Astecas FEITOS (sessão p). Falta só **Egípcios** (20 cartas + General Cleópatra) — roster completo já mapeado pelo `card-catalog-sync` da sessão p (ver decisão 2026-07-06 abaixo para o texto verbatim de cada carta, ou re-correr o agente).
Primitivos novos exigidos (arquitecturalmente maiores que os de Astecas):
1. **Keyword Imortal** — intercepta `GameState.Destroy()` ANTES de enviar ao cemitério: paga descarte 1 carta → cancela a destruição (NÃO dispara OnDeath/Julgamento). Mão vazia → morre normalmente (fallback).
2. **Trigger Julgamento** — broadcast de "carta entrou no cemitério" (não só unidades) em 4 pontos: `Destroy()` (pós-Imortal), discard-cost, mill, resolução de Evento. Gating 1×/turno para `egy_sentinela` (padrão `HasBlockedThisTurn`). Ordem: Imortal-check → Destroy → Julgamento → OnDeath.
3. **Mill N** (topo do deck → cemitério, incondicional) + **playability-gate por contagem de cemitério** (`egy_farao`) + extensões de resurrect (selecção por maior-custo, modificador de stats na ressurreição, auto-referência à unidade que acabou de morrer — reaproveita o `killer`-threading já construído para Astecas).
4. `GraveyardBuffEffect`: adicionar divisor (per-2/per-3) — default per-1 preserva Skald vikingue.
Boundaries: nunca tocar Romanos/Vikings/Gregos/Astecas; `cards.html` só leitura.

### Última sessão
2026-07-06 (p) — **Facção Astecas 100% implementada + auditoria completa do catálogo + 4 bugs de correcção fixados. COMMITADO (2 commits: `750c1f3` feat Astecas/Gregos/Vikings, `9a8e02f` fix bugs auditoria).**
- Astecas (20 cartas + Montezuma) implementadas de raiz. Fundação nova para sacrifice/discard-cost, killer-reference, reactive listeners.
- Auditoria (`card-catalog-sync`, escopo Rom+Vik+Gre+Azt): **Astecas 0 discrepâncias** (trabalho novo correcto); 4 bugs pré-existentes encontrados e corrigidos — `vik_forjador` (cost-cap morto, exploit), `vik_skald`/`vik_ulfhednar`/`vik_drengr` (contagem de cemitério incluía Eventos/Equipamento), `gre_lira` (auto-target maior ATK em vez de escolha do jogador — `LiraEffect` bespoke removido), `rom_hastati` (formação faction-scoped em vez de qualquer aliada, bug dormente).
- 96/96 testes verdes. DLL Release + `TCG.exe` reconstruídos e verificados 2× (uma vez por commit) via `unity-build-runner`.
- Egípcios continuam por fazer (ver plano acima) — não tocados nesta sessão.
