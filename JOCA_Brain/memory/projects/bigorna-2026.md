---
name: bigorna-2026
description: Primeira loja online real do Bigorna — e-commerce de raiz em Laravel + Filament + React/Tailwind
type: project
directorio: D:\Mega\Bigorna\2026_Website_Bigorna
---

**Stack:** React + Tailwind (frontend) · Laravel + Filament (backend/admin) · MySQL/MariaDB.
**Objectivo:** Construir a primeira loja online a sério do Bigorna — catálogo, checkout com pagamento (Stripe) e painel de gestão Filament.
**Directório:** `D:\Mega\Bigorna\2026_Website_Bigorna`
**Iniciado:** 2026-05-30
**PRD:** PRD.md existe — actualizar via skill `prd` no /save quando o scope mudar.
**North Star:** Encomendas completas por mês.
**Why:** O Bigorna nunca teve loja online. A loja WordPress + WooCommerce em `D:\Mega\Bigorna` (theme clássico, Docker) foi só teste de desenvolvimento — não é produção. Esta é a primeira loja real, feita de raiz.
**How to apply:** Projecto dev e-commerce. Skill-first: `frontend` (director → `react-patterns`, `react-composition`, `tailwind`, `shadcn`), `laravel-specialist`, `filament`, `payment-integration` (agent, Stripe), `file-storage`, `search`, `caching`, `queues`, `rest-api`, `auth`, `mysql`. Design (Renato é designer): `design-system`, `design-tokens`, `design-review`, `brand-guidelines`. Backend/DevOps → explicar decisão arquitectural 1 linha antes de implementar.

## Contexto
- Marca: Bigorna (bigorna = anvil). Loja WP/Woo anterior em `D:\Mega\Bigorna` = só dev/testes (tema `theme/`, `docker-compose.yml`, Local by Flywheel, deploy cPanel).
- Assets/brand em `D:\Mega\Bigorna\base` e `brand-preview.html` (parent folder).
- Tema Envato comprado (`Tema_Envato/electromart-...zip`) — referência de design e-commerce, não usado directamente na stack Laravel.

## Estado actual
**Admin completo (10 módulos) + dashboard KPI + sistema de e-mail (fases 0-4 de 8) — 99 testes.** Laravel 13 + Filament v5 + Sail (PHP 8.5 / MySQL 8.4 / Redis) em Docker, com `queue.worker` + `scheduler`. App :8080, painel /admin. Admin local: `renato@bigorna.test` / `bigorna2026` (super_admin). Loja gerível ponta-a-ponta no admin. **Novo nesta sessão:** dashboard com KPIs (receita hoje/semana/mês/ano + deltas, gráfico, últimas encomendas); menu reorganizado (Vendas/Catálogo/Clientes/Conteúdo/Marketing/Definições); **gestão de utilizadores admin** (`/admin/users`); **e-mail** — infra (BigornaMailable+EmailLog+opt-out+unsubscribe), transaccionais (confirmação/pagamento/reembolso/boas-vindas), e **motor de funil/sequências** (construtor de regras no admin, grupo Marketing). **Falta no backend:** carrinhos abandonados (Fase 3), stock (5), state-machine+envio (6), notificações admin (7) — ver secção "Backend gaps". **Frontend da loja ainda por montar.**

## Ambiente (importante)
- Arrancar: `$env:WWWUSER=1000; $env:WWWGROUP=1000; docker compose up -d` (na pasta do projecto).
- Artisan/composer: `docker compose exec laravel.test php artisan <cmd>`.
- Após rebuild da imagem: `storage/` volta a root → 500 `tempnam()`. Fix: `docker compose exec -u root laravel.test chown -R sail:sail storage bootstrap/cache`.
- DB Docker: base `bigorna`, user `sail`/`password`, porta 3306. Porta app = 8080 (evita conflito porta 80).
- **Performance (fix 2026-05-31):** páginas estavam a ~2,5s. Causa: web servido por `php artisan serve` (SAPI **CLI**) com `opcache.enable_cli=0` → recompilava tudo por request, agravado por bind-mount Windows + `D:\Mega` (MEGAsync activo). Fix persistente: `docker/php/99-joca-perf.ini` (opcache CLI on, `validate_timestamps=0`, xdebug off) montado via volume no `compose.yaml`; sessão+cache → Redis. Resultado ~0,5s. Projecto fica no Mega (decisão do Renato).
- **GOTCHA opcache `validate_timestamps=0`:** depois de editar **código PHP**, o `artisan`/testes correm código **STALE** (opcache CLI ligado). Para ver mudanças: ou `docker compose restart laravel.test` (web) ou correr com bypass: `docker compose exec -T laravel.test php -d opcache.enable_cli=0 artisan <test|migrate>`. Sem isto, edições "não fazem nada".
- **Worker + scheduler (desde 2026-05-31):** serviços compose `queue.worker` (`queue:work database --queue=mail,default`) e `scheduler` (`schedule:work`). Arrancar: `docker compose up -d queue.worker scheduler`. **Mesma gotcha de opcache:** após editar Job/Listener/Mailable/Action/Command, `docker compose restart queue.worker scheduler` (senão processam código stale). Emails só SAEM quando o token SMTP ZeptoMail estiver preenchido (até lá `mail.default`=log/array, mas `EmailLog` regista na mesma).
- **GOTCHA mailable base:** `BigornaMailable` faz só `onQueue('mail')` (NÃO `onConnection('database')`) → em testes (`QUEUE_CONNECTION=sync`) corre inline e os listeners de mail disparam. Testes de `EmailLog` usam o array transport (`MAIL_MAILER=array` no phpunit), **nunca `Mail::fake()`** (saltaria MessageSending/MessageSent).

## Decisões tomadas
- 2026-05-30: Stack = Laravel + Filament + React/Tailwind (descartado continuar WordPress/WooCommerce; loja WP era teste).
- 2026-05-30: DB = MySQL/MariaDB.
- 2026-05-30: Pagamentos = **ifthenpay** (Multibanco/MB WAY/Cartão) — NÃO Stripe (corrigido). Faturação = Moloni (real) / FIZ (stub).
- 2026-05-30: MVP = Catálogo + Checkout + Admin Filament. North Star = encomendas/mês.
- 2026-05-30: Deploy ainda não decidido.
- 2026-05-30: Produtos COM variantes (cor/tamanho, stock/preço por variante). Moeda = só EUR.
- 2026-05-30: RBAC = Filament Shield. Contas de cliente = guard `customer` separado. 2FA fora do scope.
- 2026-05-30: Construído todo o admin (10 módulos) via workflow de design (12 agentes) → implementação faseada (foundation→módulos), cada fase com testes.
- 2026-05-31: Backend gaps (ex-frontend) construídos via plano de workflow (8 agentes) em 8 fases, e-mail primeiro. Eventos canónicos `app/Events/` + auto-discovery (sem EventServiceProvider). Worker+scheduler como serviços compose.
- 2026-05-31: **Carrinho abandonado corre sobre o motor de sequências** (Renato quer construtor de regras no admin, não cadência hardcoded).
- 2026-05-31: **Stock decrementa quando a encomenda é PAGA** (MVP; overselling aceite entre carrinho e pagamento MB lento).
- 2026-05-31: Sequências/funil no novo grupo de menu **Marketing**.
- 2026-05-31: Pausa de build pedida no fim da Fase 4 (motor de funil). Fases 3/5/6/7 ficam para próxima sessão.

## Pendente
- **Backend (continuar o build multi-fase):** Fase 3 carrinhos abandonados, Fase 5 stock, Fase 6 state-machine+envio/tracking, Fase 7 notificações admin. Detalhe completo na secção "Backend gaps" + `docs/BACKEND_BUILDPLAN.md`. Reentrada: ler essa secção, continuar pela Fase 3.
- **Frontend da loja:** decidir Inertia+React vs SPA headless a consumir os endpoints JSON existentes, depois montar. (Inclui: renderizar carrinho/checkout que alimenta `Cart`, `order_items.variant_id`, link de recuperação `/cart/recover/{cart}`, campos personalização.)
- **Credenciais de produção (preencher no ADMIN, BD encriptada — não `.env`):** Moloni, ifthenpay, **ZeptoMail SMTP token** (sem isto os emails só fazem `log`/`array`, mas o `EmailLog` regista na mesma). Sender precisa de domínio verificado (SPF/DKIM).
- **Afinar:** nota de crédito Moloni (linhas espelhadas) + estorno ifthenpay (manual via `gateway_refunded`).
- Decidir plataforma de deploy (Ploi / cPanel / Docker) + correr worker+scheduler em produção (supervisor/serviço).
- Opcional: 2FA admin; integração transportadora CTT (API tracking); prune de `email_logs`/notificações antigas; JIT (precisa descarregar xdebug).

## Faturação (multi-fornecedor)
- Abstração em `app/Invoicing/`: contrato `InvoicingProvider` + DTOs + `InvoicingManager` (switch via `INVOICING_PROVIDER=moloni|fiz`).
- **Moloni** ✅ implementado a sério (docs moloni.pt/dev): OAuth password grant (token 1h/refresh 14d em cache), `invoices/insert` (status 0=rascunho/1=fecha+AT), `customers/getByVat`+`insert`. Falta preencher credenciais MOLONI_* no `.env` para emitir.
- **FIZ** ⏳ stub honesto: API pública de emissão **não documentada/acessível** (api.fiz.co é Swagger JS, `/openapi.json` etc. 404; help center diz API é feature paga futura). `FizProvider` lança erro claro até haver spec. Slot pronto — só preencher os 3 métodos.
- Comando `php artisan invoicing:ping {moloni|fiz}`. Página Filament `/admin/invoicing-status` (Configuração→Faturação) com teste de ligação. Teste `InvoicingStatusPageTest` passa.
- Gotcha v5: `Page::$navigationGroup` exige tipo `string|UnitEnum|null` (não `?string`) senão fatal no boot do painel.

## Domínio e-commerce + admin
- Models `Category`, `Product` (soft-deletes), `Order`, `OrderItem` — todos ULIDs. Preços decimal(10,2), `OrderStatus` enum.
- Totais auto: `OrderItem::booted` → `saving` calcula `line_total`, `saved/deleted` chamam `Order::recalculateTotals()` (subtotal/IVA/total).
- Filament grupo *Loja*: ProductResource, CategoryResource, OrderResource (List/View/Edit + `ItemsRelationManager` + `OrderInfolist`). Página *Configuração→Faturação*.
- **Ligação faturação↔domínio:** `IssueInvoiceAction` (Filament) na tabela e view da encomenda → constrói `InvoiceData` das linhas e chama `InvoicingProvider::issueInvoice()`, grava `invoice_*`+`invoiced_at`. Visível só se `OrderStatus::isInvoiceable()` e ainda não faturada.
- `ShopSeeder` (dados demo ferraria: 3 categorias, 8 produtos, 1 encomenda paga). 6 testes passam (`ShopBackendTest`+`InvoicingStatusPageTest`).
- Gotcha Filament v5: enum `getIcon()` deve devolver o **Heroicon enum case** (não `->value`) senão "Svg ... not found". E `Page/Resource::$navigationGroup` exige tipo `string|UnitEnum|null`.

## Roadmap admin (workflow) + Fase 0
- Workflow de design (12 agentes) gerou roadmap dos 10 módulos do admin → `docs/ADMIN_ROADMAP.md`. Princípio: `Order` tocada por 7 módulos → **Fase 0 atómica** + `Order.php` append-only + enums separados. Gateway de pagamento = **ifthenpay** (não Stripe).
- **Fase 0 (foundation) FEITA:** `Customer`+`Address` (ULIDs), enums `AddressType`/`PaymentStatus`, `Order` estendida (`customer_id` nullOnDelete + snapshot, `shipping_*`, `payment_*`, recalc soma portes). Skeleton `app/Payments/` adiado p/ Fase 2.
- **Item #1 (clientes) FEITO:** CustomerResource (List/Create/Edit/View) + RM Moradas (CRUD) + RM Encomendas (RO) + select de cliente no OrderForm. 10 testes passam.
- **Fase 1 produtos FEITA** (decisão: variantes SIM, só EUR): `ProductImage` (galeria, capa sync→product.image), `ProductVariant` (preço override/stock/options JSON via KeyValue, has_variants flag, priceFrom()/totalStock()), dimensões (weight_grams/length/width/height_cm p/ envios), SEO (meta_title/description). RM Galeria + Variantes no ProductResource. 12 testes passam.
- **Fase 2 pagamentos FEITA** (ifthenpay: Multibanco+MBWay+Cartão): `app/Payments/` (contrato `PaymentGateway`+`IfthenpayGateway`+DTOs+`PaymentManager`+`config/payments.php`, espelha o módulo Invoicing). Models `PaymentMethod`/`Payment`, enum `PaymentMethodType`. Webhook `GET /payments/ifthenpay/callback` (anti-phishing key + `ConfirmPaymentAction` idempotente → Order Paid → opcional fatura). Filament: acção "Gerar pagamento" + PaymentMethodResource (config) + PaymentResource (RO) + seeder. Chaves IFTHENPAY_* no .env. Endpoints ifthenpay (api.ifthenpay.com: /multibanco/reference/init, /spg/payment/mbway, /creditcard/init/{key}) configuráveis — confirmar no backoffice. `IssueInvoiceAction::invoiceDataFor()` agora público (reuso no webhook). **21 testes passam.**
- **Fase 3 envios FEITA**: `ShippingMethod`→`ShippingZone`(prefixos CP)→`ShippingRate`(RateRule Flat/Weight/OrderTotal, up_to, cost). `app/Shipping/ShippingCalculator` (zona por prefixo + escalões + free_over). `Order.shipping_method_id`+`shipping_cost`; **total agora derivado via `Order::saving` hook** (subtotal+IVA+portes — testes não devem setar `total` directo, usar `subtotal`). Acção "Calcular portes". `ShippingMethodResource`+`ZonesRelationManager` (Repeater de escalões). **26 testes passam.**
- **Tickets+Contacto FEITO**: `Ticket`+`TicketMessage` (enums TicketStatus/Priority/Category), thread auto (staff→Respondido, cliente→reabre via `TicketMessage::created`). `TicketResource`+`MessagesRelationManager`+acções Fechar/Reabrir. **Formulário contacto `POST /contact`** (FormRequest+honeypot `prohibited`+throttle, CSRF except em `bootstrap/app.php`). Fix: `shouldRenderJsonWhen(... || expectsJson())` senão validação em rota web com postJson dava 500. **30 testes passam.**
- **Contactos sem encomenda (2026-05-31):** dados já suportavam (`order_id`/`customer_id` nullable; `/contact` cria ticket `category=Contact` sem encomenda; form admin tem `order_id` opcional). Faltava só a **vista**: `ListTickets::getTabs()` com 3 abas — **Todos** / **Contactos** (`category=Contact`) / **Encomenda** (`order_id` not null), cada uma com badge de contagem. Classe Tab v5 = `Filament\Schemas\Components\Tabs\Tab` (não `Filament\Resources\Components\Tab`). Teste `tabs split contacts from order tickets`.
- **Reembolsos(#7) FEITO**: `Refund` (nº auto, total/parcial por valor), enums `RefundStatus`/`RefundReason`. `ProcessRefundAction` idempotente (total→Order Refunded+payment Refunded; parcial→PartiallyRefunded). `InvoicingProvider::issueCreditNote` adicionado ao contrato (Moloni `creditNotes/insert`+associated_documents; FIZ stub); gated por `invoicing.credit_note_on_refund` (default off). `RefundResource`(RO)+`RefundsRelationManager` na encomenda+acções partilhadas `RefundActions` (Aprovar/Processar/Rejeitar). Nota crédito MVP=1 linha pelo valor; estorno ifthenpay manual (`gateway_refunded`). **35 testes passam.**
- **Blog(#8) FEITO**: `Post`(ULID, RichEditor, cover, SEO, `scopePublished`, autor=User via Hidden), `BlogCategory`, `Tag`(belongsToMany `post_tag`). Enum `PostStatus`. Grupo Filament *Conteúdo*: PostResource+BlogCategoryResource+TagResource. **38 testes passam.**
- **CMS(#9) FEITO**: `Page`(blocos via Filament **Builder** json, `scopePublished`), `Menu`+`MenuItem`(page_id/url, `resolvedUrl()`). Enum `PageStatus`. `PageResource`(Builder hero/text/image/gallery/cta+SEO)+`MenuResource`+`ItemsRelationManager`. Rota pública `GET /pages/{slug}` (JSON, só publicadas). **42 testes passam.**
- **Login&Segurança(#10) FEITO**: RBAC via **Filament Shield** (spatie/laravel-permission; 15 policies+181 permissões; RoleResource; super_admin→renato). `User` implements FilamentUser+HasRoles. **`Gate::before` no AppServiceProvider dá tudo ao super_admin** (crítico: faz os testes Filament passarem com BD fresca — caso contrário 403 porque permissões não existem em RefreshDatabase). Contas cliente: `Customer` agora Authenticatable, guard `customer`+provider em config/auth.php, `POST /customer/{register,login,logout}` (JSON, throttle, CSRF except, FormRequests Password::defaults). Hardening: Password::defaults, canAccessPanel. 2FA não incluído. Helper `superAdmin()` no TestCase. **47 testes passam.**
- **ADMIN COMPLETO: 10/10 módulos, 47 testes.** Pendente produção: credenciais reais Moloni+ifthenpay; afinar nota crédito/estorno; front-end (Inertia/React ou headless via endpoints JSON existentes: /pages/{slug}, /contact, /customer/*).
- Gotcha Shield: após `shield:install`, é preciso publicar+migrar a migration do spatie (`vendor:publish --provider=Spatie\Permission\PermissionServiceProvider`) senão `shield:generate` falha (tabela permissions não existe).

## Settings / Configurações (config de terceiros no admin) — FEITO 2026-05-31
- **Decisão:** configurar Moloni/FIZ/ifthenpay/email **no admin**, guardado em **BD encriptada** (NÃO escrever no `.env`). Email = **ZeptoMail (Zoho)** via SMTP (não Postmark).
- **Arquitectura:** tabela `settings` (group/key/value/is_encrypted) + serviço `App\Settings\AppSettings` (singleton, cache `app_settings.v1` em Redis, `Schema::hasTable` guard) + **`SettingsServiceProvider`** que no `boot()` sobrepõe `config()` a partir da BD com **fallback ao `.env`**. Código existente (`config('invoicing...')`/`config('payments...')`/`config('mail...')`) **intocado**. No-op com tabela vazia → testes mantêm-se. Segredos `Crypt::encryptString` em repouso; casts bool/int antes do `Config::set`.
- **Mapa group/key→config-path** vive em `SettingsServiceProvider::MAP` (fonte única; alimenta os forms Filament). `mail.default` só vira `smtp` quando a **password SMTP** existe (gate — senão email partido).
- **Filament:** grupo nav **'Configurações'** (super_admin only via `canAccess`), base abstracta `app/Filament/Pages/Settings/SettingsPage.php` + 3 páginas: `FaturacaoSettings` (Moloni 11 + FIZ + toggles + acções "Testar ligação" moloni/fiz), `PagamentosSettings` (ifthenpay), `EmailSettings` (ZeptoMail SMTP + "Enviar email de teste"). Segredos **mascarados + write-only** (campo vazio = mantém; nunca enviados ao browser). `InvoicingStatus` antiga **removida** (consolidada). **62 testes.**
- **Gotcha Filament v5:** keys com ponto (`moloni.client_id`) colidem com statePath aninhado → mangle `.`→`___` no nome do campo (lookup usa a key real). Páginas custom auto-discovered recursivamente em `app/Filament/Pages` (sem editar o PanelProvider).
- **Gotcha cache settings:** invalidação por eventos Eloquent `saved`/`deleted`. `Setting::where()->delete()/update()` em massa **não** dispara eventos → fazer `Cache::forget(Setting::CACHE_KEY)` manual.
- **Pendente:** preencher as credenciais reais nas páginas (Moloni/ifthenpay/ZeptoMail SMTP token). ZeptoMail: host `smtp.zeptomail.eu`/`.com` (região), user `emailapikey`, pass = **SMTP token** (não o API token), from = domínio verificado.

## TODO (corrigir/rever depois) — registado 2026-05-31
1. **Perf — cura estrutural:** o fix actual (opcache CLI + `validate_timestamps=0`) é workaround. Cura real = tirar o projecto do bind-mount lento `D:\Mega` (ex.: mover para filesystem WSL2 `\\wsl$`). Renato quer manter no Mega por agora → adiado.
2. **Trade-off `validate_timestamps=0`:** obriga a `restart`/bypass após editar PHP. Se incomodar no dia-a-dia, avaliar modo dev com auto-reload (`validate=1`+`revalidate_freq`) e medir impacto no bind-mount.
3. **Storefront personalização:** renderizar os campos `customer_note`/`customer_attachment` na página de produto/checkout (entra com o frontend da loja).
4. **Shield role `guard_name` = TextInput livre (trap) — RESOLVIDO 2026-05-31:** o RoleResource do Shield (vendor `bezhansalleh/filament-shield`) tem `guard_name` como TextInput livre (default `web`). Se lá escreveres um guard inexistente em `config/auth.php` (só há `web` e `customer`), `Guard::getModelForGuard()` devolve null → ao apagar a role, o evento `deleting` (HasPermissions: `$model->users()->detach()` via `morphedByMany(null,...)`) dá 500 "Class name must be a valid object or a string", e a role fica **não-apagável pela UI**. Aconteceu com roles `Gestor_Encomendas` (guard `gest_enc`) e `teste` (guard `teste`) — leftovers de testes. **Fix permanente em `App\Models\Role extends SpatieRole`** (`config/permission.php` models.role → `App\Models\Role`; Shield usa `PermissionRegistrar::getRoleClass()` → respeita esta config): (a) `saving` guard rejeita `guard_name` fora de `array_keys(config('auth.guards'))` → bloqueia criar roles partidas em qualquer via (UI/seeder/tinker/API); (b) **override de `users()`** com fallback ao user model default quando `Guard::getModelForGuard()` devolve null → roles legacy com guard inválido ficam **apagáveis** em vez de rebentar. Plugin Shield hardcoda o seu RoleResource (sem hook de override) → guard na camada do modelo é mais robusto que mexer no form. Testes `RoleGuardTest` (4, inclui legacy bad-guard apagável).

## Personalização de produtos (artigos personalizados) — FEITO 2026-05-31
- **Produto:** toggles `allows_customer_note` + `allows_customer_attachment` (migration+model+ProductForm secção "Personalização"). Activáveis por produto.
- **Encomenda (workflow 4 fases):** `order_items.customer_note` (text) + `customer_attachment_path` (string). RM "Linhas": campo nota + upload anexo (`->image()->maxSize(5120)->acceptedFileTypes(png/jpeg/webp)->downloadable()->openable()->directory('order-attachments')`), visíveis só se o produto tiver o toggle ligado. Tabela: coluna nota + thumbnail. OrderInfolist (View): nota+foto na RepeatableEntry de items. Anexos no disco **`local`** (privado). **50 testes.**
- **GOTCHA Filament v5:** o utilitário `Get` dos closures `->visible()`/`->disabled()` é `Filament\Schemas\Components\Utilities\Get` — **NÃO** `Filament\Forms\Get` (não existe no v5; `php -l` não apanha, dá TypeError/500 ao montar o form). O review adversarial apanhou; testes que só renderizam a tabela não cobrem o path do form.
- **Pendente storefront:** renderizar estes campos na página de produto/checkout (entra com o frontend).

## Gestão de utilizadores admin (UserResource) — FEITO 2026-05-31
- **Faltava sítio para criar contas admin** (não havia UserResource — só dava para gerir roles via Shield). `App\Filament\Resources\Users\` (UserResource + Schemas/UserForm + Tables/UsersTable + Pages List/Create/Edit), grupo nav **'Configurações'**, **super_admin only** (`canAccess` espelha as Settings pages). Rota `/admin/users`.
- Form: name, email (unique), **password** (required só no create; em branco no edit mantém; `dehydrated(filled)`; `rule(Password::default(), filled)`; hash automático via cast `password=>'hashed'` no User), **roles** (Select multiple `relationship('roles','name')`). Tabela: nome+email, roles (badges). DeleteAction escondido para o próprio user e para super_admins (não te apagas a ti nem a outro super_admin). Testes `UsersResourceTest` (3).
- **Contas admin novas:** `canAccessPanel()=true` para todos → qualquer User entra no /admin, permissões controladas pelas roles (Shield). Para um admin "normal", criar role com permissões e atribuir.

## Backend gaps (ex-frontend) — build multi-fase EM CURSO 2026-05-31
Plano completo em `docs/BACKEND_BUILDPLAN.md` (workflow 8 agentes). Ordem: e-mail primeiro. Worker+scheduler em compose (`queue.worker`, `scheduler`; `docker compose up -d`). Eventos canónicos em `app/Events/` (auto-discovery Laravel 11+, SEM EventServiceProvider). **Decisão Renato:** carrinho abandonado corre sobre o motor de sequências (construtor de regras no admin), stock decrementa quando PAGO, grupo nav 'Marketing', construir todas as fases.
- **Fase 0 ✓** worker+scheduler+notifications table.
- **Fase 1 ✓** infra email: `BigornaMailable` (base queued, stamps `X-Bigorna-*` headers), `EmailLog`+`EmailStatus`, `MailServiceProvider` (RecordEmailLog em MessageSent + BlockOptedOutMarketing em MessageSending), Customer `email_opt_out`+`canReceiveMarketing()`, unsubscribe assinado (`/email/unsubscribe/{customer}`), `EmailLogResource` (read-only, Definições). Gotcha: testes de EmailLog NÃO usam Mail::fake (saltaria os listeners) — array transport.
- **Fase 2 ✓** transaccionais: eventos OrderPlaced/OrderPaid/RefundProcessed/CustomerRegistered + listeners (auto-discovered) + mailables (OrderConfirmation/PaymentReceived/RefundProcessed/CustomerWelcome, views em `mail/transactional/` que `@extends('mail.layout')`). Dispatch post-commit em CreateOrder/ConfirmPaymentAction/ProcessRefundAction/register. **91 testes.**
- **Fase 4 ✓** motor de funil/sequências (construtor de regras no admin): models `EmailSequence`/`EmailSequenceStep`/`EmailSequenceEnrollment` (enrollee polimórfico ULID), enums `EmailSequenceTrigger`(customer_registered/first_purchase/abandoned_cart/manual)/`DelayUnit`/`EnrollmentStatus`. Motor em `app/Marketing/`: `EnrollmentRouter` (enrollForTrigger + guarda re-inscrição app-layer), `AdvanceEnrollmentAction` (envia passo devido→agenda próximo→completa; cancela se opt-out/inactivo), `Jobs/AdvanceEnrollmentJob` (ShouldBeUnique), `Mail/SequenceStepMail` (marketing, merge `{{name/first_name/email}}` via `str_replace`+`e()`, NUNCA Blade::render). Contrato `Marketing/Contracts/Enrollable` (Customer implementa). Comando `sequences:tick` (everyMinute). Evento `CustomerFirstOrderPaid`+detecção 1ª compra no `ConfirmPaymentAction`. Listener multi-handle `EnrollInSequences` em `AppServiceProvider::boot` (Event::listen — NÃO auto-discovered). Filament `EmailSequenceResource` (grupo **Marketing**)+`StepsRelationManager` (reorderable builder)+`EnrollmentsRelationManager` (read-only+inscrever+cancelar). Delay = após passo anterior. **99 testes.**
- **PARADO aqui a pedido do Renato (fim da Fase 4). FALTA fazer:**
  - **Fase 3 — Carrinhos abandonados (sobre o motor):** `Cart`+`CartItem`+`CartStatus`; `Cart implements Enrollable` (emailAddress/mergeData); `FlagAbandonedCartsAction`+comando `carts:flag-abandoned` (15m) que inscreve carrinho numa `EmailSequence` trigger `abandoned_cart` (AdvanceEnrollmentAction já é genérico — só falta o enrollee Cart). Link recuperação assinado `/cart/recover/{cart}` (placeholder até storefront). `AbandonedCartResource` (Vendas)+widget. Cadência via builder (já existe).
  - **Fase 5 — Stock (decrementa quando PAGO):** `StockMovement` ledger, `order_items.variant_id` (migration+fillable), `low_stock_threshold` em Product/ProductVariant, `DecrementStockOnOrderPaid` (listener OrderPaid — evento JÁ existe), `RestockOnRefund` (RefundProcessed — JÁ existe), `AdjustStockAction`, eventos `LowStockReached`/`StockOutEvent`, `config/inventory.php` (default 5), `StockMovementResource` (Catálogo).
  - **Fase 6 — State machine + envio:** `OrderStateMachine` (mapa transições), `OrderEvent` timeline+migration, `Carrier` enum (CTT), migration+fillable `tracking_number`/`carrier`/`shipped_at`, `Order::transitionTo()`/`markShipped()` (saveQuietly — NÃO tocar no total derivado), eventos `OrderShipped`/`OrderCancelled`/`OrderStatusChanged` + listeners+mailables `SendOrderShipped/CancelledEmail` (deferidos de propósito na Fase 2 — só criar quando os eventos existirem, senão auto-discovery rebenta), `MarkAsShippedAction` Filament, refactor ConfirmPaymentAction/ProcessRefundAction p/ `transitionTo`.
  - **Fase 7 — Notificações admin (sino):** `->databaseNotifications()`, listeners `NotifyAdmins{OrderPlaced,OrderPaid,LowStock,TicketOpened}`→`sendToDatabase`, `AdminRecipients`, evento `TicketOpened`+dispatch no ContactController, prune diário. Tabela `notifications` JÁ migrada (Fase 0).
  - Plano completo + gotchas: `docs/BACKEND_BUILDPLAN.md`.

## Dashboard de admin (KPIs) — FEITO 2026-05-31
- **Vista rápida do negócio** na Dashboard (`/admin`). Serviço `App\Support\DashboardMetrics` (queries read-only, testável, ranges explícitos): receita = encomendas em estado faturável (`OrderStatus::revenueStatuses()` = Paid/Processing/Shipped/Completed) bucketed por `COALESCE(paid_at, created_at)`. 4 widgets auto-descobertos em `app/Filament/Widgets/` (polling OFF p/ perf):
  - **RevenueStatsWidget** (sort 1): receita Hoje/Semana/Mês/Ano, cada uma com Δ% vs período anterior (ontem/sem./mês/ano) + sparkline + cor verde/vermelho.
  - **OverviewStatsWidget** (sort 2): Encomendas (mês, Δ), Ticket médio (AOV), Mensagens por responder (tickets Open + nº pendentes), Clientes novos (mês, Δ).
  - **RevenueChartWidget** (sort 3, full): linha receita últimos 12 meses.
  - **LatestOrdersWidget** (sort 4, full): últimas 8 encomendas (nº/cliente/total/estado/data).
- `OrderStatus::revenueStatuses()` adicionado (reusa `isInvoiceable()`). Testes `DashboardMetricsTest` (6, lógica) + `DashboardWidgetsTest` (4, render). **80 testes.**
- **GOTCHA:** `Order::saving` deriva `total` de subtotal+IVA+portes → nos testes definir `subtotal`, nunca `total` directo. `Stat`: `description()`/`color()` vêm de traits `HasDescription`/`HasColor`. `pollingInterval` é `?string` (não static).

## Performance do admin — optimização 2026-05-31 (workflow diagnóstico)
- **Diagnóstico medido (workflow 4 agentes):** a lentidão a navegar **NÃO é DB/Redis/Shield** (1.3ms DB/página, tudo indexado, cache spatie a acertar em Redis, datasets minúsculos). Causa = **execução PHP + stat de ficheiros no bind-mount D:\Mega (21× mais lento: 0.45ms vs 0.02ms/stat; re-stat dos 13438 ficheiros vendor a frio = 9.9s)**, agravada por: (1) **SPA mode desligado** → cada clique = page-load completo (~0.8s + rebuild sidebar 309-552ms); (2) **`php artisan serve` single-worker** serializa os ~10 sub-pedidos por navegação (serial 5× = 3.98s vs concorrente = 1.14s); (3) discovery de 113 ficheiros Filament + config/rotas não-cacheados por request. Opcache web OK (97.9% hit). Vite NÃO é factor (admin usa assets publicados, não o pipeline Vite).
- **Aplicado (ganhos sem fricção no dev/testes):** (a) **`->spa()`** no AdminPanelProvider — só o conteúdo re-renderiza por clique (maior win); (b) **`PHP_CLI_SERVER_WORKERS: '8'`** no compose (`docker compose up -d` recria) — acaba a serialização dos sub-pedidos; (c) **realpath_cache_size=8192K/ttl=1800** no `99-joca-perf.ini` (JIT tentado mas **revertido** — xdebug carregado, mesmo com mode=off, faz override de `zend_execute_ex()` e desactiva o JIT com warning; para JIT teria de se descarregar o xdebug, opt-in); (d) removido `FilamentInfoWidget`; (e) nav-badge dos Tickets cacheado 60s (Redis). 70 testes mantêm-se. **GOTCHA:** após editar a ini/compose → `docker compose up -d laravel.test` (não basta restart para env do compose).
- **NÃO aplicado (opt-in, apresentados ao Renato):** (1) **mover vendor/storage/node_modules para fora do bind-mount Mega** (named volumes + exclusões MEGAsync) — é a *cura estrutural* (elimina o 21×; boot a frio 10s→~1s) mas invasivo (`docker compose down`+`composer install`); = TODO #1. (2) **`artisan optimize` + `filament:optimize`** (config/route/event/filament-components cache) — grande win mas **atrapalha o workflow de dev/testes** (config:cache fixa env → testes correm config errada; filament cache esconde resources novos até `filament:clear-cached-components`). Adiados enquanto há desenvolvimento activo. (3) **APP_DEBUG=false** — adiado (Renato está a debugar; mantém páginas de erro úteis).

## Navegação do admin (sidebar) — reorganizada 2026-05-31
- **Antes:** grupo "Loja" sobrecarregado (catálogo+vendas+finanças+suporte) + colisão "Configuração" (singular, métodos) vs "Configurações" (plural, settings) — confuso.
- **Agora (6 grupos):** *Dashboard* (sem grupo, topo) · **Vendas** (Encomendas·Pagamentos·Reembolsos) · **Catálogo** (Produtos·Categorias) · **Clientes** (Clientes·Tickets) · **Conteúdo** (Páginas·Menus·Posts·Cat. blog·Tags) · **Definições** (Utilizadores·Roles·Métodos envio·Métodos pagamento·Faturação·Pagamentos·Email).
- Ordem dos grupos forçada em `AdminPanelProvider::navigationGroups([...])`. Roles (Shield) movida para Definições via `FilamentShieldPlugin::make()->navigationGroup('Definições')->navigationLabel('Roles')->navigationSort(2)` (o plugin usa o trait fluent `HasNavigation`; não há config publicada). Cada resource/page com `navigationGroup`+`navigationSort` actualizados. 70 testes mantêm-se verdes.

## Última sessão
2026-05-31 (3ª) — **Build do sistema de e-mail (fases 0-4 de 8 do plano de backend).** Via workflow de 8 agentes → `docs/BACKEND_BUILDPLAN.md`. Feito: Fase 0 (worker+scheduler+notifications), Fase 1 (infra: BigornaMailable+EmailLog+opt-out+unsubscribe+recurso read-only), Fase 2 (transaccionais: confirmação/pagamento/reembolso/boas-vindas, via eventos post-commit), Fase 4 (motor de funil/sequências — construtor de regras no admin, grupo Marketing). **99 testes** (80→+6 dashboard... na verdade 70→dashboard 80→+6 email infra 86→+5 transaccionais 91→+8 funil 99). Parado no fim da Fase 4 a pedido. Faltam Fases 3/5/6/7 (ver Pendente + "Backend gaps"). Antes disso na mesma sessão: dashboard KPIs, reorganização do menu, optimização de performance (SPA mode+workers).

2026-05-31 (2ª) — (1) **Roles partidas (guard inválido) resolvidas a sério**: `App\Models\Role` agora (a) rejeita guard inválido no save e (b) override de `users()` torna roles legacy apagáveis (ver TODO #4). Limpas `Gestor_Encomendas` e `teste`. (2) **Settings não estavam "a meio"** — só invisíveis por falta de restart (opcache `validate_timestamps=0`); restart expôs `admin/{faturacao,pagamentos,email}-settings` (grupo Configurações). Falta só preencher credenciais. (3) **Tickets: abas** Todos/Contactos/Encomenda. (4) **UserResource** — criar/gerir contas admin + atribuir roles (faltava por completo). **70 testes** (62→+4 RoleGuard→+1 tabs→+3 Users).

2026-05-31 — Perf fix (~5×, ver Ambiente) + personalização de produtos completa (produto+encomenda, 50 testes). Backoffice a correr em :8080.

2026-05-30 — **Admin completo (10/10 módulos), 47 testes.** Workflow de design (12 agentes) → implementação faseada: Fase 0 foundation (Customer/Address, Order estendida) → Clientes → Produtos(variantes/galeria/SEO) → Pagamentos(ifthenpay MB/MBWay/Cartão + webhook) → Envios(zonas CP, escalões) → Tickets+Contacto → Reembolsos(nota crédito) → Blog → CMS(Builder de blocos) → Login&Segurança(Shield RBAC + contas cliente + hardening). Cada fase com testes verdes. Pendente: credenciais reais Moloni/ifthenpay, front-end da loja.
