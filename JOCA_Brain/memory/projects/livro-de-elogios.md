---
name: livro-de-elogios
description: Livro de Elogios — Nova Plataforma 2026. SaaS multi-tenant multi-país para empresas gerirem elogios de clientes; substitui plataforma em produção (27.000+ empresas). Esta entrada = só a frente PRODUTO; visão de toda a pasta-mãe em [[livro-de-elogios-geral]].
type: project
directorio: /Users/renatoferreira/MEGA/Livro_de_Elogios/2026_Nova_Plataforma
---

> **⚠ Ambiente migrou para macOS (2026-07-08).** Paths abaixo que digam `D:\Mega\...` são da máquina Windows anterior — o real agora é `/Users/renatoferreira/MEGA/Livro_de_Elogios/...`. PHP 8.5.2 no PATH.
> **✅ A plataforma VOLTOU a ser repo git neste Mac (2026-07-08, tarde).** `.git` real enxertado, `origin` = `https://github.com/setuptechpt/livro-de-elogios-v3.git`, HEAD em `renato-dev`. **`renato-dev` = `182c1e5`** (fast-forward de `88dda1e`). `.env` na raiz da plataforma + `database.sqlite` (BD com dados) confirmados gitignored. `.env` NÃO está no MEGA (dotfiles não sincronizam). Commits normais a partir de agora (sem snapshot MEGA). Regra de git absoluta mantém-se: só push `renato-dev` com ordem.

> **Esta entrada cobre só a plataforma (`2026_Nova_Plataforma/`).** Visão de toda a pasta `D:\Mega\Livro_De_Elogios` (8 frentes: legacy Wireframes, marketing, redes sociais, Brasil, parceria FIZ) → [[livro-de-elogios-geral]]. Umbrella `CLAUDE.md` na raiz da pasta-mãe criado 2026-07-01.

**Stack:** Laravel 13.14 + **PHP ≥8.4** (o `dev` exige 8.4; vendor compilado p/ 8.4) + Filament v5 (backend/admin) · React 19 + Vite 8 + Tailwind 4 + TypeScript (frontend) · MySQL (SQLite local) · Sanctum stateful SPA + Socialite (Google/Facebook) · Ifthenpay (MB/MBWay) + Stripe · Moloni (faturação) · Spatie Media Library · Sentry · Pest.
**Objectivo:** Reconstruir de raiz a plataforma de elogios. Multi-país com **isolamento completo por país** (gateway, produtos, encomendas, portes, CMS isolados; detecção por IP+sessão+perfil; tabela `countries` = fonte de verdade). PT e BR base, arquitectura para N países.
**Directório:** `D:\Mega\Livro_De_Elogios\2026_Nova_Plataforma`
**Idioma:** PT-PT sempre, nunca BR. Copy direto e humanista (foco nas pessoas).
**Why:** Plataforma antiga (27k+ empresas) em produção; esta é a substituição de raiz.
**How to apply:** Projecto full-stack. Skill-first: `frontend` (director → `react-patterns`/`react-composition`/`tailwind`/`shadcn`), `laravel-specialist`, `filament`/`filament-builder`, `laravel-react`, `auth`, `payment-integration`+`portugal-payments` (ifthenpay), `portugal-invoicing` (Moloni), `webhooks`, `saas-patterns`, `search`, `file-storage`, `rest-api`. Design (Renato é designer): `design-system`/`design-tokens`/`design-review`/`brand-guidelines`. Plugin local `design-craft@renato-local` (`/design-craft`) instalado scope project. Backend/DevOps → explicar decisão 1 linha antes de implementar.

## Regras de marca (CRÍTICAS)
- **1 só laranja** `#FD5000` (hover `#E54300`). Fonte única **Poppins**. Botões `border-radius: 9999px`, weight 600.
- **Fundos pretos PROIBIDOS** — nunca `#000/#0D0D0D/#111` como background de secção/card. `--color-dark` só p/ texto/botão Dark.
- SEM emojis em UI. SEM estrelas/rating de texto (usar SVG vetorizado quando necessário). Iconografia: Font Awesome + lucide-react no backoffice.
- Tokens completos em `CLAUDE.md` (cores, radius, shadows). Moeda backoffice: `€{n.toFixed(2).replace('.',',')}`.

## ⚠ Fontes de estado divergentes (ler na ordem certa)
- `docs/estado.md` — **DESACTUALIZADO** (sessão 12, 2026-05-25). Não confiar.
- `README.md` + `CLAUDE.md` = stack/fases reais.
- Specs: `spec-encomendas-institucionais.md` (raiz, novo, módulo institucional Pedro), `docs/email-automations-strategy.md`, `docs/marketplace-integration-spec.md`, `docs/widget-api-spec.md`, `docs/diretivas-revendedores.md`.
- **ATENÇÃO:** `docs/backoffice-cliente-ESTADO.md`, `docs/backoffice-cliente-CONTRATO.md`, `docs/goal-auth-checkout.md` — referenciados na memória anterior mas **NÃO EXISTEM** no repo local (eram ficheiros do Mac não committados).

## ⚠ REGRAS DE GIT (Renato — 2026-06-29, absolutas)
- **NUNCA push para o GitHub sem ordem explícita do Renato. NUNCA criar branches sem ordem.** (Uma sessão criou `feat/backoffice-integration` + PR #16 por iniciativa própria — não autorizado.)
- **`dev` é LOCAL-ONLY** — nunca push para `dev`, nunca merge para `dev` no remoto.
- Quando for ordenado push para o git → **alvo é SEMPRE `renato-dev`** (não `dev`, não feature branches).
- **2026-06-29:** branch `feat/backoffice-integration` APAGADA a pedido do Renato — **local E remoto** — e **PR #16 FECHADO** (não merged). Os 5 commits da integração backoffice↔backend (uploads/auth/12 clients/tour) deixaram de existir (só reflog temporário do GitHub). Os 43 ficheiros do admin redesign (uncommitted) repostos em `dev` local via stash. Branches restantes: `dev` (local, à frente de `setuptech/dev` 1 commit não-pushed), `backup/local-pre-dev`, `master`.
- **2026-06-30:** Renato ordenou **force push** explícito do estado local para `setuptech/renato-dev` ("podemos perder o que está lá"). Antes do push, todo o working tree (overhaul backoffice 30 itens + admin dark redesign + demo toggle + componentes novos = 71 mod + 9 untracked) foi **commitado num único commit `2686461`** sobre `dev` local (`d90e373`). `git push --force setuptech dev:renato-dev` → `125df48…2686461 (forced update)`, descartou os 2 commits que `renato-dev` tinha próprios (incl. merge `125df48`). `dev`/`master` **não tocados**. `.env` confirmado gitignored (sem secrets no push). NÃO foi re-corrido build/teste (honrou "faz mesmo force push") — última validação verde foi a da sessão anterior.

## Overhaul backoffice (30 itens) via workflow — 2026-06-29
Workflow `le-backoffice-overhaul` (11 agentes, fundação→áreas-paralelas→rename→verify). **Build final VERDE** (confirmado em disco). Camada mock+frontend (sem backend real). Feito: #1 tutorial(ausente) · #2 nav User/Empresa separado · #3/#4 conta-nova→PerfilPessoal (novo, grupo Empresa escondido até haver empresa) · #5 "Sem plano" · #6 ícones lucide · #7/#11 foco modais(base) · #9 créditos single-source (PLAN_LIMITS dos seeders) · #10 campos loja (description+logo; região/lat-lng/horário/socials NÃO existem no backend→TODO) · #12 billing fora do plano · #13 "Créditos"→"Páginas Digitais" (~120 ocorrências, 13 ficheiros; keys/rotas intactas) · #14 ImageCropUpload (crop→800×800 WebP, sem upload real→TODO backend) · #15/#22 perfis públicos ligados ao mock (causa: chamavam API axios morta→fallback CTT hardcoded) · #16 funcionários auto-accept 10s · #17 mensagens removidas · #18 Partilha redesign · #19 downloads reais via canvas (SVG/PDF→TODO) · #20 QR reais (qrcode.react) · #21 Créditos page unificada c/ planos · #23 ShareModal · #26/#27/#28 removidos 2FA/contas-associadas/sessões · #29 loja nasce inactiva + activação gasta Páginas Digitais acima do limite.
- **Novos componentes partilhados:** `components/ui/{QrCode,ShareModal,ImageCropUpload}.tsx`. Novo `lib/account.ts` (`accountHasCompany`). Nova `pages/app/PerfilPessoal.tsx`. Deps add: `qrcode.react`, `react-easy-crop`.
- ⚠ **GOTCHA Git+MEGAsync (CRÍTICO):** com vários agentes a escrever em paralelo na pasta sob MEGAsync, o MEGAsync criou conflict-copies `X(1).tsx` e apagou os canónicos → partiu o build a meio. O verify agent restaurou os nomes. **Lição: workflows que escrevem ficheiros em paralelo numa pasta MEGAsync são perigosos — pausar MEGAsync OU usar isolation:worktree fora da pasta sync.**
- Após instalar deps novas, o dev server a correr dá 504 "Outdated Optimize Dep" → reiniciar Vite (rm -rf node_modules/.vite + restart).
- **TODO(backend) pendentes** (8): create-company flow, PATCH /auth/email, upload de blob (path na BD), directório/perfis via API real, coordenadas lat/lng, geração SVG/PDF, addBoughtCredits escalões antigos. **TODO(produto):** Q1 limites por tier (UI usa BASE_SHOP_LIMIT=5 vs seeder maxShops=3).

## Revisão profunda — wave 1 + bloqueadores (2026-06-29)
- **Bug de foco em modais (transversal) CORRIGIDO** em `components/ui/Modal.tsx`: o `useEffect` de focus dependia de `[open, onClose]`; `onClose` é passado inline → nova identity a cada render → cada tecla (setForm) re-corria o effect → `dialog.focus()` roubava o foco. Fix: `onCloseRef` + deps só `[open]`. Cobre Lojas + Funcionários + todos os modais. Validado em runtime (playwright: 6 teclas, foco mantido).
- **Mensagens/Inbox removido** do nav (`AppSidebar`) + rota (`Backoffice.tsx`); página `Inbox.tsx` fica dormente (tipo `'inbox'` mantido no `NavKey`). (H28)
- **Tutorial/tour já ausente** no `dev` (sem dep driver.js) — A1 resolvido por estado.
- **BLOQUEADOR DE FUNDO (gate de ~metade do scope):** backoffice = MOCK, site público = backend real. Itens C10 (loja no directório), C11 (perfil dados reais), E16-19 (imagem/email/perfil pessoal — `Perfil.tsx` mostra o `user` do mock, não o real), F23, A2-A4 (User vs Empresa) **não são corrigíveis de forma real sem decidir mock↔backend** (wiring real foi apagado; vários endpoints não existem = lane Pedro). "Corrigir" no mock = maquilhagem (proibido pelo Renato).
- **BLOQUEADOR DE PRODUTO:** PRD Q1 (limites por tier) + Q2 (pricing) ABERTAS → D13/D15/B8(custo activação) bloqueados até decisão do cliente.
- **Wave segura pendente (frontend-only, sem decisão):** D14 rename Créditos→Páginas Digitais, E20-22 remover 2FA/associadas/sessões (tab Segurança), G24/G26/G27 (partilha: design + QR real via lib pública + modal de partilha), B8-parcial (loja nasce inactiva = flip `emptyForm().isActive`), B9 (auto-accept funcionário 10s, mock), A5 (afinar "sem plano").

## Consola: 401 /auth/me + ruído de extensões (2026-06-29)
- **`/auth/me` 401:** está dentro do grupo `auth:sanctum` (routes/api.php) → 401 por design quando não autenticado (há teste `AuthTest.php:109` a exigir 401 — NÃO mexer no backend). O browser loga o 401 nativamente (não suprimível por JS). Fix no frontend (`hooks/useAuth.ts`): flag `localStorage` `le-auth` — só sonda `/auth/me` se já houve login neste browser (set no login/register, clear no logout e em sessão inválida/self-heal). Visitante fresco → sem probe → consola limpa. Validado com playwright (sem flag=0 chamadas/0 erros; com flag=probe; flag stale=401 1× + clear → reload seguinte silencioso). **Único caller de `/auth/me` é o `useAuth`; `getMe()` em `api/auth.ts` é dead code.** Login social está meio-implementado (callback devolve JSON+token Bearer, sem handler frontend) — não é fluxo activo, a flag não o regride.
- **`proxy.js` / "message channel closed" / "disconnected port object":** ruído de **extensão do Chrome** (content script injectado), NÃO da app — grep no `src` por `chrome.runtime`/`proxy.js`/ports = 0 (só `IntersectionObserver/ResizeObserver.disconnect()` legítimos). Sem efeito colateral no código.

## ~~Backoffice corre sobre MOCK~~ — SUPERSEDED 2026-07-05 (mock APAGADO; tudo real — ver sessão 2026-07-05)
- O `/app` (backoffice cliente) corre **100% sobre `frontend/src/mocks/backofficeStore.ts`** (mock em memória, account-agnostic). Os 17 clients `api/backoffice/*` importam o store; **não há toggle mock/real** no `index.ts`. O **auth é real** (`hooks/useAuth.ts` → `/auth/me`/login/register contra o backend) mas o backoffice ignora a conta autenticada e mostra o seed do mock.
- **Causa de "conta nova já tem loja/dados":** o wiring backoffice→backend real estava nos 5 commits da branch `feat/backoffice-integration` (apagada na sessão anterior). O `dev` está em `d90e373`, antes do wiring → backoffice voltou ao mock seeded (CTT/Padaria/lojas/elogios/etc.). DB real está vazia (`companies=0`).
- **Fix aplicado (Renato escolheu "mock vazio + toggle demo"):** `backofficeStore.ts` agora arranca **VAZIO por defeito** (1 empresa-shell vazia "A minha empresa", stats 0, todas as colecções `[]`, singletons zerados) → conta nova = backoffice limpo, sem loja. Dataset demo CTT/Padaria preservado atrás de toggle: **`?demo=1`** carrega-o (persiste em `localStorage` `le-bo-demo`), **`?demo=0`** volta a limpo. Mantidos: `plans`/`creditPackages`/`shareAssets`/`user` (catálogo/template). Build verde (tsc+vite). **Por commitar** (working tree `dev` local).
- ⚠ Fix arquitectural definitivo = ligar o backoffice ao backend real (o trabalho apagado) — pendente; vários endpoints ainda nem existem (gaps doc).

## Estado git + ambiente local (2026-06-26, sessão 2)
- **Branch `dev`** a track `setuptechpt/livro-de-elogios-v3` (o repo REAL). O antigo `origin` `MirrasPT/livro_redesign` é repo de protótipos SEPARADO (histórias NÃO-relacionadas). Branch `backup/local-pre-dev` guarda o working tree pré-switch. Backend = Pedro (`pedro-dev`→PRs); frontend/admin-design = Renato.
- **TODO o redesign do admin desta sessão está POR COMMITAR no `dev`.**
- **Login admin:** `admin@livrodeelogios.com` / `password` (seeded; trocar em produção). 35 users de teste, todos password `password`.

### Ambiente dev local (Windows) — montado nesta sessão
- **PHP 8.4.22 portátil** em `C:\Users\renat\php84` (XAMPP é 8.2; `dev` exige ≥8.4). `php.ini` lá: **opcache ON + JIT OFF** (JIT segfalha no Windows, exit 5); extensões curl/exif/gd/gmp/intl/mbstring/openssl/pdo_sqlite/sockets/sodium/sqlite3/zip. `composer.phar` em `C:\Users\renat\php84`.
- **Arranque:** backend `& C:\Users\renat\php84\php.exe artisan serve --host=127.0.0.1 --port=8000`; frontend `npm run dev` em `frontend/` (Vite :5173). Aceder via **`localhost`** (não 127.0.0.1 — `SESSION_DOMAIN=localhost`).
- **DB:** SQLite `backend/database/database.sqlite` (35 users), 6 migrações novas do dev aplicadas. `.env` na raiz = cópia do `.env.example` do dev (sqlite default, Sanctum stateful :5173, mail=log). Serviços externos desligados em local.
- ⚠ **Octane/FrankenPHP IMPOSSÍVEL no Windows nativo** (exige `pcntl`/`posix` POSIX; FrankenPHP só Linux/Mac). Velocidade Octane = WSL2/Docker. Em Windows: `artisan serve` + opcache (~0.2s/req warm) é o tecto; é single-thread → pedidos Livewire serializam.
- ⚠ **`composer install` no Windows falha symlinks** (precisa Developer Mode) → vendor pode ficar inconsistente → **opcache corrompe a tabela de funções** (`mb_convert_encoding`→`intltz_create_default`). Fix: `rm -rf vendor && composer install --no-scripts --ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix`, depois `php -d opcache.enable_cli=0 artisan package:discover`.
- Mobile: Flutter app em `mobile/` + `APP-MOVEL.md`.

## Admin Filament — design system DARK (2026-06-26)
**Decisão Renato: admin é DARK-MODE-ONLY** (`->darkMode(true, isForced: true)`) — **excepção** à regra "fundos pretos proibidos" (essa vale só p/ site/marketing). NÃO reverter para light.
- Tema = `backend/resources/css/filament/admin/theme.css` (fonte da verdade do design do admin; tokens `--st-*`). Charcoal quente (oklch hue ~55, nunca preto puro), 1 acento `--st-accent` (#FD5000 / #FF6A2B dark), Poppins (peso 900 carregado), radius 12 / pill, divisor laranja 44×3px no page header.
- Stats dashboard: número laranja peso 900 + label uppercase + sparkline 1-cor (`->color('primary')`) + descrição humanista (NUNCA "-100%"/trending icons). Contador animado: render hook `view('filament.stats-counter')`.
- Cores semânticas SÓ p/ estado: success #1A9A5C, info #1C64F2, error #c0392b, warning #d99a2b, muted #595959. Taxonomia/categoria = badge neutro.
- `fi-section-not-contained` = transparente (sem box-in-box). Item activo sidebar = fundo laranja-soft (sem side-stripe). Login custom dark (`app/Filament/Auth/Login.php` + `resources/views/filament/auth/login.blade.php`) espelha o AuthLayout do site.
- Auditoria anti-slop completa (workflow 6 agentes + 4 implementadores): emojis/em-dash/paletas multi-cor/roxo removidos; página API Docs (`resources/views/filament/pages/api-docs.blade.php`) reescrita dark. 3 dashboards: Elogios/Financeiro/Operacional consistentes.

## Roadmap / fases
- Fase 0 Design (protótipos HTML + design system) ✅
- Fase 1 Setup backend (Laravel, auth, migrações, 12 domínios) ✅
- Fase 2 Website público (React 19/Vite 8, 20 páginas committadas) ✅
- Fase 3 Backoffice utilizador — **RECUPERADO** (restaurado da `backup/local-pre-dev`, tsc verde, por testar/commitar)
- Fase 4 Admin + badges + rankings + afiliados + revendedores + multi-país — por iniciar
- Pedro (paralelo): módulo encomendas institucionais em curso (spec-encomendas-institucionais.md)

## Integração backend ↔ backoffice (2026-06-28) — branch `feat/backoffice-integration` · **PR #16**
**Decisão de arquitectura (Renato escolheu): backend Laravel REAL = fonte única de verdade** (não um store mock unificado). O frontend já estava construído sobre um seam swappável: as 15 páginas `/app` falam com `api/backoffice/*` clients que ANTES embrulhavam um mock store em memória (`mocks/backofficeStore.ts`, sem persistência). Esta sessão ligou tudo à API real.
- **Backend novo (IDOR-safe, 20 testes Pest verdes — `tests/Feature/Phase3Test.php`):** trait `StoresUploadedImage` (disco público, URL `/storage`, limpeza de órfãos) + endpoints multipart `POST /companies/{slug}/logo|cover`, `/shops/{shop}/image` (→ `shops.logo`, a tabela `shops` NÃO tem coluna `image`), `/showcase/{item}/image`; `POST /companies/{slug}/employees` (criar funcionário, policy + `shop_id` scoped). Campo multipart = `image`. `storage:link` feito.
- **Auth real confirmado** (já estava ligado): Sanctum stateful (csrf→register/login/logout/me), `/app` protegido, **conta nova = backoffice vazio** + `CreateCompanyPrompt` (POST /companies). Login seed: `maria.silva@teste.pt`/`password` (dono de `clinica-saude-total`).
- **12 clients `api/backoffice/*` ligados mock→real** (assinaturas + query keys preservadas; pages intactas). `orders` 100% real; `analytics`+`inbox` sem endpoint → ficam mock; resto **parcial** (real + `// TODO(real-backend)` onde o backend não expõe campos — sem dados inventados).
- **Headline ✅ provado e2e no browser:** carregar logótipo em *A minha empresa* → persiste em `companies.logo` → `/empresa/{slug}` (PerfilEmpresa) renderiza-o. Wiring: `company.uploadCompanyLogo/Cover`, Empresa.tsx file-picker (era stub "em breve"), PerfilEmpresa hero usa `cover_image ?? logo` (era placeholder picsum). Também UI de upload de imagem de **loja** (Lojas) e **montra** (Montra). **`/storage` já proxied no `vite.config` → URLs relativas renderizam.**
- **Onboarding:** tour guiado driver.js (`OnboardingTour.tsx` + css, 7 passos PT-PT, anchors `data-tour`, persistido em `localStorage` por userId, repetível via "Ver tutorial" no menu).
- **Gaps de backend** (para o Pedro) enumerados/priorizados em **`docs/backend-gaps-backoffice-integration.md`** (créditos/contagens por loja, preço+listagem-admin da montra, elogios rejeitados, ledger/billing, persistência de widgets, role/convites de funcionário, toggles de visibilidade, endpoints analytics/inbox/notifications).
- Commits no branch: `d90e373` (Ciclo 1-3 + diretório, ESTE no `dev` local) · `11dcc5b` (backend+auth+tour) · `61ee40d` (12 clients) · `773744b` (headline) · `d86e7cf` (shop/montra image UI + doc) · `8c594cf` (remove botão Criar-conta do topo do login). **PR #16 aberto contra `dev` (não merged — toca backend, lane do Pedro).**

## Ciclo 1-3 do backoffice + diretório (2026-06-28, commit `d90e373` no `dev`)
Workflow de review (15 páginas `/app`): Ciclo 1 fidelidade vs protótipos `design/app/*.html` (só 7 têm protótipo), Ciclo 2 marca/a11y/responsivo, Ciclo 3 smoke browser — **50 correções page-local** + 2 shared (FilterPills focus-ring tone-aware, Button sm 40px). **Diretório de Empresas (`FazerElogio.tsx`) limpo: removido mock `COMPANIES`/`MAP_PINS`/contagem fixa 11.247 → 100% API `/directory`** (4 empresas reais seed), pins do mapa só com coords reais, empty states, paginação real. Fix: `api/companies` normaliza paginator Laravel cru (`total` no topo) → `{data,meta,links}` (corrigia fallback fantasma 11.247).

## Follow-up UX pós-imagens (2026-06-30, fim) — POR COMMITAR (working tree `dev`)
Bugs/UX reportados pelo Renato a testar no browser, todos corrigidos+provados (playwright), **ainda não commitados** (4 lotes além do `b169b2b`):
1. **Saldo "Páginas Digitais" na sidebar a 0** — efeito colateral da migração: a sidebar lia `company.stats.creditsBalance` (mapper real → default 0) enquanto a página Créditos lê o mock. Fix (`AppSidebar`): passa a ler `useQuery(qk.creditOverview, getCreditOverview)` (mesma cache da página; o buy invalida `qk.creditOverview` → actualiza nos dois). NÃO tocar nos créditos por loja (feature parkada).
2. **Avatar do topo-direito não reflectia** — `AppTopbar` mostrava `user.initials` (mock); agora lê `authUser.avatar` (real, mesma fonte do Perfil) → `<img>` ou iniciais. **+ nome ao lado** do avatar+chevron (chevron roda ao abrir). **Dropdown redesenhado** (header com avatar+nome+email faixa cinza, itens rounded-xl, sombra suave, w-264). **Sidebar: removido o chip nome+logout repetido no fundo** (logout fica só no dropdown do topo); link de créditos mantém-se. (Limpos imports órfãos: useNavigate/LogOut/useAuth na sidebar.)
3. **Toggles "Perfil público activo"/"Montra" não persistiam** ("não deixa guardar a menos que mude algo"): `PATCH /companies/{slug}` não validava/gravava `is_public`/`montra_enabled` (frontend até as dropava, TODO). Fix REAL: backend valida+grava (já `fillable`+cast boolean); `toCompanyPatch` envia `publicProfileEnabled→is_public`, `montraEnabled→montra_enabled`. Provado e2e (PATCH persiste ambos os sentidos). **→ o `TODO(backend)` "is_public/montra_enabled sem write path" do `_shared.ts`/`company.ts` está RESOLVIDO.**
4. **Card de loja (`Lojas.tsx`): "Token API" removido por completo** (botão+prop+estado+mutação `rotate`+modal+imports/helper órfãos `KeyRound`/`RefreshCw`/`rotateShopToken`/`maskToken`+subtítulo). Layout: Ver elogios (full) · Carregar|Editar · QR Code (full). **"Ver elogios" já abre Elogios filtrada por loja** (`/app/elogios?loja=<slug>`; Elogios lê o param + pré-selecciona o dropdown) — confirmado, não precisou de mudança.
- **Fluxo criar-empresa** (parte do `b169b2b` mas afinado aqui): `getCompanyProfile` devolve `null` sem empresa (em vez de lançar) + `createCompany` (POST /companies, country='PT') + `Empresa.tsx` ganhou **modo criação** (form em branco, esconde uploaders/perfil-público até existir empresa). Browser apanhou o gap (a página era só edição → erro em conta vazia) + o barrel `index.ts` não re-exportava `createCompany`.
- **Gotcha:** `artisan serve` (single-thread Windows) larga conexões (HTTP 000/401 intermitente) sob rajada de curls + ao recarregar um ficheiro PHP editado a meio de um script multi-request → testes e2e parecem falhar (slug vazio, Unauthenticated). Não é bug do código; testar 1 request de cada vez OU reiniciar o backend. (Renato tem `renato-teste` criada na BD — preservar.)

## Sistema de imagens REAL (2026-06-30, tarde) — backoffice mock→real, no `renato-dev`
**Pedido do Renato:** uploads de imagem que ficam gravados e reflectem, **Laravel real, sem mock**; imagens em pasta (não na DB), **WebP 800×800**, DB só o caminho.
- **Backend (`b169b2b`):** `app/Services/ImageUploadService.php` — GD→WebP 800×800 (cover-crop centrado, preserva alpha), grava em `storage/app/public/<entidade>/`, **DB guarda só `/storage/...`** (path root-relative via `parse_url(Storage::url())` — NUNCA o domínio absoluto: `Storage::url()` devolve `http://localhost:8000/...` porque `APP_URL` está set; guardar isso partiria produção + a limpeza de órfãos). Limpa o ficheiro antigo. 5 endpoints IDOR-safe (`authorize('manage')`): `POST companies/{slug}/logo|cover`, `shops/{shop}/image` (→ `shops.logo`), `showcase/{item}/image`, `auth/profile/avatar`. `CompanyPublicResource` expõe `nif` **owner/admin-gated**; `ShopResource` passou a expor `logo`. GD do php84 tem WebP read+write ✓.
- **Frontend:** camada `api/backoffice/*` migrada de mock→real para as entidades com imagem. Novo `api/backoffice/_shared.ts` (mappers raw→Bo* + `resolveActive`/`getActiveCompanySlug` + unwrapping: `response()->json(new R)`=unwrapped; `R::collection`/`return new R`=`{data}`-wrapped). Avatar real (`Perfil` → `POST /auth/profile/avatar` + `refreshUser`). **Criar empresa real** (`POST /companies`; a página `Empresa.tsx` ganhou **modo criação** quando a conta não tem empresa — antes só editava e dava erro). Reads company/shops/showcase/PerfilEmpresa/sidebar→`useAuth` agora reais.
- **Provado e2e:** curl (4 uploads→800×800 WebP servidos HTTP 200) + **browser real** (login admin sem empresa → criar empresa → carregar logótipo via crop → persiste `companies.logo=/storage/...webp` + reflecte no `<img>` após reload, 0 erros consola).
- **Parkado (sem endpoint backend = lane Pedro, marcado `TODO`):** créditos/activação de loja, stats de empresa/loja (defaults neutros, não inventados).
- **Gaps backend confirmados:** `PATCH /companies` ignora `is_public`/`montra_enabled` e espera `country_id`/ISO (esses edits não persistem); lista showcase é só-activos (itens inactivos somem da gestão); `nif`≠`tax_number` (colunas distintas; backoffice usa `nif`).
- **Git:** os 80 ficheiros do overhaul+admin redesign foram commitados em `2686461` (já estava no `renato-dev` remoto). Sistema de imagens commitado em `b169b2b` (`dev` local) → **push fast-forward `2686461..b169b2b` para `setuptech/renato-dev`** (só renato-dev; `dev`/`master` intactos). **renato-dev = `b169b2b`.**

## Backoffice migrado mock→real via workflow + smoke (2026-07-01, tarde) — POR COMMITAR (working tree `dev`)
**Pedido:** "cria um workflow p/ analisar e resolver os problemas do backoffice, em loop até tudo funcionar, sem intervenção salvo necessário."
- **Descoberta-chave:** MUITOS endpoints que a memória dava como inexistentes JÁ EXISTEM (Pedro construiu): `companies/{slug}/dashboard`, `.../compliments/inbox`, `affiliate/dashboard`+`conversions`, `reseller/dashboard`+`codes`, `subscriptions/current|checkout|cancel`, `plans`, `credit-packages`, `me/compliments`, `me/shops/qr`, `members/invite`. → a maioria dos 14 clients mock era ligável a real. Gaps reais (sem endpoint): créditos saldo/histórico/comprar, analytics, notifications, `PATCH /auth/email`.
- **Workflow `le-backoffice-real`** (script no scratchpad): análise PARALELA read-only (6 grupos) → correcções SEQUENCIAIS (1 agente/ficheiro, tsc-gated + auto-revert) → verify (tsc+Pest). **3 rondas, 54 agentes, 6M tokens.** Desenho anti-MEGAsync (só escritas sequenciais) → **0 conflict-copies**. Ligou ~22 clients/páginas mock→real (dashboard/compliments/employees/affiliate/reseller/subscription/widgets/credits/orders + páginas). Também corrigiu link partido em Funcionarios (`/colaborador/<slug-vazio>`→`/colaborador/<id>`).
- **Smoke no browser (playwright, como user6 owner de renato-teste) — os agentes NÃO navegam, por isso o loop de código não apanha runtime bugs.** 14/14 páginas funcionais: Dashboard/Elogios/Funcionarios com dados reais; Lojas/Montra/Empresa/Widgets/Encomendas/Inbox/Perfil/Analytics 0 erros; Afiliado/Revendedor com estado honesto "não és participante" (404 = ruído nativo esperado como o 401 do /auth/me).
- **BUG REAL que só o browser apanhou + corrigi:** Créditos presa em "A carregar…" → `GET /subscriptions/current` dava **403**. Causa: `SubscriptionController` chamava `authorize('view'/'update',$company)` mas a `CompanyPolicy` só define **`manage`** (convenção do projecto; `CompanyDashboardController` usa manage). Fix: 3 chamadas → `manage` (linhas 26/54/69). 403→200 confirmado.
- **PlanTest vermelho PRÉ-EXISTENTE (não meu):** `PlanController@index` não exclui Enterprise (teste espera 3 planos, devolve 4). Fora do meu diff; domínio de planos (lane Pedro) + ambíguo (card Enterprise aparece no Créditos). Não corrigido.
- **Build:** tsc exit 0 · AuthTest 10/10 · Phase3Test 14/14. 36 ficheiros modificados, 0 conflict-copies.
- **Dados de teste na sqlite LOCAL** (para o smoke): password do **user 6 (`renato5`/renatorff93+5@gmail.com) posta a `password`**; + colaborador id=1 (user4@empresa5) + 1 elogio (das secções anteriores). Reversíveis. (⚠ empresas 1,2 do admin estão soft-deleted desde 2026-06-30 → admin não tem empresa activa; smoke feito com user6/renato-teste.)
- **Backend tocado por mim nesta sessão (4):** CompanyProfileController, EmployeeProfileController, AuthController, **SubscriptionController** (novo). Nada commitado/empurrado.

## Última sessão (2026-07-09, tarde) — review read-only "Lojas ativas" (tornada obsoleta por dev concorrente) + estado ACTUAL do gate
**Pedido:** review+teste read-only da feature de activação de loja (regra do plano gratuito, créditos, contagem de activas).
- ⚠ **A review foi invalidada em curso: o `ShopActivationService` mudou ENTRE dois `Read()` na MESMA sessão** — desenvolvimento concorrente re-arquitectou o gate. O relatório inicial (baseado em `chargeForFirstActivation` c/ quota simples `is_active<max_shops`) ficou obsoleto. Confirmar SEMPRE re-Read/`git diff` do disco no FIM de uma review (padrão point-in-time; recorrente — ver umbrella 2026-07-02).
- **Estado ACTUAL do gate (disco, POR COMMITAR) = modelo de SLOTS** (supersede o `paid_with_credits` descrito na entrada abaixo — a implementação foi refeita):
  - `companies.purchased_shop_slots` (migration `2026_07_09_120000_add_purchased_shop_slots_to_companies.php`, untracked). `Slots totais = plans.max_shops + purchased_shop_slots` (Enterprise max_shops=null → ∞). Seeder: gratuito 1 / base 3 / pro 15 / enterprise ∞.
  - `Company::hasFreeShopSlot()`/`activeShopsCount()` (linhas ~173-205). `ShopActivationService::chargeForActivation()` (renomeado): há slot livre → grátis; senão **compra 1 slot permanente** (débito `extra_shop_credits`, `reason=shop_slot_purchase`, `increment purchased_shop_slots`). Desactivar liberta o slot (reutilização sem pagar).
  - `ShopController::update` agora chama o gate em **toda** a activação (não só 1ª); `activated_at` set só se null. → **reactivação também precisa de slot livre** (o bypass "reactivação sempre grátis" do modelo antigo está RESOLVIDO).
  - **Testes: `CreditsAndActivationTest` 22/22 verdes** (132 asserts); `tsc` limpo; servers :8000/:5173 → 200. Working tree `renato-dev`: 10 M + 1 migration nova, POR COMMITAR.
- **Achados que sobrevivem (a re-verificar no código novo, não corrigidos):** (P1) onboarding — empresa free nova sem plano/slots (`purchased_shop_slots=0`, sem subscrição) → `totalSlots=0` → 1ª loja exige comprar slot (10 créditos); saldo 0 → 422 dead-end. Registo/`CompanyController@store` não dá slot grátis nem auto-subscreve gratuito. Renato: onboarding deve dar 1 slot grátis / auto-subscrever gratuito? (P2) `Lojas.tsx BASE_SHOP_LIMIT=5` (banner) diverge de base.max_shops=3, e o texto "plano Base" aparece a tier gratuito. (P2) `activationCost` sem guarda p/ plano fora dos 4 tiers → possível ecrã branco. (P2) débito+increment+update não atómicos (sem `DB::transaction`). (P2, fora-scope) full-suite crasha `Premature end of PHP process` em `Phase3Test` (segfault provável PHP 8.5).

## Última sessão (2026-07-09) — bug fix: loja por créditos + quota do plano (aditivo) — POR COMMITAR
**Pedido:** utilizador com 1 loja comprada por créditos + plano Base (3 lojas) só conseguia 3 activas; a 4ª voltava a cobrar. Deviam ser 4 (1 crédito + 3 plano).
- **Causa raiz:** `ShopActivationService::chargeForFirstActivation` (`backend/app/Domains/Company/Services/`) media a quota do plano contra **todas** as lojas activas (`shops()->where('is_active',true)->count() < max_shops`). A loja-crédito ocupava um slot do plano → créditos **substituíam** plano em vez de **somarem**. Base = `max_shops` **3** (confirmado `PlansSeeder.php`; Gratuito 1, Pro 15, Enterprise null/∞).
- **Fix (4 ficheiros, aditivo):** (1) migration `2026_07_09_120000_add_paid_with_credits_to_shops.php` — coluna `paid_with_credits` bool default false + **backfill** das lojas já activadas por débito via `transactions` meta (`reason=shop_activation` → `shop_id`); (2) `Shop.php` cast boolean (FORA de `$fillable` — não mass-assignável); (3) service: contagem da quota exclui `paid_with_credits=true` e, ao cobrar, marca a loja; (4) teste `test_credit_shop_is_additive_to_plan_quota`.
- **Semântica final:** loja paga por créditos nunca consome slot do plano; `max_shops` só é gasto por lojas cobertas pelo plano (grátis/cortesia). Além da quota volta a cobrar (5ª loja no cenário). Reactivações (`activated_at != null`) e cortesias intactas.
- **Testes:** 77 verdes nas 4 suites que tocam Shop (`CreditsAndActivationTest` 18/18, `ResellerRedemptionTest`, `BackofficeGapsTest`, `Phase3Test`, `Mobile\QrResolveTest`). Sem deploy/push (por ordem). Working tree `renato-dev`: 3 M + 1 novo, POR COMMITAR.
- ⚠ **Aprendizagem de domínio:** quota de recurso "grátis" de um plano deve contar só os recursos **cobertos pelo plano**, não os pagos à parte — senão o pago-à-parte come a franquia. Padrão: flag no recurso a marcar a origem do pagamento; a contagem da franquia filtra por essa flag.

## Última sessão (2026-07-09) — Slots de loja (regra de negócio) — COMMITADO + PUSH `renato-dev` (`2a76b54`)
**Pedido Renato (regra canónica confirmada):** activação de loja paga por créditos abre um **SLOT** de loja activa — contentor **transferível**, não ligado a nenhuma loja. Nº lojas activas simultâneas = `plano.max_shops` + `companies.purchased_shop_slots` (comprados). Desactivar liberta o slot; activar outra reutiliza-o sem pagar. Planos abrem N slots; comprados somam-se.
- **Descoberta crítica:** o modelo antigo (`shops.activated_at` = desbloqueio permanente POR LOJA) **contradizia** a regra (não-transferível). E havia um **attempt uncommitted de hoje** (`shops.paid_with_credits` + migração + service/model/test) que só resolvia a aditividade, **não a transferibilidade** — **descartado** (git checkout + rm migração) com autorização do Renato. ⚠ **A pasta MEGA sincronizou POR BAIXO da sessão** a meio (o attempt apareceu entre 2 leituras) — Renato **pausou o MEGAsync** antes das escritas (0 conflict-copies confirmado).
- **Design implementado (slots transferíveis):** migração `2026_07_09_120000_add_purchased_shop_slots_to_companies` (int default 0 + **backfill** dos `shop_activation` históricos → `renato-teste` ficou com 1 slot, verificado). `Company`: `activeShopsCount/shopSlotsForPlan/totalShopSlots/hasFreeShopSlot` (max_shops null=ilimitado). `ShopActivationService::chargeForActivation` (renomeado): slot livre→grátis; senão auto-compra slot (`extra_shop_credits`, `purchased_shop_slots++`, ledger `shop_slot_purchase`); saldo curto→422. `ShopController::update`+`ShopsRelationManager` chamam o novo método; `activated_at` fica só como marca histórica. **Downgrade bloqueado** (decisão Renato: bloquear, não auto-desactivar) no `SubscriptionController` `cancel`+switch (422 `{active_shops, available_slots}`). Dashboard expõe `shop_slots {used,total,plan,purchased,slot_cost}`.
- **Frontend:** `credits.ts` (`CreditOverview.shopSlots`), `dashboard.ts` (raw type), `Lojas.tsx` (`activationCost` por slots, stat "activas / N slots", banner + modal de **compra de slot** transferível, removido `BASE_SHOP_LIMIT`+`hasBeenActivated` gate), `shops.ts` (comentários). `hasBeenActivated` fica como dado honesto (deixou de ser gate).
- **Verificação:** `CreditsAndActivationTest` reescrito p/ slots + testes novos (transferibilidade — cenário exacto do Renato, aditividade, reactivação=reutiliza slot, bloqueio cancel/downgrade, dashboard slots): **22/22 verde**. Suite: 195 + related 60/60 verde (flaky Livewire boot em `PackagingServiceTest` no full-run single-process é ambiental — passa isolado 10/10). tsc frontend limpo. **Doc: `docs/slots-loja.md`** (spec canónica + mapa de implementação).
- **Decisões Renato (3):** sem slot livre → auto-comprar slot + confirmar (não passo separado); downgrade → **bloquear**; `activated_at` → **manter** (histórico). **Commit `2a76b54` + push fast-forward `182c1e5..2a76b54` para `renato-dev`** (ordem do Renato; 13 ficheiros: 11 mod + migração + `docs/slots-loja.md`; `.env`/sqlite verificados fora do stage). ⚠ Backend tocado (lane Pedro) incluído no push por ser branch do Renato; ao sincronizar backend → `php artisan migrate`.

## Última sessão (2026-07-08, tarde) — reconexão git + push renato-dev
**Pedido:** "da update no github no branch renato-dev". Bloqueador: a pasta da plataforma **não tinha `.git`** (é snapshot MEGA do working tree Windows).
- **Descoberta crítica — o MEGA NÃO sincroniza dotfiles.** Consequência: todos os `.gitignore` (×10), `.env.example`, `.editorconfig`, `.gitattributes`, `.htaccess`, `.npmrc` aparecem como **deletados** no diff vs remoto. Um `git add -A` cego (a) **apagaria 27 dotfiles do repo** e (b) como os `.gitignore` sumiam, **commitaria `.env` (secrets) + `database.sqlite` (BD local)**. ⚠ Nunca commitar snapshot MEGA sem restaurar os dotfiles primeiro.
- **Técnica de reconexão (validada):** `git clone --branch renato-dev --single-branch` para o scratchpad → **enxertar o `.git`** na pasta (`cp -R clone/.git plat/.git`) → `git checkout -- <só dotfiles deletados>` (basename começa por `.`) para repor os `.gitignore` → `.env`/sqlite voltam a ser ignorados. Diff passou de 347 sujo para 216M+3D+29?? limpo. Antes de mover, inspeccionei o diff **sem tocar na pasta** via `git --git-dir=clone/.git --work-tree=plat status`.
- **Estado remoto real:** `renato-dev` estava em **`88dda1e`** ("mock eliminado / ligação completa ao backend real"), **não `b169b2b`** como a memória dizia (memória estava desactualizada nesse SHA).
- **Diff = todo o working tree acumulado desde `88dda1e`** (não só o de hoje): afiliados/revendedores (migrações+services+tests 07-06/07) + perfil público + laranja (hoje). Não é ruído de line-endings (confirmado: `CouponService` +4/−24, `Order.php` +5/−1 = mudanças reais).
- **Commit `182c1e5`** (filho de `88dda1e`) → `git push origin HEAD:renato-dev` **fast-forward, sem force, sem perda de histórico**. 251 ficheiros; `.env`/sqlite/`.DS_Store`/`bootstrap/cache` verificados fora do stage. 3 deleções reais (`ProductLowStockNotification.php`, `ProtectedRoute.tsx`, `Dashboard.tsx`). Inclui 42 ficheiros backend (lane Pedro) — Renato autorizou por ser o branch dele. Verificado no GitHub: tip = `182c1e5`.

## Última sessão (2026-07-08) — 1º arranque no Mac + laranja dos botões + Perfil público ao nível da PESSOA (opt-out)
**Contexto:** primeira sessão da plataforma em macOS (migração de Windows). `/resume` da umbrella resolveu por nome (directorio Windows desactualizado — corrigido nesta sessão).
- **Arranque (bloqueadores da migração MEGA):** (1) faltava `.env` (o MEGA não sincroniza dotfiles) → criado. ⚠ **`bootstrap/app.php:52` faz `useEnvironmentPath(dirname(__DIR__,2))` → o `.env` vive na RAIZ da plataforma (`2026_Nova_Plataforma/.env`), NÃO em `backend/`.** sqlite + `APP_KEY` gerada (`key:generate` procura na raiz; gerei manual). (2) `frontend/node_modules` veio do Windows → binários nativos (vite/esbuild) partidos → `npm install` refez p/ Mac. `database.sqlite` tem dados (veio na sync). Servidores: `php artisan serve :8000` (admin `/admin`, `admin@livrodeelogios.com`/`password`) + Vite `:5173`.
- **Laranja dos botões (regra de marca):** os CTAs usavam um token separado **`--color-primary-cta: #D14100`** (+ hover `#BF3B00`) — laranja **escurecido de propósito p/ texto branco passar WCAG AA 4.5:1** (comentado no `index.css`). Renato: só existe **um** laranja. Alinhados `--color-primary-cta`→`#FD5000`, hover→`#E54300`; + 1 botão em `App.tsx` com hover `#e54800`→`#E54300`. ⚠ Trade-off consciente: branco sobre `#FD5000` fica ~3.4:1 (<AA) — marca > compliance (regra do Renato); mitigar com peso/tamanho do texto, não escurecendo.
- **Perfil público ao nível da PESSOA (feature nova, cross-stack):** antes a "página pública" era só o registo de **colaborador** (por empresa). Renato quis página **da pessoa, sempre acessível mesmo sem empresa** (a pessoa não recebe elogios directamente, só via empresas), **com toggle on/off**.
  - **Backend:** migração `users.slug` (unique + backfill nome-based + `User::booted` auto-gera único no `creating` + helper `uniqueSlug`); migração `users.public_profile_enabled` (boolean **default true** = opt-out). `AuthController@updateProfile` aceita a flag; `slug`+flag auto-serializam no `/auth/me`. Novo **`PublicProfileController`** (`show`/`compliments`): resolve **user-first** (agrega elogios de TODOS os empregos activos+públicos: `whereIn employee_id … status published/approved … visible_on_employee_profile`), **fallback ao `EmployeeProfileController`** por slug/id (mantém o preview do admin e links legacy), **404 quando o user desligou a página**. Rotas públicas `GET colaborador/{slug}`(+`/compliments`) no prefixo `/api/v1`.
  - **Frontend:** `PerfilColaborador.tsx` repointado p/ `/colaborador/{slug}` (company agora nullable — página da pessoa não tem empresa única; type `EmployeePublic.company` `|null` + `is_person?`). `Perfil.tsx`: link vem do `user.slug` **sempre** (não do `public_profile` do employee); **`Switch` "Página pública activa"** com save instantâneo (`PATCH /auth/profile`, optimista); copy nova (página sempre tua; elogios via empresas). `types/api.ts`: `User.slug` + `User.public_profile_enabled`.
  - **Verificado e2e (curl `/api/v1` + tinker):** pessoa agrega (is_person, company null, total✓), fallback employee (company="Renato Teste"), user sem empresa existe c/ 0, slug inexistente 404, paginação ok, toggle OFF→404 (página+compliments) / ON→200, flag no payload. `php -l` + `tsc` limpos.
  - ⚠ **Privacidade:** default ON → qualquer user registado tem página pública (nome guessável pelo slug), mas só expõe elogios já públicos. Flag por-pessoa é independente do `is_public` por-empresa (Funcionários).
  - ⚠ **2 migrações correram na sqlite LOCAL** (`add_slug_to_users`, `add_public_profile_enabled_to_users`) — backend é lane do **Pedro**: ao sincronizar precisa de `php artisan migrate`. **Nada commitado (a plataforma nem é git neste Mac).** Após adicionar rotas → `php artisan route:clear` (estavam cacheadas → 404).
  - **Backend tocado (3):** `User.php`, `AuthController.php`, **`PublicProfileController.php`** (novo) + 2 migrações. **Frontend (4):** `PerfilColaborador.tsx`, `Perfil.tsx`, `api/employees.ts` (`getPublicProfile`), `types/api.ts`.
  - Nota: SEV-001 da review 07-02 (botão "Ver mais elogios" removido) — **está presente** em `PerfilColaborador.tsx` (memória desactualizada nesse ponto).

## Última sessão (2026-07-01) — Perfil público de pessoa ligado a real — POR COMMITAR (working tree `dev`)
**Pedido:** "o perfil público ainda não está a funcionar?" → estava um **stub fabricado**. Diagnóstico: link no backoffice (`Perfil.tsx`) construía `https://livrodeelogios.pt/perfil/<nome>` (domínio prod hardcoded + path `/perfil` inexistente + slug do nome); a rota real é `/colaborador/:slug`; `PerfilColaborador` lia o **mock**; `/auth/me` não expunha o id/slug de colaborador do user. Backend TINHA perfil público (`GET /api/v1/employees/{id}`, público) mas só por id. Renato deu luz verde a **backend+frontend (opção C)**.
- **Backend:** `EmployeeProfileController@show/@compliments` resolvem por **slug OU id** (`ctype_digit`→id senão slug; helpers `publicQuery()`/`identifierColumn()`). `AuthController` — `login`/`register`/`me`/`updateProfile`/`uploadAvatar` devolvem `public_profile:{id,slug}|null` via helper `userPayload()` (relação **`employments()`** — NÃO `employees()`, apanhado a meio: partiria login com BadMethodCallException). AuthTest **10/10** + Phase3Test **14/14** verdes; PHP lint ok.
- **Frontend:** `types/api.ts` `User.public_profile`; `api/employees.ts` aceita slug/id + **corrigido unwrap** (endpoint devolve o resource SEM envelope `data` — `response()->json(new R)`; fazia `data.data`→partia); `PerfilColaborador.tsx` **reescrito** p/ endpoint real (React Query por slug, mock removido, loading/404/empty honestos, pesquisa client-side); `Perfil.tsx` link → same-origin `${origin}/colaborador/<slug-real>` do `/auth/me`, e **estado honesto** quando `public_profile` é null (sem link morto). tsc verde.
- **Provado e2e (playwright):** `/colaborador/renato-fernandes-ferreira-1` renderiza dados reais; backoffice admin (sem colaborador) mostra o empty-state honesto; API do user 4 → `{id:1,slug:renato-fernandes-ferreira-1}`.
- **Dados de teste na sqlite LOCAL** (para provar o happy-path, reversíveis): colaborador `id=1` (user 4 "Renato Fernandes Ferreira" @ empresa 5 "Renato Teste", public+active, slug `renato-fernandes-ferreira-1`) + 1 elogio publicado (`Cliente Satisfeito`). A decidir: manter demo ou remover.
- ⚠ **2 ficheiros mudaram FORA das minhas edições** durante a sessão (estavam limpos no arranque — git status inicial só tinha 5 ficheiros frontend): `backend/bootstrap/app.php` (redirectGuestsTo API→null/Filament + `shouldRenderJsonWhen` api/*) + `backend/tests/Feature/AuthTest.php` (teste de regressão 401-JSON). Fix legítimo mas de origem desconhecida (sessão paralela?) — **não commitar sem confirmar com o Renato**.
- **Fora do âmbito (por tratar):** `Perfil.tsx` ainda usa `PROFILE_BASE_URL` (prod hardcoded + rota `/perfil/elogio/{id}` inexistente) para partilha de **elogios individuais** — mesma classe de bug, outra feature.
- **Working tree `dev`:** 8 ficheiros modificados por mim (backend `EmployeeProfileController`/`AuthController` + frontend `types/api.ts`/`api/employees.ts`/`PerfilColaborador.tsx`/`Perfil.tsx` + os 5 do follow-up UX prévio: `CompanyProfileController`/`company.ts`/`AppSidebar`/`AppTopbar`/`Lojas`) + os 2 de origem desconhecida. **Nada commitado nem empurrado** (regra de git). renato-dev continua em `b169b2b`.

## Última sessão (2026-06-30) — force push para renato-dev
Renato ordenou force push do estado local para `setuptech/renato-dev`. Working tree (71 mod + 9 untracked = overhaul backoffice + admin dark redesign) commitado em `2686461`, force-pushed sobre `renato-dev` (descartou 2 commits próprios do remoto). Detalhe completo na secção "REGRAS DE GIT" (entrada 2026-06-30). `dev`/`master` intactos. **renato-dev = `2686461`.** Trabalho local agora ESTÁ commitado em `dev` local (já não há working tree uncommitted).

## Sessão anterior (2026-06-29/30)
Overhaul do backoffice (30 itens do Renato) via workflow `le-backoffice-overhaul` (11 agentes), camada mock+frontend, build verde — ver secção "Overhaul backoffice (30 itens)" no topo. **Follow-ups pós-overhaul (mesma sessão):** (a) PerfilPessoal: atalho "Segurança" deixou de dizer "dois passos"; (b) **conta sem empresa** já NÃO mostra o switcher "A minha empresa" na sidebar — mostra botão "Criar empresa" (`AppSidebar` gated por `accountHasCompany`); (c) cards "O meu perfil" / "Criar a sua empresa" no PerfilPessoal igualados em altura (removido `items-start`, card em `flex flex-col` + botão `mt-auto`). Entregue também o **workflow de QA manual** por áreas para o Renato. Tudo validado em runtime (playwright). NÃO commitado (working tree `dev` local, regra do Renato).
**Pendente:** os 8× `TODO(backend)` + Q1/Q2 (limites/pricing) + decisão mock↔backend (ver secção overhaul). Dev server a correr em :5173 (reiniciado após deps novas).

## Última sessão anterior (2026-06-28)
Backoffice ligado ao backend real (PR #16): uploads+auth+tour, 12 clients wired, headline imagem→perfil provado e2e, Ciclo 1-3 + diretório a dados reais. (PR #16 foi DEPOIS fechado + branch apagada a pedido do Renato — ver regras de git no topo.)

## Pendentes Renato (2026-06-28)
1. **Rever + merge PR #16** (`feat/backoffice-integration` → `dev`). Toca backend (lane do Pedro) → revisão antes de merge.
2. **Gaps de backend** (`docs/backend-gaps-backoffice-integration.md`, P1/P2/P3) — implementar p/ levar backoffice a 100% funcional. P1: créditos por loja, listagem-admin da montra, elogios rejeitados, buy-credits, inbox reply.
3. **Glância visual** aos uploads de imagem de **loja** (Lojas) e **montra** (Montra) — código tsc-verde, padrão `uploadImage` provado no logótipo, mas não cliquei o flow no browser.
4. **`dev` local tem `d90e373` (Ciclo 1-3 + diretório) ainda NÃO empurrado** para `setuptech/dev` — vai no PR #16; OU empurrar `dev` à parte se quiser separar.
5. **Admin redesign (42 ficheiros backend Filament) AINDA por commitar** no `dev` (theme.css+widgets+provider+login+api-docs blade + `public/images/`). Separado da integração; commitar quando validar.
6. **Auth público + checkout** (loja) — cart só localStorage, `Carrinho.tsx` sem submissão real, checkout 3-step MB/MBWay por ligar. (O backoffice JÁ usa auth real; isto é o lado loja/checkout.)
7. **Decisões de produto bloqueadas** — ver secção abaixo.

## Gotchas validados nesta integração (2026-06-28)
- **Uploads de imagem servem em `/storage/...` (relativo) — o `vite.config` JÁ tem proxy `/storage`** → `<img src="/storage/...">` no :5173 renderiza (forward p/ :8000). Não prefixar origem; em produção precisa same-origin/CDN.
- **`shops` não tem coluna `image`** → imagem de loja persiste em `shops.logo` (ShopResource passou a expor `logo`; `BoShop.logo` adicionado no frontend).
- **PerfilEmpresa (`/empresa/:slug`) é o "perfil da loja"** que o user quer; hero usava placeholder `picsum` → agora `cover_image ?? logo`.
- **Mock store `backofficeStore.ts` era in-memory** (refresh perdia edições); ao ligar à API real, alterar `BoShop`/types obriga a actualizar os seeds do mock (senão `tsc` parte com "Property missing").

## Filament v5 — gotchas validados (ver CLAUDE.md secção completa)
- Namespaces: `Section`/`Fieldset`/`Schema` → `Filament\Schemas\*` (NÃO `Filament\Forms\*`). `EditAction`/`DeleteAction` → `Filament\Actions\*` (NÃO `Filament\Tables\Actions\*`). Form/Table components mantêm namespaces antigos.
- Table API v5: `->recordActions()` / `->toolbarActions()` (não `->actions()`/`->bulkActions()`).
- Multi-tenant scoping (country_admin): `Select::make('country_id')` precisa SEMPRE de `->default(auth country)` + `->disabled(isCountryAdmin)` + `->dehydrated()` — só `getEloquentQuery` scope NÃO previne IDOR no POST.
- Multi-dashboard Pages: cada um precisa de `$slug` E `getRoutePath()` senão colidem em `/admin`.
- Notification em transaction: envolver em `DB::afterCommit()`.
- `ExternalPartner` usa `protected $table = 'marketplaces'` (rename só conceptual; FKs `marketplace_id`).
- **TableWidget heading:** `getHeading()` é **IGNORADO** num `TableWidget` v5 → o heading visível auto-deriva do nome da classe (em INGLÊS, ex.: `LowStockWidget`→"Low Stock"). Definir `->heading('...')` no `Table` dentro de `table()`.
- **`DATE_FORMAT` é MySQL-only** — parte em SQLite (dev local). Usar `config('database.default')==='sqlite' ? "strftime('%Y-%m', col)" : "DATE_FORMAT(col,'%Y-%m')"` (padrão já no codebase). `DATE()` é portável.
- **StatsOverviewWidget:** `->color()` pinta a sparkline/ícone; p/ 1-cor-laranja pôr tudo `'primary'`. Botão primário do login renderiza `fi-bg-color-400` pálido → forçar `var(--st-accent)` no `.le-login-card .fi-btn`.

## Config
- **`.env` ÚNICO na raiz** (não em backend/ nem frontend/). Laravel via `useEnvironmentPath()`; Vite via `envDir`. `VITE_*` = frontend. `cp .env.example .env` na raiz.
- Pricing 3 tiers independentes: mensal s/fidelização (`price_monthly`), anual mensal (`price_annual_monthly`), anual upfront (`price_upfront_monthly`+`price_annual_annual`).
- Domínios: Commerce, Company, Compliments, Content, EmailAutomation, Gamification, Integrations, Marketplaces, Monetization, Platform, Shipping.

## Sentry
- Org `setuptech-07`. Projectos: `le-backend` (id 4511461122048080) · `le-frontend` (id 4511461122179152). Token em `~/.sentryclirc`.
- Bug `sentry-cli organizations list` falha (`missing field 'requireEmailVerification'`) → usar API directa via curl. Events: `/api/0/organizations/{slug}/issues/{id}/events/latest/`.

## Decisões de produto BLOQUEADAS (não implementar sem decisão do Renato)
Limites concretos por tier · pricing final (subscrição/add-ons/créditos/afiliados/revendedores) · resposta a elogios (se avança) · mensagens diretas utilizador→empresa · widgets/API (formatos/webhooks/rate limiting por tier) · rankings de pessoas (elegibilidade/opt-in/out privacidade).

## Gaps de migração conhecidos (multi-país)
Faltam `country_id` em `products`, `articles` (+ slug UNIQUE por país, não global), `article_categories`, `shipping_costs`.

## Marketing — Campanha de Julho 2026 (2026-07-01)
Plano de campanha do **"Pacote livro + suporte"** entregue em `JOCA_Brain/memory/projects/livro-elogios-campanha-julho-2026.html` (HTML self-contained, PT-PT, paleta navy `#1a3a5c`+dourado `#c9a227` — paleta **do documento**, NÃO da marca). Cobre 1–31 Jul: estratégia, 5 pilares (dor/benefício/prova/suporte/oferta), momentos PT reais (época alta turismo+restauração, saldos de verão — sem feriados inventados), calendário editorial 4 semanas (~30 touchpoints) IG/FB/LinkedIn/Email, copy REAL para 20 peças sociais + 8 emails, KPIs.
- ⚠ **Ambiguidade de produto (2 definições em conflito):** a decisão activa do Brain trata "Pacote livro+suporte+cartaz" como **produtos FÍSICOS** da loja (Livro 24,90 / Suporte 9,90 / Cartaz 1,00 €, +IVA). O brief desta campanha redefiniu "Pacote livro + suporte" como **subscrição** (acesso à plataforma + suporte dedicado). São coisas diferentes — **confirmar com o Renato qual é o produto real** antes de publicar.
- ⚠ **Preço/oferta por fixar:** o pacote de subscrição não tem preço público; a copy usa "condição de lançamento de Julho" (sem número inventado). Substituir por oferta concreta em EM-06/07/08, IG-08, IG-10 antes de publicar.
- Única estatística usada = "27.000+ empresas" (real). Sem testemunhos fictícios (marcados como "inserir real com autorização").

## Review "Corrigir e melhorias LE" (2026-07-02, read-only — nada corrigido)
Review+teste do working-tree diff sobre `b169b2b` (backoffice mock→real + fixes). tester-code + tester-ui-ux + smoke live (Playwright, :8000/:5173). **PHP:** `C:\Users\renat\php84\php.exe`.
- **Verde:** 220/221 testes (1 vermelho = `PlanTest` **pré-existente**, lane Pedro — não tocado); `tsc --noEmit` limpo. Verificado live: 401-JSON sem header Accept (fix `bootstrap/app.php` do 500 "Route [login]"); `SubscriptionController authorize('manage')` (fix real do 403 Créditos — `CompanyPolicy` só tem `manage`); `AuthController.userPayload` expõe `public_profile{id,slug}` sem vazar password/2FA (model `$hidden` cobre); `EmployeeProfileController` resolve por id **e** slug (`renato-fernandes-ferreira-1` → 200; slug inválido → 404), gating `is_public`+`is_active` preservado. Frontend: estados loading/empty/error consistentes, anti-fabricação real (`TODO(backend)`), 0 erros de consola em Dashboard/Créditos/Perfil/Colaborador.
- **A corrigir (0 Critical):** **[High] SEV-001 regressão** — `PerfilColaborador.tsx` removeu o botão "Ver mais elogios recebidos" → elogios além do 1º batch ficam inacessíveis (página de social-proof). **[High]** grid/hero do colaborador com inline styles (sem breakpoint mobile, 2-col a 375px). **[Med]** botão "Partilhar" por elogio sem `onClick` (dead). **[Med]** PT-PT "Ativado/Ativados" (AO90) em `Revendedor.tsx` vs "Activo" no resto. **[Low]** radius 8px na tab (só 12px no sistema), placeholder `#bbb` vs `#ABABAB`, tab única com ARIA tabs completo, glifo `♥` como ícone.
- ⚠ **Seed `maria.silva@teste.pt`/`password` já não autentica** (422 "Credenciais incorrectas") — usar `admin@livrodeelogios.com`/`password` (admin sem empresa → testa estados vazios).
- ⚠ **Review foi point-in-time:** snapshot de 36 ficheiros revisto, mas o working tree cresceu para 47 durante a sessão (worker concorrente continuou "Corrigir e melhorias LE": +`CompanyMemberController`, `EmployeeController`, `routes/api.php`, `Carrinho/Contactos/PerfilEmpresa.tsx`, `PRD.md`). Novos ficheiros untracked legítimos: `frontend/src/lib/phone.ts`, `postalCode.ts`.

## Sessão 2026-07-02 (tarde) — 16 pontos do Renato corrigidos/testados — POR COMMITAR
Relatório completo em `docs/sessao-2026-07-02-16-pontos.md` (ficheiros tocados, causa-raiz de cada bug, como foi testado). Resumo:
- **Feito e testado e2e** (curl/tinker/Pest/Playwright real, não assumido): #1 dados pessoais não gravam (cache `qk.session` vs `AuthContext` dessincronizadas) · #2 telefone em blocos de 3 (`lib/phone.ts`) · #3 código postal PT auto-formato (`lib/postalCode.ts`) + tarefa futura (auto-morada) registada em `PRD.md` §11 · #6 loja nasce inactiva (já estava correcto, só verificado) · #7 admin auto-associar-se a loja (endpoint novo `employees/self`) · #8 convite de funcionário desaparecia (faltava endpoint `GET members/invites` — não existia nenhuma forma de listar convites pendentes) · #9 logo partido em `/empresa/*` (`logo-branco.svg` não existe, é `logo-white.svg`) · #10 elogio público não chegava ao backoffice (**o form "Introduzir Elogio" em `PerfilEmpresa.tsx` era um stub fake** — nunca chamava a API, só mostrava "sucesso") · #11 "Configurações da empresa" também no menu lateral · #12 widget com "código inútil" (**`embed.js` não existia em lado nenhum do repo** — criado de raiz; cores/formato não persistiam — migration nova `company_integrations.{format,theme,lang,primary_color}`; CORS dedicado via `HandleCors::skipWhen` p/ rota pública) · #13 link hardcoded p/ domínio de produção inexistente (Widgets.tsx E Partilha.tsx — mesmo bug em 2 sítios, `window.location.origin` agora) · #14 logo real da empresa em vez de iniciais (canvas+preview) + assinatura de email redesenhada como botão real (deixou de depender de imagem de selo que nunca existiu).
- **Não feito** (falta tempo): #4/#5 wizard passo-a-passo de criação de empresa + check "loja=empresa" · #15 comprar Páginas Digitais/planos sem gateway (créditos reais — `User` já tem `Bavix\Wallet` instalado, por explorar) · #16 recriar produtos da loja.
- **Ambiguidade de produto por confirmar** (liga ao Q1 do PRD): "Gratuito" inclui hoje 1ª loja activável de graça (`PLAN_LIMITS.gratuito.maxShops=1`, não alterado); o pedido do Renato ("para activar é preciso gastar créditos OU ter assinatura") pode significar zero activações grátis, incl. a 1ª — não decidido sozinho.
- ⚠ **Incidente MEGAsync — repetiu-se a meio desta sessão:** `frontend/src/api/backoffice/widgets.ts` foi renomeado para `widgets(1).ts` pelo MEGAsync enquanto estava a ser editado (apagou o canónico) — mesmo bug já documentado (2026-06-29). Detectado pelo erro de build do Vite, restaurado de imediato. Confirma que o MEGAsync ainda intercepta ficheiros em edição activa mesmo em sessões *single-agent* (não só workflows paralelos) — considerar pausar o MEGAsync durante sessões longas de edição neste projecto.
- **Dados de teste criados** (BD local, reversíveis): funcionário auto-associado `renato5`→loja 7 (id 2), 2 elogios de teste (ids 2 e 3), 1 widget "Site principal" (tema escuro, `#1C64F2`).
- Servidores reiniciados a meio (Vite, cache/HMR presa após o incidente MEGAsync). `tsc` limpo, **220/221 Pest verdes** (falha = `PlanTest` pré-existente).

## Sessão 2026-07-02 (FASE 2, workflow `le-16-pontos-fase2`) — pontos 4/5/15/16 + gate do 6 FEITOS — POR COMMITAR
**Os 16 pontos do Renato estão TODOS implementados e provados no browser.** Plano em `docs/plano-2026-07-02-fase2-backoffice-funcional.md`; detalhe na secção "FASE 2" de `docs/sessao-2026-07-02-16-pontos.md`. Workflow 5 agentes (backend → frontend-créditos → wizard → produtos → verify, escritas sequenciais anti-MEGAsync, **0 conflict-copies**) + smoke browser no main loop.
- **#15 créditos/planos sem gateway:** `POST /credit-packages/{id}/purchase` (deposit Bavix directo; padrão sandbox Ifthenpay) · `GET /companies/{slug}/credits/transactions` (`CompanyCreditController` novo) · checkout de subscrição com **activação directa** quando plano free/sem Stripe (cria Subscription active, deposita `credits_per_month`, grava `companies.plan_id`; cancel limpa-o). ⚠ enum `billing_cycle` da BD = `monthly|annual_monthly|annual_annual` ('annual' da API → `annual_annual`). Frontend `credits.ts`/`subscription.ts`/`Creditos.tsx` reais. **Provado:** compra 500 → saldo+histórico; Base anual activo, "Renova a 2 de Julho de 2027".
- **#6 gate real:** `shops.activated_at` (migration); 1ª activação: sub c/ quota → grátis, senão cobra `extra_shop_credits` (10) da wallet, 422 c/ `{required,balance}` sem saldo; PATCH devolve `charged`; `Lojas.tsx` modal c/ custo/saldo reais e botão desactivado sem saldo. **Provado:** cobrou 10 (500→490); saldo 0 bloqueia.
- **#4/#5 wizard:** modo criação de `Empresa.tsx` = wizard 4 passos (`components/app/wizard/`): dados→empresa · imagens (opcional) · **1ª loja c/ check "mesma informação?"** (explicação empresa vs loja; não → mini-form) criada inactiva · resumo+CTAs. Provado e2e (ambos os caminhos do check).
- **#16:** `ProductSeeder` corrido (BD tinha 0) + registado no `DatabaseSeeder` → `/loja` com os 6 produtos reais, imagens ok.
- **Bugs apanhados SÓ no smoke (agentes não navegam):** (a) **`CompanyObserver` auto-criava loja default ACTIVA e grátis** ao criar empresa (era a origem da queixa do #5, violava o #6, e partia o wizard c/ 500 UNIQUE `shops.slug`) → observer des-registado do `AppServiceProvider` (classe+backfill command ficam), wizard = criador canónico da 1ª loja, `ShopController` c/ `uniqueSlug()` e `is_active=false` forçado no store (paywall não contornável); testes ajustados (`Phase3Test` 4→3, `QrResolveTest` cria loja explícita). (b) **CORS do widget (#12 da Fase 1) partia o perfil público da app**: `AllowPublicWidgetCors` forçava `*` mas o axios usa `withCredentials` → `/empresa/{slug}` sem elogios; fix: ecoa origens da allowlist c/ credentials, `*` só p/ sites terceiros (validado curl+browser). (c) `formatLongDate` mostrava ISO cru (`ends_at` completo). (d) switcher sempre "Sem plano": `CompanyController@index` sem eager-load `plan` + nada gravava `companies.plan_id` (nem o caminho Stripe — esse gap pré-existente mantém-se no webhook).
- **Estado final:** tsc limpo · **Pest 233/234** (só `PlanTest` pré-existente) · 13 testes novos `CreditsAndActivationTest` · nada commitado (regra git).
- **Dados de teste novos:** renato-teste com 500 créditos comprados, loja activada (-10), **plano Base anual activo** (`plan_id=2` backfilled); empresa `estudio-wizard-teste` do admin (1 loja inactiva) criada pelo teste do wizard.
- **Gaps TODO(backend):** gateway real p/ compra de créditos/planos · job mensal recorrente de `credits_per_month` (só o 1º ciclo é depositado) · referência/fatura nas transacções · "usados este mês"=0 (sem fonte) · bug latente `StripeService.php:131` grava `billing_cycle='annual'` (viola o CHECK enum) quando o Stripe real for ligado.

## Sessão 2026-07-02 (FASE 3, workflow `le-fase3-resto`) — gaps + achados da review FEITOS — POR COMMITAR
Renato pediu "tirando o gateway real, implementa e corrige o resto". Feito e provado (browser + comando ao vivo): **tsc limpo · Pest 237/238** (só PlanTest pré-existente). Detalhe na secção "FASE 3" de `docs/sessao-2026-07-02-16-pontos.md`.
- **Job mensal de créditos:** `subscriptions:credit-monthly` (agendado daily em `routes/console.php`) — idempotente por (subscription, mês civil; activação do mês conta), cap `max_credits_accumulated`. Provado ao vivo (renato-teste saltada, saldo intacto).
- **Webhook Stripe corrigido:** `year`→`annual_annual` (enum) + `companies.plan_id` gravado/limpo no sync/cancel.
- **Transacções com `reference`** (uuid Bavix real) + **`credits_used_this_month`** no dashboard → Créditos mostra "Usados este mês" real e uuid por linha.
- **Achados da review resolvidos:** SEV-001 "Ver mais elogios" restaurado (load-more paginado real, dedupe por id) · grid/hero do colaborador responsivo (provado a 375px) · "Partilhar" copia link c/ feedback · `♥`→lucide Heart · placeholder #ABABAB · Revendedor AO90→pré-AO90. ⚠ o [Low] "radius 8px na tab" da review NÃO é reproduzível no código actual — reportado, nada mudado.
- **Fica (por decisão/dependência):** gateway real (excluído pelo Renato) · nº de fatura (Moloni) · depósito do 1º ciclo no caminho webhook Stripe (decisão de produto) · `::placeholder #bbb` global no `index.css` (corrigido só no PerfilColaborador).

## Ligação Admin ↔ Backoffice — ANALISADA, plano AGUARDA GO (2026-07-02, fim)
Renato quer no admin: ver/editar tudo do cliente (info+lojas), pré-visualizar, "entrar" na conta, criar/apagar. Scout de 2 agentes → **plano em `docs/plano-2026-07-02-admin-backoffice-ligacao.md`**.
- **Descoberta-chave: a ponte técnica JÁ existe** — admin Filament e SPA partilham guard `web` + sessão (`SESSION_DOMAIN=localhost` ignora porta; `sanctum.guard=['web']`; `AdminPanelProvider->authGuard('web')`). **Impersonate custom JÁ existe** no `UserResource` (`impersonating_from` na sessão + `Auth::loginUsingId` + rotas `stop-impersonating` + banner Filament). Policies dão bypass total ao role `admin`.
- **4 gotchas do impersonate:** redirect usa `config('app.frontend_url')` que NÃO existe (a real é `le.frontend_url`, default :8000 → cai na welcome page) · SPA só sonda `/auth/me` com hint `localStorage['le-auth']` · **logout da SPA invalida a sessão toda incl. `impersonating_from`** (perde-se o regresso) · SPA sem banner/estado de impersonation (`/auth/me` não expõe flag).
- **Cobertura do admin (45 resources):** Company parcial (sem logo/cover/is_public/montra/plan_id, ZERO RelationManagers, saldo wallet invisível) · **Shop/Employee/Montra/Wallet AUSENTES** (sem creditar/debitar manual — lacuna crítica) · Compliment tem moderação Publicar/Rejeitar ✓ · Subscription só lista+"Alterar plano" · Plan/CreditPackage completos ✓.
- **Plano (5 frentes):** F1 CompanyResource=hub (form completo + 7 RelationManagers + saldo) · F2 lojas CRUD · F3 créditos (saldo/histórico/action ajustar c/ motivo + criar/cancelar sub) · F4 fechar impersonate (fix redirect + flag no /auth/me + banner SPA + esconder logout) · F5 previews (/empresa/{slug}, /colaborador/{slug}).
- **Decisão pendente do Renato:** admin activar loja cobra créditos ao cliente ou é grátis (plano assume grátis/cortesia).

## Última sessão (2026-07-07, cont.4) — workflow analisar-plataforma + análise total — POR COMMITAR
**Pedido:** "cria um workflow para analizar toda a plataforma" → criado `.claude/workflows/analisar-plataforma.js` (recon → 8 lentes read-only paralelas → verificação adversarial de Critical/High → síntese em docs/). Corrido contra o LE (2 resumes por rate-limit; consolidei o relatório à mão do `journal.jsonl` quando a síntese automática não chegou a correr). **Relatório: `docs/analise-plataforma-2026-07-07.md`.**
- **Verificação adversarial: 0 refutados de todos os Critical/High.** 3 rebaixados High→Medium (deploy/email/queue/RGPD = gaps de produção já sequenciados na Fase 3).
- **5 CRITICAL (3 NOVOS, não estavam no roadmap):** C1 `ComplimentShareController` IDOR — qualquer user debita a wallet de qualquer empresa (`credits_used` vem do cliente, sem authorize); C2 webhook Ifthenpay FORJÁVEL com keys vazias (md5 anti-phishing reduz-se a `md5(id_ped+valor)`, computável); C3 webhook Stripe quebra assinatura sempre (`json_encode($request->all())` em vez de `getContent()` raw); C4 Moloni stub (HTTP comentado — facturação legal); C5 IVA GATE (total mostrado≠cobrado).
- **8 HIGH:** H1 `orders:cancel-expired` filtra `payment_method` por valores que ninguém grava (`ifthenpay_mb` vs `multibanco`) → orders nunca expiram, stock/cupões presos; H2 refund ignora `discount_amount` (reembolsa >que pago); H3 form de elogio bypassa Termos+loja+erros 422; H4 Drawer do carrinho sem focus-trap e focável fechado (activa "Remover" invisível); H5 "Adicionar empresa" dead button; H6 disputas de claim invisíveis ao admin; H7 reset de 2FA impossível (lockout); H8 QR de loja → 404.
- **~22 Medium** (CouponService não-atómico, approveByCompany double-débito, cancel-subscrição-directa no-op, checkout sem idempotência, a11y do checkout/sidebar, login social perde redirect, etc.) **+ ~20 Low** (hero 3.7MB=LCP, Scout collection driver, tokens sem expiração, etc.).
- **Sólido confirmado:** núcleo transaccional (locks/atomicidade/idempotência), N+1 OK, admin 46 resources amplo, Modal focus-trap, AuthGate.
- **FALTA correr:** lentes `dead-code` + 2ª passagem `security` (retomável: resume runId `wf_a2c7ddca-465`, agentes concluídos vêm de cache).
- **Aprendizagens JOCA registadas:** workflow custom só por `scriptPath` (não `name`); `args` pode chegar como string JSON (normalizar no topo); journal.jsonl permite consolidar à mão quando a síntese não corre.

## Sessão (2026-07-07, cont.3) — auditoria mobile viva (Playwright 375px+320px) — POR COMMITAR
**Pedido:** "verifica e melhora o Mobile em todas as secções". Sweep automático de overflow horizontal (scrollWidth vs clientWidth, excluindo scrollers intencionais) em TODAS as rotas: 15 públicas + 2 dinâmicas (empresa/produto) + 14 backoffice, a 375px E 320px, com login real (admin@livrodeelogios.com/password) + fluxo de checkout completo (3 passos) + interacções (drawer, modais, toggle filtros). **Veredicto: mobile globalmente sólido — 4 bugs encontrados e corrigidos** (tsc+build verdes; verificados no browser após fix):
1. `/app/funcionarios` 139px overflow — barra de acções do header (2 botões, 475px) sem `flex-wrap` → adicionado.
2. `/app/empresa` 58px overflow — save bar (Cancelar+Guardar ~360px) → empilhados full-width em mobile (`flex-col-reverse sm:flex-row` + `w-full sm:w-auto`).
3. Carrinho a 320px 32px overflow — input do cupão `flex-1` sem `min-w-0` (min-width intrínseco do placeholder empurrava o "Aplicar" para fora) → `min-w-0`.
4. **Drawer mobile da sidebar não fechava com Escape** (só backdrop/rota) → handler keydown no AppShell (descoberto porque o drawer ficou a interceptar cliques no teste).
+ **Bug de auth apanhado de borla:** um burst de navegação (~15 loads) fez o rate-limiter devolver 429 no `/auth/me` e o `refreshUser` tratava QUALQUER resposta sem data como sessão expirada → limpava o hint `le-auth` e deslogava o utilizador. Agora só 401/419 limpam o hint; 429/transientes mantêm-no.
Verificado OK sem alterações: home/loja/blog/planos/login/checkout 3 passos/perfis públicos/13 páginas backoffice/modais/toggle FazerElogio (fix da sessão anterior provado a funcionar).

## Sessão (2026-07-07, cont.2) — reauditoria (4 lentes) + Fases 1+2 executadas — POR COMMITAR
**Pedido:** "verifica o backoffice + o plano, implementa o que falta". Plano em `docs/plano-2026-07-07-reauditoria-producao.md` (4 auditores paralelos: backend/frontend/admin/produção; relatórios `reaudit-*.md` no scratchpad). Verificação prévia das claims corrigiu 3: 2FA backend EXISTE (agente frontend errou), reset password estava PIOR (forgot-password 500 por falta de `password.reset`), "créditos grátis sem gateway" NÃO é bug (sandbox deliberado, parked). **Gates: Pest 280/280 (+8) · tsc · vite build verdes.**
- **F1.1 2FA (3 bugs reais):** recovery codes 10 chars vs check `strlen===8` (nunca validavam) → regex TOTP 6 dígitos + single-use (código gasto removido); rota `/2fa/verify` estava atrás de `auth:sanctum` (impossível verificar DURANTE o login) → movida para o grupo público; **campos `two_factor_*` não são `$fillable` → TODOS os update() do 2FA eram no-ops silenciosos** (enable nunca persistia o secret) → `forceFill()->save()` (padrão Fortify). verify() agora também faz `Auth::guard('web')->login` + regenerate (sessão SPA).
- **F1.2 reset password fim-a-fim:** `ResetPassword::createUrlUsing` no AppServiceProvider → `{le.frontend_url}/repor-password?token=&email=` (antes o envio REBENTAVA com RouteNotFoundException); página nova `ReporPassword.tsx` + rota.
- **F1.3 convite:** página nova `Convite.tsx` em `/convite/:token` (login-gate com redirect, aceitar/recusar via POST, estados); `Login.tsx` aceita `?tab=register`.
- **F1.4 login social:** callback devolvia JSON cru à navegação do browser → agora `wantsJson()` mantém JSON (mobile) e browser recebe redirect com **code one-time (Cache 60s)** para `/auth/callback?code=`; novo `POST /auth/social/exchange` troca code por sessão SPA (`Auth::guard('web')->login`); página `AuthCallback.tsx` + `completeSocialLogin` no useAuth (guard StrictMode com ref).
- **F1.5 Encomendas server-side:** `OrderController::index` aceita `group` (STATUS_GROUPS open/delivered/closed) + `q` (order_number/product_name) e devolve `counts` agregados do TOTAL; frontend passa filtros, debounce 300ms, reset page em mudança de filtro (antes counts/filtro operavam só na página de 15).
- **F1.6:** BackofficeContext expõe `isError`+`refetchSession`; AppHome mostra AppErrorState (antes: skeleton eterno em falha de sessão).
- **F1.7:** wizard passa `country` real (era hardcoded PT) via `toCountryCode`; `isValidPostal(v, country)` só aplica NNNN-NNN a PT.
- **F1.8 races busyId→Set:** Dashboard, Elogios (3 mutations), Lojas (togglingIds — evita dupla cobrança na 1ª activação), Widgets (Map id→acção), Montra, Funcionarios. Guards nos handlers. `reorderShowcase` Promise.all→sequencial.
- **F1.9:** moderação invalida `qk.analytics` (Dashboard+Elogios); compra/upgrade/cancel em Creditos invalida `qk.dashboard` + memo.
- **F1.10 lows:** FazerElogio ganhou toggle de filtros mobile (padrão Loja.tsx — antes inacessível <1024px); CopyLinkButton aria dinâmico; contactSales guard anti-duplo-clique; campo morto `free_shipping_threshold` removido do CountryResource.
- **F2 admin:** ContactResource com coluna/filtros por empresa (dirigidas vs site geral) + **acção "Responder"** (CompanyMessageReplyMail p/ empresa, Mail::raw p/ gerais, status→replied); `BatchesRelationManager` novo no ResellerResource (auditoria por lote + aprovar c/ desconto%/preço + cancelar — antes só approve-em-bloco).
- **GATE 0 NOVO (decisão Renato, deixado como está por ordem dele):** Home/Login/Blog têm testemunhos+case-study FABRICADOS atribuídos a empresas reais (CTT, FNAC) + marquee "confiado por" com 10 marcas reais como fallback de CMS vazio — risco legal; não tocar até decisão.
- **FICA (Fase 3, sequenciar):** Moloni real (stub, HTTP comentado) → RGPD (zero) → email real (log driver) → deploy/CI (zero) → worker+scheduler → migração legacy (import:legacy pronto, nunca corrido). + GATE IVA/portes/comissão (inalterado). Reportados baixa-prioridade: M1 termos auto-aceites no interceptor (compliance), M5/M6/M7 menores, admin #3-6.

## Sessão (2026-07-07) — autoplan auditoria total + loop QA (Lotes 1-3) — POR COMMITAR
**Pedido:** "avalia, analiza e testa toda a plataforma... código morto ou over-complicado... loop para corrigir e testar." Plano em `docs/plano-2026-07-07-auditoria-total-loop-qa.md`. 4 auditores read-only paralelos (bugs backend · bugs frontend · código morto · over-complexity) + smoke vivo. Achados: **2 Critical · 9 High · ~18 Med · ~19 Low · 31 código-morto · 3 gaps de rota** (relatórios no scratchpad: qa-backend/qa-frontend/qa-deadcode/qa-complexity).
- **LOTE 1 (backend money/correctness) — Pest 272/272 (+4 testes):**
  - **Critical:** (a) `SubscriptionController::activateDirectly` mintava comissões de afiliado E créditos ilimitados por flip de planos A→B→A (sem gateway=sem pagamento) → conversão de subscrição REMOVIDA daqui (nasce no webhook Stripe, parked) + guard anti-flip nos créditos (`meta->reason`+`meta->plan_id` no último mês). (b) `/affiliate/conversions` dava 500 garantido (`->with('order:...,total')` mas a coluna é `total_amount`).
  - **High:** checkout apagava order sem libertar reservas de stock (DoS de stock — `releaseReservationsForOrder` antes do `$order->delete()`) · `markPaid` check-then-act sem lock → webhook Ifthenpay + polling MBWay concorrentes duplicavam stock/conversão (update condicional atómico `whereNull('paid_at')` + **unique index em `affiliate_conversions.order_id`** + catch de UniqueConstraintViolationException) · downgrade não cancelava a subscrição Stripe remota (agora chama `stripe->cancelSubscription`).
  - **Med/Low:** devolução do uso de código-revendedor/cupão no cancel de order nunca paga (`releaseDiscountUsageForUnpaidOrder` no observer Order::updated) · refund PARCIAL revertia comissão INTEIRA → agora pro-rata (`reverseForOrder($order, $refundAmount)`) · `ResellerCodeBatch::approve` não-atómico (double-click emitia 2× códigos → gate `where('status','pending')->update()===1`) · webhook Ifthenpay comparava floats estritos → cêntimos · `ShopsRelationManager::uniqueSlug` sem `withTrashed` (500 com loja soft-deleted) · publish RM sem catch de InsufficientCreditsException · adjust_credits `->integer()` · retry unicidade `Affiliate.code`.
- **LOTE 2 (frontend runtime) — build verde + smoke e2e:**
  - **High:** state-leak no Carrinho — código-revendedor digitado + cupão aplicado enviavam o que a UI não mostrava (exclusividade real: aplicar cupão limpa reseller e vice-versa; payload SÓ envia `appliedDiscount.code` validado, nunca o texto cru) · **`/app/*` sem auth guard** — deep-link/sessão expirada caía em ecrãs de erro sem redirect → `AuthGate` no `Backoffice.tsx` (isLoading→null, !user→`/login?redirect=`) + interceptor 401 no `client.ts` limpa `le-auth` (**provado e2e: visitante em /app/elogios → /login?redirect=%2Fapp%2Felogios**) · logout movido para `finally` (não deixa "meio-logado" se o POST falha).
  - **Med:** memos `dashboardMemos`(2s)/`plansMemo`(60s) nunca invalidados → `invalidateDashboardMemo`/`invalidatePlansMemo` chamados na moderação/compra de créditos/upgrade · MB WAY polling infinito → estados terminais (paid/failed) + timeout 5min + UI "Pagamento não confirmado" · sondas Afiliado/Revendedor da sidebar refaziam 404 a cada nav (keys DEDICADAS `[...qk,'probe']` + 404→valor `false` cacheável: 42→3 pedidos).
- **LOTE 3 (código morto + gaps de rota):** apagados (grep 0-consumidores) ProtectedRoute, mocks/, getMe+updateProfile, getOrders+getOrder, subscribeCheckout+getCurrentSubscription, getCompanyDashboard, 4 fns de platform.ts (getPage preservado), ComplimentController::employeeIndex+myCompliments, CompanyController::show, CompanyProfileController::compliments, ProductLowStockNotification. **Gaps de rota corrigidos:** QR web-fallback apontava `/elogiar` (inexistente→NotFound) → redirect ao perfil público da empresa da loja (`/empresa/{slug}?loja=&colaborador=`); link de convite `config('app.frontend_url')` (inexistente) → `le.frontend_url`. **Reportado não-apagado:** 8 relations Eloquent (Filament usa strings), rotas PARKED (2FA/institutional/email-prefs/Inbox), UI de convite+reset (feature em falta, backend pronto).
- **LOTE 4 EXECUTADO (2026-07-07)** — 7 simplificações aplicar-já (Pest 272/272 · tsc · vite build verdes): A1 `StatSkeleton` partilhado em `components/app/Skeletons.tsx` (5 páginas); A2 `components/app/AppErrorState.tsx` (title+text?+onRetry) substitui os 17 blocos de erro "Tentar novamente" das páginas /app (+imports AlertTriangle/AppEmpty órfãos removidos); A3 `ComplimentService::computeExpiresAt()` agora PÚBLICO = fonte única da regra de expiração (cópia inline do ExternalPartner\ComplimentController removida); A4 `lib/apiError.ts` `extractApiError` usado em Perfil/AderirConta×2/Blog/Login×2/Carrinho×2 (`api/backoffice/subscription.ts` NÃO — precisa de semântica `message ?? null`, sem fallback); A5 Carrinho: `LockIcon`/`CheckIcon`/`BackIcon` locais substituem 11 SVGs inline; A6 `OrderService::itemQty()` (formato dual qty/quantity documentado num sítio); A7 dead spacer `<div className="mb-3"/>` removido (margens colapsavam — no-op visual). Reportadas não-aplicadas: R1-R7 (ShopFormFields, partição Carrinho/god-components, formatDate, useCopyToClipboard, Field canónico, markPaid extraction).
- **FICA:** **GATE FINAL** (decisões do Renato, não auto-decidíveis): (1) **IVA** — seeder tem preços s/IVA (24,90 + `vat_rate:23`) mas backend cobra `subtotal+shipping` SEM IVA e frontend MOSTRA +23% → total exibido ≠ cobrado; backend soma IVA ou frontend deixa de mostrar? (fiscal+Moloni); (2) portes do resumo hardcoded vs regras reais; (3) comissão de afiliado incide sobre subtotal+portes (só mercadoria?).

## Sessão (2026-07-06, cont.) — autoplan backoffice/admin/afiliados/revendedores → FASE A EXECUTADA — POR COMMITAR
**Plano APROVADO em `docs/plano-2026-07-06-backoffice-admin-afiliados-revendedores.md`** (5 fases A-E; 3 scouts read-only no scratchpad). **Gate do Renato:** G1 código de revendedor = **DESCONTO % em compras** c/ N usos (migração cria `discount_percent`; resgate no CHECKOUT paralelo a coupon_code) · G2 **candidatura pública + aprovação** (ambos os programas) · G3 admin activar loja = **toggle cobrar/cortesia** · G4 Inbox parked. Registado no Brain.
- **FASE A FEITA e verificada** (tsc -b + vite build verdes · **Pest 255/255**, 2 testes novos): A1 cache 30s single-flight `GET /companies` (`ttlMemo` em `_shared.ts`; invalidada em switch/patch/logout) · A2 dedupe `/dashboard` (fetchDashboardRaw 2s, partilhado getOverview↔getCreditOverview; RawDashboard unificado em dashboard.ts) + `/plans` (memo 60s) · A3 sidebar Afiliado/Revendedor gated pelo sinal REAL 200/404 (sondas com qk.affiliate/qk.reseller partilhadas) + A11 creditOverview `enabled: hasCompany` · A4 Encomendas paginação real (backend `withExists has_open_return` no OrderController; orders.ts devolve BoOrdersPage; UI c/ Anterior/Seguinte; fetchOpenReturnOrderIds APAGADO) · A5 elogios counts endpoint (`GET .../compliments/counts`, `companyCounts` manage-gated) + filtros `shop_id`/`q` server-side no companyIndex (1 listagem+1 counts em vez de 3-4 listagens) · A6 dashboard consome expires_at+moderator · A7 contactSales → POST /contact real · A8 Lojas: CustomFieldsEditor escondido (PATCH descartava) + métrica elogios/loja fabricada removida · A9 TODOs stale corrigidos + qk.perfil na factory (moderação invalida Perfil) · A10 Revendedor copy honesta (sem "imediato", sem €12,50 mock, KPI plafond€0 → códigos activados).
- **FASE B FEITA e validada E2E no browser** (Pest **256/256**; agente da fase morreu no session-limit → executada no main loop):
  - **F1 hub Company:** form completo (FileUpload logo/cover conversão `/storage/`↔disk-relative, is_public, montra_enabled, website/address/postal/social_links KeyValue) + infolist "Plano & Páginas Digitais" (saldo Bavix + plano + subscrição + contagens) + **7 RelationManagers** (Shops/Employees/Compliments c/ moderação/Subscriptions c/ criar+cancelar/ShowcaseItems c/ price/Integrations c/ revogar/WalletTransactions) — tudo renderiza no tema dark.
  - **F2+G3:** `ShopActivationService` NOVO (`app/Domains/Company/Services/`) = fonte única do gate de cobrança (ShopController refactorado para o usar — 17/17 CreditsAndActivationTest); action Activar no RM tem toggle "Cobrar créditos" (default cortesia), cobrança SÓ no ramo ON.
  - **F3:** action "Ajustar Páginas Digitais" (creditar/debitar + motivo obrigatório) — **provado e2e: saldo 89→94**, meta `{reason:'admin_adjustment', description:'Ajuste pela equipa Livro de Elogios: <motivo>', admin_id, note}` = exactamente o que credits.ts renderiza.
  - **F4 IMPERSONATION ida-e-volta PROVADA no browser:** admin → "Entrar como cliente" → SPA 5173 c/ banner "A ver como X (impersonação de Admin Plataforma)" + logout escondido + `?impersonated=1` consumido → "Voltar ao admin" → painel restaurado. SPA: useAuth consome param+hint, AppShell banner (backendOrigin() de VITE_API_URL), AppTopbar esconde logout. Backend: redirect `config('le.frontend_url')`+LE_FRONTEND_URL no .env raiz, `/auth/me` expõe `impersonating` (nome do admin; teste AuthTest 11/11).
  - ⚠ **GOTCHA NOVO (5º, não estava no plano 07-02): `AuthenticateSession`** (painel Filament + pipeline stateful do Sanctum) compara `password_hash_web` da sessão com o user actual → `Auth::loginUsingId()` sem actualizar o hash **desloga a sessão INTEIRA no request seguinte** (user=null na tabela sessions). Fix: `session(['password_hash_web' => $target->getAuthPassword()])` ao impersonar E ao voltar (stop-impersonating) — aplicado nos 3 sítios (ViewCompany, UserResource, routes/web.php). O impersonate ORIGINAL do UserResource estava partido por isto desde sempre.
  - Extras: Contact ganhou relação `company()` + coluna Empresa no ContactResource; ViewAffiliate mostra links+cliques.
  - ⚠ Smoke admin: aceder via **localhost:8000** (não 127.0.0.1 — SESSION_DOMAIN); login Filament via Playwright = pressSequentially nos `#form.email`/`#form.password` + click submit (fill+Enter não sincroniza Livewire).
- **FASES C+D FEITAS (2026-07-07 madrugada) — Pest 268/268 · vite build verde:**
  - **C Afiliados ponta-a-ponta:** `lib/referral.ts` (captura ?ref=/&link= → localStorage 30d last-click, consumido em App.tsx antes do render; register+checkout enviam automaticamente) · migração `2026_07_06_200001` (orders.affiliate_id/affiliate_link_id, users.referred_by_affiliate_id, conversions.affiliate_link_id+paid_at, affiliates.iban) · **`AffiliateAttributionService`** (fonte única: resolveRef/attachRefToOrder/recordConversionForPaidOrder/reverseForOrder) — **conversão nasce SÓ em markPaid** (cobre ref E cupão; a criação pré-pagamento no CouponService foi REMOVIDA — inflava com Multibanco nunca pago), reversão no refund (ReturnRequestService) excepto se já paga · conversão type=subscription em activateDirectly p/ donos referidos · **payout admin**: `ConversionsRelationManager` (AffiliateResource) pending→approved→paid c/ bulk actions + paid_at + IBAN no form + role `afiliado` no afterCreate · **candidatura pública** (G2): `POST /affiliate/apply` (202 pending/409 dup) + `NewPartnerApplication` notification DB aos admins + UI no empty-state do Afiliado.tsx (botão real, estado "em análise") · **7 testes** `AffiliateAttributionTest`.
  - **D Revendedores (G1=desconto):** migração `2026_07_06_210001` (reseller_codes.discount_percent/activated_by/cancelled_at; batches.discount_percent/unit_price/cancelled_at; orders.reseller_code_id) · **`applyResellerCode` REESCRITO** contra o schema real (validações PT-PT, consumo atómico anti double-spend mantido, activated_by na 1ª utilização, desconto sobre subtotal, notificação DB `ResellerCodeUsed` ao revendedor) e **ligado ao checkout** (`reseller_code`, `prohibits` mútuo com coupon_code) · `ResellerCodeBatch::approve(discountPercent, unitPrice)` — admin fixa o desconto na aprovação (form na action approve_batches) + `cancel()` · ResellerResource: **form real + página Create** (antes nem o admin conseguia criar) + action suspend + create_code na semântica desconto/usos + role `revendedor` no afterCreate · candidatura pública `POST /reseller/apply` + modal de candidatura real no Revendedor.tsx (substituiu o mailto) · retry de unicidade no code · frontend: campo "Código de revendedor" no Carrinho (exclusivo com cupão) + `CheckoutRequest.reseller_code` · **5 testes** `ResellerRedemptionTest` · `docs/diretivas-revendedores.md` actualizado c/ nota da decisão.
  - ⚠ Gotchas de sessão: heredoc bash→python COME um nível de backslashes (usar r-strings/Edit tool p/ PHP namespaces); `Order.fillable` sem `reseller_code_id` fazia o update falhar silenciosamente (apanhado por teste).
- **FICA (Fase E parcial + parked):** smoke browser do resgate no checkout + candidaturas (backend coberto por Pest; impersonate/créditos admin JÁ smoked e2e) · tester-code review final · plafond Bavix/pricing automático de lotes (parked por decisão) · gateway real (excluído) · dados de teste na BD local: company 14 (Teste10) c/ +5 créditos "Oferta de teste da equipa".

## Sessão (2026-07-06) — commit+push do corpo 07-05 · autoplan design/mobile · plano EXECUTADO — POR COMMITAR (~51 ficheiros frontend, `dev`)
1. **Corpo de 07-01→07-05 commitado (`88dda1e`) e pushed fast-forward para `setuptech/renato-dev`** (141 ficheiros; `dev`/`master` intactos). renato-dev = `88dda1e`.
2. **Autoplan frontend+backoffice (design/correcções/mobile):** 4 auditorias read-only (backoffice · público · componentes · bugs) → plano em `docs/plano-2026-07-06-frontend-design-mobile.md`. Gate decidido pelo Renato: **G1 contraste CTA → laranja escurecido só em fills** · **G2 prova social → 11.000 empresas / 27.000 livros**.
3. **Plano implementado (49 mod + 2 novos, só frontend+docs; zero backend):**
   - **Tokens novos em `index.css` @theme** (DESIGN.md §3 + CLAUDE.md sincronizados): `--color-primary-cta #D14100` (AA 4.70:1 c/ branco) + `-hover #BF3B00` — **fills laranja usam CTA; #FD5000 fica para bordas/ícones/texto grande/focus**; `--color-app-canvas #F2F2F5`; escala `grey-100..500`; famílias success/error/warn/info c/ `-bg`+`-text`; **escala z-index `--z-*`** (usar `z-(--z-modal)` etc.).
   - **Mobile:** inputs ≥16px em todo o lado (mata zoom iOS); touch ≥44px (Nav/AppTopbar/paginações/Modal/Drawer close/menus; `pointer-coarse:min-h-11` em pills/tabs/steppers; switches c/ hit-area `after:-inset-2.5`); tabela Créditos esconde Referência/Valor `<md`, Encomendas esconde Data `<sm`; perfis públicos deixaram de ser dead-end (topbar c/ Directório+Aderir + `<Footer/>`); SobreNos timeline vertical simples `<sm` + hero fluido.
   - **Componentes:** InfoBanner/AppCallout/StatusBadge/AppStatCard → mesmos tokens; Button central: focus=outline global (fora o ring), `aria-disabled` + click-guard; SegmentedControl sem `outline-none`, `width` aceita string (`clamp()` responsivo no billing dos Planos); ProductCard `object-contain`; Tooltip Escape.
   - **Marca/PT-PT:** moeda 100% prefixo `€X,XX` (helper único `lib/currency.ts`; fmtPrice/prose incl.); glifos ♥/✓/✕/→/★ → SVG/removidos; AO90→pré-AO90 (Directório/directamente/reflectem, incl. Termos); card preto do Afiliado → tint `#FFF1EB`; watermark "protótipo"+zoom fake do mapa FazerElogio removidos; 27.000→11.000 em Planos+Login.
   - **Bugs:** `pages/Dashboard.tsx` órfão APAGADO; `getMe()` dead code removido; Footer: newsletter stub→Link /blog, card Digital→/fazer-elogio, sociais dimmed, hover fix; botões "Pesquisar" mortos removidos (Blog/FazerElogio); stats inventadas do fallback do Blog reescritas.
   - **Descobertos no teste (não estavam no plano):** keys duplicadas nas categorias do directório (API multi-país → dedup por slug em FazerElogio); overflow-X fantasma nos Planos = spans `sr-only` absolute a escapar da tabela comparativa → `aria-label` no svg; `ComplimentSubmission.author_name` obrigatório vs fluxo anónimo (só `tsc -b` do build apanha — o `--noEmit` não) → opcional.
4. **Verificado:** `tsc -b` + `vite build` verdes · smoke Playwright real a 375px+1366px (Home/Loja/Planos/FazerElogio/PerfilEmpresa/Login/app Dashboard/Créditos/Afiliado) · 0 erros de consola (404 affiliate = ruído esperado) · 0 overflow-X real · 0 conflict-copies MEGAsync.
- ⚠ Home "Como Funciona" continua a servir imagens de `livrodeelogios.com` (domínio prod) — sem asset local no repo; adicionado width/height p/ CLS. Hospedar local quando houver os ficheiros.
- Fica do gate: G4 citações imprensa SobreNos (confirmar reais) · press/`Componentes.tsx` showcase mantidos.

## Sessão (2026-07-05) — TUDO ligado ao backend real + verificação 4-modelos — commitado em `88dda1e` (2026-07-06)
Pedido do Renato: "verifica o que falta no backoffice do cliente, implementa TUDO agora, front↔backoffice ligado e funcional, mobile, sem erros; autoplan; no fim todos os modelos concordam". Executado com 4 scouts Explore → implementação (main loop + 3 agentes) → verificação por 4 revisores (tester-code, tester-ui-ux, codex/OpenAI, gemini/agy) → fixes de TODOS os findings accionáveis. **Estado final: Pest 253/253 · tsc 0 · mocks/backofficeStore.ts (~1600 linhas) APAGADO — zero mocks.**
- **Backend novo:** 9 migrações (contacts.company_id · affiliate_links · reseller_code_batches+status · invites name/job_title/shop_id · integrations impressions/clicks · showcase price/price_label · employees.role · reseller plan_id nullable) · controllers: CompanyAnalytics (GET companies/{slug}/analytics), CompanyMessage (inbox+read+reply c/ email), Notification (me/notifications+read-all; sino alimentado por NewComplimentPending/CompanyMessageReceived), Country (GET /countries), AffiliateLink (CRUD + redirect público /go/{code} c/ contagem), MeCompanyHistory · alargados: Compliment (status filter gated por manage, expires_at+moderator no resource — moderator SÓ p/ gestores, revert→pending c/ novo expires_at), CompanyMember (invite c/ campos+email real via CompanyInviteMail, resend throttled, token FORA do JSON, rejectInvite verifica email), Showcase (?all=1 gestão incl. inactivos + gestor gere empresa inactiva), Reseller (batches PENDENTES → aprovação admin via acção Filament approve_batches; dashboard c/ builders frescos), Widget (track PÚBLICO por integration_id + throttle — a API key NUNCA vai ao DOM), Contact (rota contextual companies/{slug}/contact), Directory (sort whitelist + counts iguais em index/search), OrderService (consumo ATÓMICO de códigos, anti double-spend). Compra avulsa de créditos (purchase-custom, preço server-side) e billing UI removida (pedidos anteriores da sessão).
- **Frontend:** 14 domínios api/backoffice religados · páginas públicas: FazerElogio (directório real + mapa c/ pins lat/lng reais projectados), AderirConta (?plano= real), Loja (compra digital), Blog (pesquisa/tags/paginação/newsletter reais), Home (produtos API), Carrinho (cupões via discount-codes/validate + coupon_code no checkout), CriarConta APAGADA (rota morta) · PerfilEmpresa (Ver mais/Partilhar/pesquisa/grids responsivos/order-first mobile) · Lojas SEM créditos por loja (arquitectura: wallet é da empresa) + maskToken restaurada · Widgets c/ modal one-time da API key + snippet data-integration · 19 fixes mobile/a11y.
- **Findings dos 4 revisores TODOS tratados** (4+2 Critical, 10 Important, 14 UI) — relatórios em scratchpad da sessão. Aprendizagem multi-modelo no Brain.
- **Testes novos:** `BackofficeGapsTest` (15) + `PlanTest` corrigido (4 planos é o correcto — teste estava obsoleto).
- **Gotchas novos:** teste multi-user Sanctum precisa `$this->app['auth']->forgetGuards()` entre users (guard cacheia) · rota pública que quer identificar token → `$request->user('sanctum')` (o default guard não resolve Bearer sem middleware) · widget track é público por design (vanity counters; key só server-side/WP).
- **Fica (documentado, por decisão/dependência):** gateway real de pagamentos · origin-binding da key do widget submit (lane Pedro) · filtro shopId/q de elogios server-side · nº fatura Moloni · BD dev: renato-teste marcada is_verified + ~52 créditos de smoke.

## Knowledge graph
`graphify-out/` existe; `graph.json` ~179 MB. Rebuild (Python API `_rebuild_code`) corrido em 2026-06-21.
