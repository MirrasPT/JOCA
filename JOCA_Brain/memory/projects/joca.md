---
project: JOCA
type: toolkit-self-development
stack: Claude Code skills/agents/commands (Markdown) + JOCA_UI (Node/Express/WS + React/Vite)
repo: MirrasPT/JOCA-OS (PRIVADO; root = JOCA_FINAL, single git repo). Backup: MirrasPT/JOCA (origin antigo, intacto)
---

# JOCA — Toolkit Self-Development

## Estado actual
**2026-07-03 (cont. 3) — Auditoria + melhoria sistémica do JOCA_Brain (skills/agentes/commands/hooks), a partir de 20 posts virais IG/Reddit sobre Claude Code.** Análise dos posts (42 Skills / 10 Repos: Superpowers, Karpathy Skills, Repomix, everything-claude-code, wshobson/agents, Claude Squad, TDD Guard, claude-mem, UI UX Pro Max, HyperFrames) → cruzados com o inventário real. **Workflow (via D):** 3 agentes de auditoria paralelos (skills 131 · agentes 40 · commands/rules/hooks) → relatórios no scratchpad → 5 workers aplicaram fixes + main loop. **Skills (65 ficheiros):** 28 descriptions reescritas (lixo auto-gerado "MUST be invoked: Behavioral, LLM" eliminado), `chain:` em 26, 24 limpas de paths inexistentes (packs importados achatados sem os assets — 12 scripts wp-* mortos), 3 `name:`≠filename alinhados (horizon/marketing/search), caveman default→lite, google-analytics reescrita (era manual de `ga_query.py` inexistente). **Agentes (26):** Step 0 Read de skills nos 3 em falta, `chain:` em 10 (par canónico tester-ui-ux→a11y-fixer), tools least-privilege nos 4 report-only, cláusula "escreve p/ `.joca/intermediate/` + devolve resumo" nos ~14 offenders (o cap 3-5 workers assume isto), gemini-brain/auditor com distinção real (ambos usam `agy` CLI — multimodal vs audit), **tester-api ganhou fase pen-test activo** (JWT alg:none, HPP, SSRF, mass-assignment — gap do wshobson/agents). **Componentes novos:** `/tdd` guard-rail (`check-tdd.js` PreToolUse + skill `tdd.md`; flag-file no padrão freeze/careful; **ask nunca deny**; janela 30min após tocar teste) · `stop-checkpoint.js` (auto-checkpoint no Stop, corre ANTES do auto-test-dispatch, throttle 10min, poda própria de 4 autos) · `pack-context.mjs` + skill `context-pack` (repomix-style, node puro, output fora da árvore-alvo) · `reference/design-dataset.md` (16 paletas OKLCH + 12 pares fontes + 12 estilos, ligado a design-shotgun+frontend p/ anti-convergence). **Splits multi-step (6 skills, padrão testing.md, zero perda):** frontend 532→187 · availability 508→105 · saas-patterns 505→77 · wp-performance-review 479→190 · reverb 483→231 · filament 408→175, denso movido p/ `.claude/reference/<skill>/*.md` on-demand. Todos os hooks novos smoke-tested. `/sync-questionnaires` corrido (contadores README 113→133/36→40/22→26/171→199; nomes antigos propagados; README ganhou secções Guard-rails + Game Dev agents). SKILL_INDEX 133 skills·40 agents, bridges recompilados. 2 hooks bash legados apagados. **POR COMMITAR.**

**2026-07-03:** **2ª ronda de `Erros.md` do JOCA_OS (feedback pós-teste da 1ª ronda) — 11 itens corrigidos, tsc backend+frontend verdes, backend rebuild+restart limpo (1 listener 7491, /projects 200), frontend live via Vite HMR. POR COMMITAR.** Decisões (Renato away → defaults recomendados): notificações = "só o Master 1× cada"; drag = restaurar cópia-fallback do Explorer. **Cluster notificações (4/14/16) — root cause:** `session-manager.input()` armava `notifyOnIdle` em QUALQUER tecla do user → trabalhar num worker acordava o Master (feedback 3-4×). Fix: **split `masterAwaiting` (só `submitMessage`/brief do Master) vs `notifyOnIdle` (qualquer burst)**. `emit('done')` (acorda worker-watch) agora gated em `masterAwaiting` → o user a escrever num worker JÁ NÃO acorda o Master. `worker-watch` só faz append/broadcast do summary FINAL (`!continued`) → **1 feedback por tarefa** (intermédios ficam no indicador de actividade). Toast de terminal **removido** (só fica o unread dot; zero popups para trabalho manual/abrir/fechar/trocar). **#13** worker do Master arranca SEMPRE no Brain mesmo com `cwd` (não só projectId) — cwd/projecto via `/resume`. **#15** directiva na automação: NÃO reutilizar terminais, abrir worker dedicado. **#17** directiva: fechar o worker com `close_worker` no fim. **#19** tools novos `close_worker`/`close_project_workers` (+ system prompt) — Master pode fechar terminais ("fecha o projecto X por hoje"). **#20** dropdown "Model ▾" no command bar do terminal (opus/sonnet/haiku/fable/default → `/model <x>`). **#3** drag restaurado nos 3 alvos (terminal-area, TerminalPane, MasterChatView): path real (JOCA Files/Finder) sem cópia; Explorer sem-path → `resolveDrop` (cópia p/ JOCA_Drops) em vez de não fazer nada. **#12** Ctrl+V de imagem no form de tarefa (`onCreatePaste`→uploadPastedImages→anexo). **#6** confirmado já OK (Ctrl+C copia selecção, Ctrl+Shift+V cola texto, Ctrl+V cola imagem). **Deferido (UI grande subjectiva):** **#8** (caixa Master cresce até 50vh + chat mais bonito/tabelas/coerente) + **#18** (painel Ficheiros no Master + estado geral na barra superior) — próxima sessão focada. Ficheiros tocados: session-manager.ts, worker-watch.ts, orchestrator.ts, master-tools.ts, automations/runner.ts (backend); useSessionSocket.ts, TerminalView.tsx, TerminalPane.tsx, MasterChatView.tsx, TasksView.tsx (frontend).

**2026-07-03 (cont.) — Optimização de contexto/tokens do Brain (memory files 43.2k → 22.7k tokens/msg, −47%, permanente e recorrente).** Diagnóstico via `/context`: os "memory files" eram o único custo controlável (o resto — system prompt/tools/autocompact buffer — é fixo). **Descoberta-chave:** este harness **auto-carrega `.claude/rules/*.md` como instruções em TODAS as sessões** (não há `@import` nem config em settings.json — é convenção do build; skills/agents/commands em `.claude/` são lazy, NÃO auto-carregados). Acções: (1) **`~/CLAUDE.md`** 24.7k→4.2k chars — tabela de projectos passou a 1 linha + ponteiro `memory/projects/<x>.md` (detalhe carrega no `/resume`). (2) **`JOCA_Brain/CLAUDE.md`** 18.6k→15.2k — cortada prosa redundante (Repository Structure ASCII, Adding Components, Cross-CLI, Lazy Loading, Autonomous Testing) + tabela Pipelines (duplicada 100% em `rules/pipelines.md` → ponteiro); **Decision Filter + Trigger Map + Commands mantidos** (routing funcional). (3) **`.claude/reference/` novo** (NÃO auto-carregado) — `api-design.md`+`workflows-and-tooling.md` (24.3k chars de referência de nicho) movidos para lá; `rules/` fica com pointer de 1-parágrafo que redirige + serve de gatilho lazy; 6 referrers continuam válidos (path `rules/…` preservado). (4) **`.claude/rules/README.md`** — nota anti-bloat (rules/ é sempre-carregado → só directivas globais terse; detalhe → `reference/`). Verificado no `/context`: 13 memory files (reference/ NÃO apareceu ✓). Resta tocável só a superfície de capacidade (Skills 10k/Custom agents 7.8k) — recomendado PARAR (6% de 1M, cortar = perder capacidade).

**2026-07-03 (cont. 2) — Stop hook a rebentar em cada mensagem + auditoria/reconciliação da doutrina de auto-orquestração.** (A) **Hooks:** os 7 hooks do `.claude/settings.json` usavam caminho RELATIVO (`node .claude/hooks/…`) → com o cwd desviado (os meus `cd memory/projects` para extrair o log de Junho), o Stop hook procurava em `memory/projects/.claude/…` e falhava (`MODULE_NOT_FOUND`, non-blocking mas ruidoso a cada msg). Fix = **caminho ABSOLUTO** em todos (`C:/Users/renat/Desktop/JOCA_FINAL/JOCA_Brain/.claude/…`); escolhi absoluto sobre `$CLAUDE_PROJECT_DIR` porque no Windows a var pode vir **vazia** E os hooks podem correr em **cmd** (não expande `$VAR`). Verificado: correm com exit 0 de qualquer cwd, sem env. ⚠ Aplicar exige `/hooks` ou restart (o Claude fotografa a config de hooks no arranque). ⚠ Mover a pasta-raiz exige actualizar estes paths. (B) **Auditoria da auto-orquestração** (workflows/goals/loops/skills/agentes): espinha dorsal sólida (4 vias + injecção por `prompt-triage.js`/`session-intake.js` + travões `loop_max_iterations=4`/3×-nada + `task-router` puro + brief de 8 cláusulas), MAS **contradição crítica**: `/goal`+`/one-shot` faziam `Agent(subagent_type="master-orchestrator")` e o orchestrator despachava workers = **subagente-spawna-subagente**, proibido pela própria regra de 1-nível → o fan-out falharia na prática. **Fix (4 ficheiros):** `master-orchestrator.md` passou a **PLAYBOOK que o main loop ADOPTA e executa** (dispara os workers ele próprio), NUNCA spawned; reescritos `rules/orchestration-patterns.md` (regra crítica + Ligações), `commands/goal.md` (passo 4), `commands/one-shot.md` (passo 3 — tinha o bloco `Agent(...)` literal). Removidas 2 notas **stale** (`task-intake.md`/`goal.md` "podem não existir" — existem). Verificado: 0 spawn-calls restantes; referrers (chaining/pipelines/task-intake/CLAUDE/install) já eram descritivos. Deixados de propósito (não-incoerências): activação-de-skill é comportamental; drift de contagens é cobertura parcial; cap 4 (Brain) vs 8 (`worker-watch` JOCA_OS) são métricas distintas. Bridges recompilados. **POR COMMITAR.**

**2026-07-01 (cont.):** **Restante do `Erros.md` do JOCA_OS implementado por WORKFLOW** (fase backend 1 agente coerente → 4 agentes frontend de ficheiros DISJUNTOS em paralelo → verify) — **3 builds verdes** (tsc backend+frontend + vite build). Itens: **#13** worker do Master arranca SEMPRE no JOCA_Brain (cwd cai no `JOCA_LOGIC_ROOT`) e carrega o projecto por `/resume "<path>"` (resumePath) — igual à UI "Abrir projecto"; `spawn_worker` separa `cwd`(Brain) de `resumePath`/`projectPath`(projecto) + `origin:'master'`. **#14** sessões ganham `origin:'user'|'master'` (em `Session`/`SessionInfo`/`SpawnOptions`, serializado por `info()`/`listInfo()`) + `worker_summary` leva `auto`/`continued` (orchestrator marca `continued` quando a turn chama spawn/send/select) → **notificar só no despacho e no fim** (`!auto||!continued`), toast de terminal **suprimido para workers do Master** (fim do spam 3-4×). **#8** caixa do Master `rows=3` até 50vh. **#9** markdown rico (tabelas zebra/blockquote/imagens inline/headings/links em nova aba; `<img src>` permitido, `on*`/`javascript:` bloqueados). **#10** board redesenhado + ícone abrir/fechar coerente (chevron roda 180°) + **criar tarefa no Master**. **#11** CSS de `MasterChatView`/`MasterTasksBoard` alinhado aos tokens do `App.css`. **#12** anexos nas tarefas (board + `TasksView`, `POST`/`PATCH /tasks`, `Task.attachments`). **#3** drag mantém a regra (path real; Explorer sem-path → **dica** "usa Ficheiros ou Ctrl+V"; Master deixou de fazer upload no drag → consistente com o terminal). **Bug apanhado no review + corrigido:** anexos guardavam-se mas o worker **nunca os via** ao correr → `tasks/engine.buildObjective` passou a injectar os caminhos nas `[Instruções da tarefa]`. Review de integração ponta-a-ponta **limpo** (wrapper novo do xterm não colapsa a altura — `.terminal-container flex:1` dentro de wrapper `flex-col/minHeight:0`; `origin` chega ao frontend). **POR COMMITAR.** Melhorias identificadas (por decidir): lazy-load do markdown/vistas (chunk >500 kB), DRY da dica de drag (3 cópias), imagens locais no chat exigiriam rota `/file` segura.

**2026-07-01:** **7 bugs do JOCA_OS corrigidos** (só JOCA_OS, JOCA_UI intacto; lista em `Erros.md`) — tsc+build verdes, app não estava a correr (arranca-se limpa). (1) **/resume intermitente** → arranque guiado por prontidão (`session-manager.runStartupSequence`: quiet-gate + aceita prompt "trust this folder?" + só então envia comando/brief; fim dos timers fixos 1200ms). (2) **Ctrl+V de imagem no textarea abaixo do terminal** → `onPaste` em `TerminalView`. (3) **Drag = caminho, não cópia** → `dragRealPaths()` em `fileDrop.ts` (drag usa só path real, síncrono; só Ctrl+V grava em JOCA_Drops). (4) **Master fazia e parava** → `master/worker-watch.ts` novo: evento `done` (que ninguém consumia) re-invoca o Master (follow-up "[EVENTO AUTOMÁTICO]… lê e continua ou responde"); serializado + adiado enquanto outro turno corre (`masterRunsActive`), cap 8 auto-continuações/worker (reset por mensagem nova), `notifyOnIdle` armado no brief inicial. (5) **Som + notificação SO** → `lib/notify.ts` (blip WebAudio + `Notification` Win/Mac + pede permissão no arranque) em session-done/master-reply/automação. (6) **Ctrl+C/Ctrl+V terminal** → `attachCustomKeyEventHandler` (Ctrl+C com selecção copia, senão ^C; Ctrl+Shift+V cola). (7) **Tarefas não apareciam** → faltava `/tasks` no proxy do `vite.config.ts` (gotcha do proxy SPA-fallback, já documentado). **POR COMMITAR** (11 mod + 2 novos no monorepo). ⚠ #3 tem limite de browser: drag do Explorer do Windows não expõe path → não insere; drag do file browser do JOCA/Finder funciona.

**2026-06-29:** Memória de projectos do **JOCA_UI portada para o JOCA_OS** (REPLACE escolhido pelo Renato): `projects.json`+`project-memory.json` do UI copiados por cima dos do OS (backups `.bak-20260629-094852` em `JOCA_OS/data/`). OS passou de 13→**14 projectos** (ganhou `tcg`/`Royal Douro`/`Eurico Fertuzinhos`/`JOCA - Open Source`; perdeu os só-OS `Medieval Chess`/`Gerador`/`Projeto Teste`). App **a correr** (arrancada à mão destacada — ver learning do `start.bat`): backend :7491 (Brain 132 skills·40 agents·27 cmds, `[tasks] engine on`) / frontend :7492; `/projects` confirma 14.

**2026-06-28:** JOCA_OS com **3 god-files refactorizados** (server.ts/App.tsx/DashboardView) + **feature Tarefas/Kanban (Fase 8) completa** (auto-pull do Master + tester + tab + 4º bloco) — tudo verificado (tsc/build/smoke) e pushed (`98aaa7f`). App a correr backend :7491 (`[tasks] engine on`) / frontend :7492.

Toolkit (JOCA_Brain) em `master`, sync `origin/master` (`MirrasPT/JOCA-OS` privado). **App JOCA_OS = AGORA NO MONOREPO** (absorvida 2026-06-27 `5de05e0` — era repo local-only sem remote; o `.git` aninhado foi removido, history salvaguardado em `~/JOCA_OS-app-history.bundle`). O repo privado tem tudo: Brain + UI + app. Ver secção JOCA_OS abaixo. **Rebrand para `JOCA-OS` feito (2026-06-21):** `origin` repontado de `MirrasPT/JOCA` → `MirrasPT/JOCA-OS` (repo novo, **privado**); push de tudo (380 ficheiros). Repo antigo `JOCA` fica como backup (reverter: `git remote set-url origin …/JOCA.git`). Commit inclui memória pessoal (profile/soul/projects/feedback) — ok porque privado. Excluídos via gitignore: `JOCA_OS/` (cópia 226M), `temp_audio/` (scratch whisper), `rename-to-brain.bat`.

`/upgrade-joca`+`/sync-questionnaires` **commitados**: 3 skills (`content-calendar`, `lyric-align`, `browser-automate`), `/build-plan`, rule `workflows-and-tooling.md`, soul/api-design/frontend/deploy-cpanel reforçados, python3→python, graph excludes, stop.bat `/T`. Contadores: **106 skills · 28 agents · 20 commands**.

**JOCA_OS** (2026-06-21) — v1 (orquestrador Agent SDK) apagado; Renato recomeça de raiz. `JOCA_OS/` = cópia literal do `JOCA_UI/` (robocopy, 226M, **gitignored**) como base a repurposar. `FUTUROS.md` mantém a visão.

**Rename `JOCA_Logic` → `JOCA_Brain` CONCLUÍDO** — o Brain. Refs em 28 ficheiros + rename físico da pasta feitos (`rename-to-brain.bat` já corrido e gitignored). Docs históricos (`AUDITORIA_LOGIC_*`, `migrate.md`) mantêm nome antigo de propósito.

**Perfil base do Renato criado** — `JOCA_Brain/memory/profile.md` (canónico, dados pessoais, fora de commits públicos) + auto-memória [[renato-profile]] + [[renato-design-prototype-fidelity]]. Via entrevista de 15 perguntas (interview mode). Captura: papel SetupTech (quer ser parceiro), produtos (livro de elogios, Bigorna 3D), filosofia AI-driven (design co-criado, resto 100% AI), meta €1500/mês, visão Jarvis, comms terso, pain points (drift protótipo→React, tarefas dispersas, marketing fraco).

## Decisões tomadas
- 2026-05-30 — Arquitectura composável: skills específicos que se encaixam > monólitos generalistas. `frontend` vira director.
- 2026-05-30 — Criados 6 skills: `react-patterns`, `react-composition`, `tailwind`, `react-email`, `shadcn`, `design-review`.
- 2026-05-30 — Integração de skills externas: preferir fonte oficial/canónica (shadcn oficial, não Google Labs/Stitch que depende de MCP externo).
- 2026-05-30 — `design-review` é o dono do gosto/AI-slop/composição; `tester-ui-ux` (agente) = QA+WCAG; `design-system-audit` = tokens. Sem overlap.
- 2026-05-30 — `html-review` é conversor md→HTML, NÃO review de UI. Citações corrigidas.
- 2026-05-30 — Commit só do meu trabalho (lista explícita), em branch novo; UI/img-gen/memory deixados uncommitted.
- 2026-05-30 — Cluster backend (3 objectivos: admin/integração/hardening): NOVO `laravel-react` skill, `laravel-refactor` + `filament-builder` agents; `filament` aprofundado; `laravel-specialist` reforçado. Derivado de 8 skills externas Laravel/Filament.
- 2026-05-30 — Backend stacked no MESMO branch (`e0526d2`), não off-master: CLAUDE.md/SKILL_INDEX partilhados já tinham o design commitado → off-master ficaria entrelaçado. Lição: trabalho que edita ficheiros partilhados deve empilhar no branch onde esses ficheiros já foram tocados.
- 2026-05-30 — `INDEX.md` deixado sempre fora dos commits (tem entradas de projecto/feedback do Renato misturadas).
- 2026-05-31 — Consolidação de branches → único `master`. Estratégia: branches já-em-master apagados (`feat/frontend-design-skills`, `joca_ui_windows`); estruturas v1/fork divergentes (`v1-legacy` 613 ficheiros, `analyze-project-BBnm8` 790 ficheiros) **arquivadas como tags `archive/*` e apagadas** (merge destruiria o v2); `ana-lisa` reduzido ao seu único valor — `FUTUROS.md` — fundido e branch apagado. Workflow versionado: `.claude/scripts/consolidate-branches.sh` (idempotente, dry-run default, `RUN=1` executa).
- 2026-05-31 — Lição: "merge todos os branches" não é literal quando há histórias divergentes; verificar `git diff --stat` vs merge-base antes — fork antigo sobrepõe arquitectura nova. Arquivar via tag > apagar cego.
- 2026-05-31 — Rate limits do dashboard: backend tornado **auto-suficiente** (lê oauth/usage + `~/.codex/logs_2.sqlite` via `node:sqlite`) em vez de depender de ficheiros tmp escritos pelos statuslines. Bugs Windows corrigidos: token Claude em `~/.claude/.credentials.json` (não Keychain macOS); codex sem binário `sqlite3` → `node:sqlite`. AGY/Antigravity **não expõe limites temporais** — não fabricar barras. Detalhes em auto-memory [[joca-ui-rate-limits]].
- 2026-05-31 — Bug de navegação: `contextProjectId` preferia o projeto da sessão activa sobre o projeto clicado → em `mainView==='project'` usar sempre `activeProjectId`.
- 2026-05-31 — Auditoria multi-agente do JOCA_UI (workflow, 6 dims × verify adversarial): **backend passou 100% limpo** (security/correctness/path-traversal/injection resistiram); todos os 10 achados confirmados eram **frontend a11y + handlers optimistas recém-escritos**. Sinal claro: o código novo é o que precisa de afinação, não o estabelecido.
- 2026-05-31 — Padrão a11y: nunca montar controlos interactivos condicionados a estado JS de `hover` (mouse-only) nem `display:none`-até-hover (sai do tab order); usar `opacity:0` + `:focus-within` + `@media(hover:none)`. `role=button` em containers com botões aninhados = ARIA inválido → o elemento clicável deve ser um `<button>` real (nome/label), com os outros controlos como irmãos.
- 2026-05-31 — Workflow tool: mau fit para git destrutivo (sequência determinística, não paralelizável) → usei script versionado. Bom fit para auditoria (fan-out de revisão + verify adversarial). Distinguir os dois casos.
- 2026-06-20 — `/upgrade-joca` corrido via Workflow (research+draft+evaluate de 6 artefactos, todos PASS 9-9.5 à 1ª iteração) + edições mecânicas manuais nos ficheiros partilhados (resume/save/CLAUDE/soul) para evitar clobber em paralelo. Lição reforçada: trabalho que toca ficheiros canónicos partilhados fica sob controlo sequencial do main loop; o fan-out é para research/draft de ficheiros independentes.
- 2026-06-20 — Verificação anti-fabricação: a nota "graphify-deps.py já ignora vendor" era falsa → tornei-a verdadeira adicionando os patterns ao `IGNORE_DIRS` em vez de fabricar. Sempre verificar antes de afirmar (a própria regra que escrevi em `workflows-and-tooling.md`).
- 2026-06-21 — **JOCA_OS v1 apagado, recomeço de raiz.** App sibling untracked (205M) removido por decisão do Renato. Arquitectura/decisões antigas limpas da memória intencionalmente — partir de folha em branco. Lições técnicas que sobrevivem (validadas por probe real, reutilizáveis): (a) `@anthropic-ai/claude-agent-sdk` conduz o CLI `claude` logado → corre na **subscrição** sem `ANTHROPIC_API_KEY`, custo zero; (b) workflow dev→test→fix em loop (review paralelo read-only → fixer sequencial → re-verifica tsc, até 3 rondas) é óptimo para endurecer codebase novo.
- 2026-06-21 — **Perfil base do Renato via interview mode** (15 perguntas). Output: `memory/profile.md` (contexto estático canónico, tipo `user.md` do conceito Agentic OS) + 2 auto-memórias. Inspirado no vídeo Simon Scrapes "Creating Your Own Agentic OS" (memória 6-níveis, herança `claude.md`, acesso remoto — valida o roadmap FUTUROS). Pain point nº1 capturado como regra accionável: **fidelidade protótipo→React** (a AI diverge após a 1ª secção).
- 2026-06-21 — **`JOCA_Logic` renomeado para `JOCA_Brain`** (é o Brain). Refs em 28 ficheiros via `sed` scoped (excluídos docs históricos). **Lição (Windows):** não se renomeia a pasta-raiz do projecto de dentro do Claude — o cwd do próprio processo Claude segura-a (`Permission denied`). Padrão: actualizar refs in-session (ficheiros são graváveis; só o *rename do dir* bloqueia) → script `.bat` que o user corre com o Claude fechado → reabrir na nova pasta. JOCA_UI tem de ser parado antes (handles).
- 2026-06-21 — **Novo `JOCA_OS` = cópia do `JOCA_UI`** (robocopy `/E /MT:16`, 226M idêntico) como ponto de partida em vez de raiz. Por repurposar (renomear refs internas, mudar portas para não colidir com 7371/7372).
- 2026-06-21 — **Upgrade global de autonomia** (sync repo → `JOCA-OS` privado). 2 workflows: (1) análise de 8 repos externos (ponytail, addyosmani/agent-skills, swc, Understand-Anything, markitdown, taste-skill, system_prompts_leaks, Anthropic-Cybersecurity) + auditoria do Brain → plano em `_improvement/`; (2) aplicação paralela. **Mecanismo de autonomia = 3 camadas:** (a) `rules/task-intake.md` — decision tree de 4 vias (directa/skill/agente/workflow) por thresholds, ancorado no Decision Filter (CLAUDE.md passo 0+2) e soul.md; (b) hooks `SessionStart`+`UserPromptSubmit` (Node, Windows-safe, fail-silent) que injectam o tree a cada turn — **tira a decisão da memória do modelo** (padrão ponytail/addyosmani); (c) `/goal` (workflow NL sem PRD) + `master-orchestrator` tornado goal-seeking-em-loop (Phase 4.5) e domain-agnostic (lê SKILL_INDEX). **+8 agentes** (task-router, knowledge-ingest, automation-builder, personal-comms, pr-repair, deploy-executor, a11y-fixer, tech-debt-auditor), **+4 skills** (knowledge-ingest, automations, personal-comms, yagni), rule `orchestration-patterns`, `validate-skill.py`. **Inventário: 110 skills · 36 agents · 22 commands.** Lições-chave verificadas na fonte: subagentes NÃO spawnam subagentes (auto-orquestração vive no main loop/command, não num agente-que-chama-agentes); o campo `skills:` no frontmatter NÃO carrega skill — garantia real = Step 0 `Read()` no corpo do agente. **Pós-trabalho concluído:** markitdown 0.1.6 instalado + MCP `markitdown` registado (user scope, connected, HTML→md verificado) p/ `/know`; `validate-skill.py` ligado a PostToolUse via hook `skill-lint.js` (só linta `.claude/skills/*.md`, não-bloqueante) + gate no `/create-skill`; validador relaxado (name≠ficheiro = WARN, convenção JOCA); `/sync-questionnaires` corrido (contadores 110/36/22, +componentes nas listas dos READMEs, /goal+/know+/migrate adicionados, global ~/CLAUDE.md actualizado, índice+bridges regenerados). MCPs documentados em `memory/tools/mcps.md`.

- 2026-06-21 — **JOCA_OS Fase 1a COMPLETA (frontend chat + e2e) + 10 melhorias do Master, tudo verificado e2e contra o backend real.** App = repo local-only (commits por fazer). **(1) Frontend Master chat:** `MasterChatView` + `lib/markdown.ts` (marked+highlight+sanitize) + wiring WS no `App.tsx` + nav "Master" na sidebar. Contrato WS mapeado do backend (`master_message`/`orchestration_step`/`worker_summary`/`master_error`). e2e PASS: NL→1 worker→resumo sem toque manual (o brain até resolveu sozinho o prompt de confiança da pasta). **(2) Dedup:** o SDK repete o último texto do assistant no `worker_summary` → frontend esconde narração+steps e mostra só a mensagem final. **(3) Workers visíveis na UI:** `sm.spawn()` (via MCP) não difundia — centralizei o broadcast num evento `spawn` no SessionManager; removido o broadcast redundante do handler. **(4) Workers em projecto:** `spawn_worker(projectId)` resolve o path real → cwd + `/resume` (igual ao "abrir projecto"); o brain recebe a lista de projectos no system prompt e mapeia NL→projectId. **(5) Submit fiável:** brief multi-linha submetia só a 1ª linha → `writeSubmit` com **bracketed-paste** (`ESC[200~…201~`) + CR atrasada; `submitMessage` para o `send_to_worker`; brain instruído a CONFIRMAR envio via `read_worker`. **(6) Concorrência:** caixa nunca bloqueia; `masterBusy` boolean → contador de pendentes (envia tarefa B enquanto A corre). **(7) Reutilização de terminais:** registo de workers tornado **singleton partilhado** (era por-run → nunca via os anteriores → abria sempre novo); `list_workers` lista TODAS as sessões (workers + terminais do user) com estado; `read_worker` reporta status; `send_to_worker(armDone)` re-arma sentinela ao reutilizar. e2e PASS: 2 tarefas, 1 terminal. **(8) create_project:** MCP tool cria/regista projecto (safePath, createDir opcional) + broadcast `projects_changed` → aparece ao vivo na UI. **(9) Chat persistente:** `master-chat.json` + `GET/DELETE /master-chat`; UI carrega ao mount (sobrevive refresh/restart). **(10) Memória conversacional 3-camadas (FUTUROS Fase 7):** `master-memory.ts` — CURTA (resumo rolante) + LONGA (`longa/<id>.md`+index, pesquisável) + DIÁRIO (`diario/<id>.jsonl` verbatim); compressão deslizante (janela WINDOW10+BLOCK8); tools `search_memory`/`read_diary`; contexto injectado no prompt do brain. e2e PASS (continuidade Bigorna/verde-musgo cross-run; arquivo+search+diário). **Bug apanhado:** `master-memory.ts` em `dist/master/` resolvia `../../data`→`backend/data` (1 nível a mais) vs `JOCA_OS/data` do project-store → exportei `DATA_DIR` e reutilizei. **Incidente:** correr `start.bat` por cima de um backend meu já a correr → **2 backends na 7381** (IPv4+IPv6) → estado dividido; recuperado matando ambos + rebuild + 1 stack. Regra adicionada a `rules/workflows-and-tooling.md` (nunca arrancar em porta ocupada).

- 2026-06-23 — **JOCA_OS: multimédia drag-drop + Ctrl+V + portas 7391/7392 + v0.4.0** (commit `0cbafd0`, repo local-only). **(1) Portas 7381/7382 → 7391/7392** (launchers + vite + server.ts) a pedido do Renato, para correr lado-a-lado com o JOCA_UI (7371/7372). As 7381/7382 já estavam livres; a mudança foi explícita. **(2) Bug "só nome" corrigido:** ao arrastar do Explorer (Windows) o browser não expõe o caminho real (sandbox) — só `dataTransfer.files` com conteúdo mas `name` sem path → caía no fallback nome. Fix: **upload do conteúdo** (já existia p/ imagens) generalizado a QUALQUER ficheiro → servidor grava em `~/JOCA_Drops` (local-first, mesma máquina) → devolve caminho real → insere. Prioriza caminho original quando a fonte o dá (FileBrowser interno via `text/plain`, Finder via `text/uri-list`). **(3) Pastas:** browser não dá path real → upload recursivo (cópia, cap 200 ficheiros) via `webkitGetAsEntry`; serve para leitura, não para editar in-place. **(4) Ctrl+V de imagem** (print/imagem copiada) no Master E nos terminais → upload → `~/JOCA_Drops` (nome único `colado-<ts>-<n>.<ext>`). No terminal, listener `paste` em **capture phase** intercepta imagens antes do xterm; texto passa intacto. **(5) Util partilhado `lib/fileDrop.ts`** (1 fonte: terminal + Master). **(6) `/upload` endurecido:** allowlist alargada (imagens/docs/media/archives/código; **svg/html/executáveis bloqueados** — XSS), `x-rel-path` p/ pastas (valida traversal), destino `~/JOCA_Drops`, limite 50→200MB, devolve `root`. e2e PASS: ficheiro/pasta OK, `.exe` + `../../` rejeitados (400). **(7) Sidebar "JOCA DEV 0.3.4" → "JOCA 0.4.0".** **Gotchas (→ `rules/workflows-and-tooling.md`):** `sed -i` do Git Bash strippa CR de `.bat` (CRLF→LF) → cmd.exe parte o parsing (`setlocal`→`tlocal`) com erro enganador ("build failed" falso); `$PID` é read-only no PowerShell. **Limitação fundamental registada:** no browser puro NÃO há acesso ao caminho real do ficheiro/pasta no disco — a via é upload (cópia local); para o caminho ORIGINAL de pastas só um picker nativo (endpoint que abre diálogo Windows) serviria.
- 2026-06-25 (cont.) — **JOCA_OS: selector de provider do Master (4 opções) + bento + stats/limites + bridge Codex.** **(1) Bento boxes:** o Master full-screen passou a **3 caixas flutuantes arredondadas** (navbar · chat · stats) com `--surface-panel`/`--border-bento`/`--shadow-bento`/`--radius-bento` e gaps — sem linhas a dividir (composer dentro da caixa do chat; rail achatado numa só box). **(2) Stats rail:** Workers (a trabalhar/idle), Sessões, Projectos, **Limites Claude** (5h/7d/Sonnet de `/rate-limits`) com **tempo até reset** (`fmtReset`). **(3) Selector de provider** (Definições → Master Provider): card com 4 radios + campo Modelo, persistido em `ui-settings` (`masterProvider`/`masterModel`); `GET /master-providers` (detecção via `where`/`/api/tags`); pill do header reflecte a escolha. Orchestrator faz **dispatch real** por provider. **(4) ProviderManager + 3 providers novos** (workflow `master-providers-wire`: fundação sequencial = registry neutro `buildMasterToolDefs` mantendo Claude byte-a-byte + 3 agentes paralelos com **sondagem real do CLI/runtime**): **Ollama** ✓ **funciona** (loop function-calling in-process, gemma4 suporta tools — round-trip verificado); **Codex** = bridge MCP construído (control-plane `POST /master/tool`+`GET /master/tools-schema` reusa os 7 handlers via singleton workers; secret file-backed em `data/master-bridge.secret`; bridge stdio `codex-master-bridge.mjs` com `@modelcontextprotocol/sdk@1.29`; `run()` via `codex exec --json --dangerously-bypass-approvals-and-sandbox -c mcp_servers.master...` + **prompt por stdin**) — control-plane verificado (list_workers→200, 403 sem secret), **falta confirmação ao vivo** (smoke manual pendurou-se, mas foi artefacto do método: passei prompt por arg em vez de stdin); **Antigravity** = **bloqueado** (`agy -p` não escreve para stdout em pipe — resposta só num sqlite interno; sem registo MCP headless verificável; saídas = build do agy que escreva p/ pipe, ou SDK Python `google-antigravity`). **Custo:** confirmado pelo Renato — codex/agy logados correm na **subscrição** (como o Claude), custo-zero; só a API é que facturaria. **(5) `/install` reconfig** (fix MCP playwright `@anthropic-ai/mcp-server-playwright` 404 → `@playwright/mcp`) + **JOCA_UI** actualizado do `MirrasPT/JOCA@main` (3 ficheiros multimédia, staged). **Lições:** `@modelcontextprotocol/sdk` permite bridge MCP stdio standalone; `requireSafeOrigin` deixa passar pedidos sem Origin (subprocesso local) e bloqueia CSRF de browser; prompt a CLIs por **stdin** evita inferno de quoting no Windows; workflow com briefs de **sondagem real do CLI** (não assumir) + ficheiros isolados + integração no main loop = padrão validado.
- 2026-06-25 — **Sessão de UX/bugs do JOCA_OS Master + reconfig `/install` + update do JOCA_UI.** (a) **`/install` reconfig** (repo oficial é `MirrasPT/JOCA` em branch **`main`**, ≠ origin local `JOCA-OS` em `master`): perfil/soul mantidos; detecção confirmou tudo instalado; **bug real corrigido — MCP playwright apontava para pacote inexistente `@anthropic-ai/mcp-server-playwright` (npm 404) → re-registado com `@playwright/mcp@latest`, agora Connected**; statusline refrescada. (b) **JOCA_UI actualizado do GitHub `MirrasPT/JOCA@main`** (3 ficheiros: `server.ts`, `TerminalPane.tsx`, +`lib/fileDrop.ts` — fixes de multimédia drag/paste; via remote temporário `upstream` + `git checkout upstream/main -- JOCA_UI/...`, origin intacto; staged, por commitar; builds verdes). (c) **JOCA_OS Master — redesign + features (tudo no repo local-only):** **bug de persistência do chat** = o proxy do Vite (`vite.config.ts`) não listava `/master-chat` nem `/roots` → fetch relativo caía no SPA-fallback (HTML, 200) → `r.json()` rebentava → histórico "perdido" a cada refresh (backend persistia bem); **fix = +2 rotas no proxy**. Master agora **full-screen** (early-return em `App.tsx`, botão Voltar volta à view anterior via `prevViewRef`). **Redesign** alinhado ao design system (tokens warm-dark) e depois a partir de 9 refs do user: coluna de conversa centrada, **empty state editorial** (orb a pulsar + Playfair), **composer tipo card com toolbar** (`+` anexar → menu ficheiros/imagem via novo `uploadPickedFiles` em `fileDrop.ts`; drag-drop + Ctrl+V mantidos), **rail de stats lateral**. **Indicador de actividade ao vivo em baixo** (movido do topo) que traduz `master_message`/`orchestration_step` em "A abrir worker…/A enviar…/A acompanhar…" (deixou de esperar em silêncio). **Default do brain = Sonnet** (`orchestrator.ts`: `opts.model ?? 'sonnet'`). Paralelo já funcionava (runMaster fire-and-forget; indicador mostra "N a correr"). **Header minimal-bento** (dot+nome+pill Sonnet, fora o brand laranja+serif), **rail em bento** (surface-panel/shadow-bento) + **card "Limites Claude"** (5h/7d/Sonnet de `/rate-limits`, barras com cor por nível). **Bug spawn→view corrigido:** `session_created` fazia `setMainView('session')` sempre → tirava o user do Master; agora **fica no Master** (workers em background) se `prev==='master'`. **Persistência de projectos endurecida:** `writeJsonFile` tornada **atómica** (temp+rename) + log de erro de leitura — defende contra ficheiro "congelado" por kill a meio da escrita (a queixa "memória bloqueada"; a investigação não achou perda no código — `projects.json` íntegro com 12 projectos, data-dir único, gitignored). Stack reiniciada limpa (1 listener/porta) após cada mudança de backend.
- 2026-06-25 (cont. 2) — **Codex como Master CONFIRMADO A FUNCIONAR ao vivo (abre terminais reais no JOCA UI).** Bug: o Codex "fazia o trabalho no background, não nos terminais" porque a bridge MCP **morria no arranque** — lia o segredo de `backend/data/master-bridge.secret` (recompute `../../data` a partir de `backend/src/master`) mas o backend escreve-o em `JOCA_OS/data` (via `DATA_DIR`); `backend/data` nem existe → `fs.readFileSync` `ENOENT` → bridge crasha → Codex com **0 tools** → com `--dangerously-bypass-approvals-and-sandbox` faz o trabalho ele próprio no subprocesso. É o trap `__dirname ../../` para data-dir (já em `workflows-and-tooling.md`), agora a morder uma `.mjs` standalone. **Fix (fonte-única):** `codex-provider.ts` passa o caminho exacto do segredo (`MASTER_SECRET_FILE` do `bridge-config`) como `argv[3]`; `codex-master-bridge.mjs` lê de `argv[3]` (falha alto via stderr+exit 1 se não conseguir) e o fallback relativo foi corrigido `../../data`→`../../../data` (= `JOCA_OS/data`). **Verificado:** tsc limpo · control-plane 200 · **handshake MCP completo da bridge (spawnada como o Codex a faz) lista os 7 tools** · backend reiniciado limpo (1 listener 7391) · **confirmado pelo Renato ao vivo ("já está a dar")**. Lição: bridge/subprocesso externo NUNCA recomputa o data-dir — recebe o caminho do `bridge-config` (única fonte de `DATA_DIR`) por argv.
- 2026-06-25 (cont. 3) — **Automação (Fase 3) DESENHADA com o Renato + v1 IMPLEMENTADA + Master destrava menus de selecção.** Discussão de design longa (ele explicou as ideias antes de qualquer código, como pediu). **Modelo acordado:** automações = grafo de nós estilo n8n, mas com a diferença-chave de que o nó-estrela é **agêntico** (`master`/`worker` = abre/usa terminais Claude Code), não uma chamada de API determinística → o n8n precisa de ~400 nós, o JOCA precisa de ~10 porque o **worker cobre a cauda longa**. Master e editor manual produzem o **mesmo grafo JSON** (fonte única; Master escreve via futura tool `create_automation`). **Decisões trancadas:** (a) motor = cron **dentro do backend always-on** (alvo VPS 24h; local dispara com backend up) — sem Task Scheduler; (b) entrega agora = nó "mensagem no Master" (escreve `master-chat.json` + broadcast); WhatsApp/email = Fase 2 (só mais um nó de saída); (c) resumir = nó **LLM directo** (não worker — barato); (d) grafo **fixo**, inteligência **dentro** do nó (não misturar determinístico com agêntico); (e) **Connections** = primitivo novo ("ligar o site" 1x, reutilizável; tipos genéricos `mysql`/`sqlite`/`http`/`project` cobrem Bigorna/renatoferreira.org/etc. sem nó bespoke por site; default **read-only**). Casos de uso dados pelo Renato: resumo matinal de emails (gws), relatórios de site (encomendas/GA), notícias (Famalicão diário / AI semanal via RSS+web-research), automações que **agem** (não só reportam). **IMPLEMENTADO v1 (local-first):** **(A) Master destrava workers presos em menus** — `read_worker` detecta menu de selecção (opções numeradas, (y/n), "Do you want", setas) → `awaitingChoice=true` + aviso (conservador: o caret `❯` normal NÃO dispara, evita falsos positivos); novo tool `select_in_worker(workerId, keys)` manda teclas reais ao PTY (`"2 enter"`, `"down enter"`, y/n, esc…) via `sm.input`; system prompt do Master: menu reversível→escolhe a melhor, irreversível/ambíguo→pergunta ao Renato e espera (continuidade entre turnos já permite "pergunta→responde→escolhe"). **(B) Automações v1** — módulo `backend/src/automations/` (store atómico `automacoes.json` + computeNextRun daily/weekly/interval + runner linear `{{input}}` + scheduler tick 30s no backend always-on). Nós: `master` (agêntico), `llm` (resumo barato), `shell`, `http`, `message` (entrega no chat do Master, reusa appendMasterChat+broadcast). API `GET/POST/PATCH/DELETE /automations`+`POST /:id/run`. UI: nav "Automações" (`AutomationsView`) — lista c/ toggle/próxima/última/correr-agora/apagar + form (objectivo + agendamento) que gera `[master]→[message]`. **Verificado e2e:** pipeline shell→message→master-chat→persist→delete ✅; schedule math (daily→amanhã, weekly→próximo sábado, interval→+30min) ✅; tsc backend+frontend limpos; backend+vite reiniciados (1 listener/porta). Nós master/llm reutilizam runMaster/claudeProvider já verificados (não exercitados no smoke p/ não gastar tokens). **Próximo combinado:** editor visual de nós (canvas) + tool `create_automation` (Master cria por NL) + confirmar `gws` (a "Resumo Matinal" real precisa). **Lição reforçada:** o Renato quer **alinhar o design antes do código** em features grandes — discutir primeiro, implementar depois (ele deu o GO explícito só no fim).
- 2026-06-25 (cont. 5) — **JOCA_OS: Master cria automações por NL + Acções (= automações manuais com input) + botão Optimizar + Optimizações independente do Master + FUTUROS (resumo, Fase 8 Tasks, Fase 4 reframed).** **(1) Master cria automações:** tools `create_automation`/`list_automations` no registo neutro (logo p/ Claude/Codex/Ollama) + broadcaster desacoplado (`setAutomationsBroadcaster` em store.ts → UI refresca live) + system prompt. Verificado via control-plane (criou "Notícias AI" semanal). **(2) Acções = automações manuais + input (decisão de design com o Renato):** NÃO se construiu subsistema novo — uma Acção = automação `trigger:manual` + 3 acrescentos: **input em runtime** (`{{input}}` preenchido no `/run` e no tool `run_automation(ref,input)`), **skills/agentes a usar** (campo `skills` → directiva injectada no objective do nó master), **requireConfirm** (directiva "pára antes de irreversível e pede OK"). Tool `run_automation` (por id ou nome). Verificado e2e (echo {{input}}→"input-foi: banana"; run_automation por nome). **(3) Skills picker:** o campo skills passou de texto livre a **selector com autocomplete** alimentado de `/joca-items` (115 skills+36 agents, nome+descrição) + chips. **(4) Botão "Optimizar"** no objectivo: endpoint `/optimize-objective` reescreve o texto via cérebro. **BUG apanhado e corrigido:** `claudeProvider.run` é o **Agent SDK COM ferramentas** (Bash) → "optimizar" uma frase imperativa **EXECUTAVA-A de verdade** (correu o gws, devolveu emails reais!). Fix: opção `noTools` no provider → `tools:[]`+`maxTurns:1` (lido do `.d.ts`: `tools:[]` desliga built-ins) + enquadramento "reescreve, não executes". **(5) Optimizações independente do Master:** settings ganhou card próprio "Optimizações — Provider" com **SDK próprio** (Claude sem-ferramentas OU **Ollama local** via novo `ollamaProvider.completeText` chat-puro-sem-tools) + **modelo próprio** (`optimizeProvider`/`optimizeModel` em ui-settings). Verificado: optimize via Ollama reescreveu mantendo `{{input}}`. **(6) FUTUROS.md:** resumo de estado (Feito: Fases 1·3·7+base0; Parcial: 5·2; Por fazer: 2·4·6; flag Princípio 4 errado), **Fase 8 — Tasks** adicionada (quadro Kanban: A Definir→A Executar→Em Execução→Concluída→Arquivada; Master pega em "A Executar"→workers→tester→notifica), Fase 4 reframed (Acções=automações manuais). **Lições:** Agent SDK tem ferramentas por default — para texto puro usar `tools:[]`; ler o `.d.ts` (não a doc) confirmou a opção; modelo "automação manual + input" elimina um subsistema inteiro (YAGNI); broadcaster/runner injectáveis (setter em store.ts) evitam threading por orchestrator/providers.
- 2026-06-25 (cont. 4) — **`gws` (Google Workspace CLI oficial) configurado + 1ª automação real "Resumo Matinal" a funcionar e2e + envio de email validado.** O "gws" que o Renato referia É real: **`@googleworkspace/cli`** (binário `gws`, "One CLI for all of Google Workspace — built for humans and AI agents"; npm global; auth OAuth via projecto GCP; creds encriptadas no keyring + suporte `GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE`/`_TOKEN` p/ headless/VPS). Instalado `gws 0.22.5` + **gcloud SDK 574** (via winget, p/ `gws auth setup`). **Conta FIXA = renatorff93@gmail.com** (pessoal; o Renato disse que usará SEMPRE esta — NÃO a setuptech.pt) — ver [[renato-gmail-account]]. Projecto GCP = **`jocaos`** (debaixo da conta pessoal). **Setup vivido (lições reais):** `gws auth setup --login` pede **86 scopes** (todos, incl. `cloud-identity.devices` que é **admin de Workspace** → numa conta pessoal dá **`invalid_scope`/Erro 400**); `--services gmail --readonly` **NÃO** limitou; o que limita é **`gws auth login --scopes <lista explícita>`** (ex.: `...gmail.readonly`). Depois deu **403 access_denied** = consent screen em **"Testing"** sem test users → fix: adicionar a conta como test user na consola (`console.cloud.google.com/auth/audience?project=jocaos`). Login OK. **Token actual tem 13 scopes** (login posterior alargado): `gmail.modify` (lê+envia+modifica), calendar, drive, documents, presentations, spreadsheets, tasks, cloud-platform → **desbloqueia automações que AGEM**. **Capacidades gws verificadas:** `+triage` (resumo de não-lidos), `+read`, `+send`/`+reply`/`+forward` (flags `--to/--subject/--body/--html/-a/--draft/--dry-run`). **Envio REAL testado** → email para design@renatoferreira.org enviado OK (id `19f0080e2484b41e`, label SENT). **gws corre non-interactive via `child_process.exec` lendo o keyring** (verificado — o nó `shell` da automação chama-o sem problema). **"Resumo Matinal" CRIADA e a correr e2e:** automação diária 08:00, nós `[shell: gws gmail +triage] → [llm: resume pt-pt] → [message: entrega no Master]`; run-now produziu *"2 promoções, nada urgente — Lidl… e IsThereAnyDeal…"* entregue no chat do Master (lastStatus ok, nextRun 26/06 08:00). **⚠ GOTCHA por resolver:** a app OAuth está em **"Testing"** → o Google **expira o refresh token ao fim de ~7 dias** (a menos que só use scopes name/email). Como a conta é pessoal (sem via Workspace-Internal), a saída p/ não re-logar semanalmente = **publicar a app em Production** (`console.cloud.google.com/auth/audience?project=jocaos` → PUBLISH APP; mostra "unverified" mas funciona p/ o dono; cap 100 users irrelevante) + re-login 1x. Renato ia publicar — **confirmar se a publicação passou** (scopes restricted podem pedir verificação; se bloquear, fica Testing + re-login semanal até ao deploy VPS).
- 2026-06-26 — **Integração gstack → JOCA (autonomia): Fase A+B implementada e verificada.** Analisado o repo `garrytan/gstack` (59 skills, lib/, docs) — toolkit-irmão. Insight: a autonomia dele são 7 mecanismos roubáveis (não magia). Implementado no Brain (NÃO importa Postgres/Supabase/ios/browse-bespoke — SKIP): **(A1) Guard-rails** — hooks Node Windows-safe `check-freeze.js` (PreToolUse Edit|Write → deny fora do dir) + `check-careful.js` (PreToolUse Bash → ask em rm-rf/DROP/force-push/taskkill/format) armados por estado em `.joca/` (sem estado=no-op, fail-open); skills `freeze`/`careful`/`guard`/`unfreeze`; wired em settings.json. e2e: allow/deny/ask ok. **(A2) Brain event-sourced** — `joca-brain.mjs` (decide/supersede/redact/learn/active/recall/search sobre `memory/decisions+learnings/<slug>.jsonl`, append-only atómico, active computado, scope repo/branch, secret-scan HIGH rejeita, datamark anti-injecção, snapshot bounded); `/learn` command; **recall injectado automaticamente no `session-intake.js`** (decisões activas+aprendizagens do projecto). e2e completo. **(A3) Chaining** — `rules/chaining.md` (encadeamento automático: reversível→dispara sem perguntar `[chain → x]`, irreversível→gate; subagentes skill-aware via Step-0 no brief + devolvem próximo passo); campo `chain:` no frontmatter de frontend/design-review/laravel-specialist/plan/security/rest-api + 4 guard-rails; `## Próximo passo` nas skills-chave; `agent-context` §0 + `create-skill` exigem chain+Step0. **(A4) Checkpoints** — `joca-checkpoint.mjs` (save stdin→`memory/checkpoints/<slug>/<ts>.md` frontmatter, poda 12, atómico; latest/list, restauro cross-branch); `/save` PASSO 2c + `/resume` 2a. **(B1) Auto-runner** — `rules/pipelines.md` (catálogo executável + princípios de auto-decisão: Brain→convenção→default-skill→YAGNI; gates ⛔); doutrina ligada a CLAUDE.md (Decision Filter + Pipelines), soul.md (Working Principles), task-intake.md (Via D + secção Auto-runner). **(B2)** `master-orchestrator` ganhou **Pipeline Runner Mode** (match pipeline→corre a fundo, auto-decide, gate, encadeia); commands `/autoplan` (NL→plano auto-revisto, gate final) + `/retro` (aprendizagens da janela→acções, manual/cron). **(B3)** loops gstack nos agentes: `tester-code` (test→fix→verify+commit atómico), `log-debugger` (Iron Law: causa-raiz primeiro + `brain search` prior-learnings + escala após 3), `design-review` (atomic-fix+screenshot-proof). Index **119 skills·36 agents**; bridges recompilados; todos os JS/MJS `node --check` verdes; 4 skills passam `validate-skill`. Staging da análise em `_improvement/gstack/` (DESIGN.md + relatórios B/C/E). **Lições:** `node -e` colapsa backslashes (escaping hell → escrever ficheiro inteiro em vez de patch via shell); datamark sem control-chars no source (filtro por code-point); hooks sempre-registados só-activos-quando-armados (estado em ficheiro) evita hooks no frontmatter da skill. **Fase C feita** (cont. abaixo). Fase A+B commitada+pushed (`cb4aa5f`).
- 2026-06-26 (cont.) — **gstack → JOCA Fase C (produto) implementada.** 4 peças novas, conceitos portados (não verbatim — gstack skills têm ~1400 linhas, 90% boilerplate): **`design-shotgun`** (skill) — fan-out de N variantes paralelas (3-5 agentes, escrevem p/ disco) + board + iterar + **bloco UX Principles** (Steve Krug "Don't Make Me Think" destilado: 3 leis, billboard design, wayfinding, goodwill reservoir, mobile 44px); chain→design-review/design-html/frontend. **`design-html`** (skill) — mockup aprovado → HTML/CSS produção zero-deps (texto reflui, tokens, fidelidade [[renato-design-prototype-fidelity]], a11y); chain→frontend/design-review. **`/ship`** (command) — sync base→testes→review diff→version/CHANGELOG→⛔gate→push→PR (irreversível, gate obrigatório; nunca push às cegas/segredos/testes vermelhos). **`cso`** (skill) — auditoria 2-modos com **gate de confiança** (daily 8/10 zero-ruído / comprehensive 2/10 TENTATIVE) + STRIDE + OWASP + phases (secrets/deps/CI-CD/code/LLM), orquestra security-review+tester-security+dependency-auditor, "provar onde for seguro", SKILL.md=código-não-docs; chain→security-review/tester-security. Index **122 skills·36 agents**; pipelines.md +2 linhas (Design, Segurança CSO); trigger map + commands + INDEX + bridges. 3 skills validadas. **SKIP final:** ios/browse-bespoke/GBrain-Postgres/multi-host/telemetria/pair-agent (não aplica ou já coberto). Integração gstack COMPLETA (A+B+C).
- 2026-06-26 (cont. 2) — **`stop-slop` integrada (de `hardikpandya/stop-slop`, MIT).** Repo pequeno (1 skill + 3 references). BORROW claro: o JOCA não tinha detector de AI-slop em **prosa** (só visual no `design-review`). Criada skill `stop-slop` (1 ficheiro denso, convenção JOCA — consolidou SKILL.md+phrases+structures+examples): 8 regras-núcleo, frases a remover (throat-clearing/advérbios/jargão/meta/vagas), estruturas a evitar (contrastes binários/listagem negativa/fragmentação/falsa-agência/passiva/Wh-starters/ritmo), quick-checks, scoring 1-10 (<35/50→reescrever), exemplos before/after + **nota pt-PT** (equivalentes + chain→pt-pt-translator). Wired: `copywriting` ganhou `chain: stop-slop, pt-pt-translator`; é o passo de polish terminal das skills de copy/marketing; `design-review` referencia-a para slop de texto. Trigger map + index **123/36** + bridges. Validada. Crédito a Hardik Pandya no header.
- 2026-06-27 — **JOCA_OS portas 7391/7392 → 7491/7492 + fixes da análise + cPanel como capacidade.** **Portas:** afastadas para o range 74xx (longe das 7371/7372 do JOCA_UI) por segurança — garante que `stop.bat`/`stop.sh` do JOCA_OS nunca matam o backend do JOCA_UI (que mataria a sessão Claude activa). Mudança em ficheiros funcionais (launchers ×5, vite, server.ts ×2, bridge-config); docs históricas deixadas. **Fixes (workflow streams disjuntos):** atomicidade da memória do Master é o fix de maior valor — `master-memory.ts` usava `fs.writeFileSync` directo contra a própria doutrina temp+rename → extraído `writeFileAtomic` em project-store + guard de re-entrância. **Lição:** quando se analisa um codebase, validar o achado de maior severidade à mão antes do workflow (li `master-memory.ts` e confirmei as 5 escritas não-atómicas) — não delegar a verificação do próprio diagnóstico. God-files NÃO se auto-refactoram num workflow (risco>benefício, é reestruturação não bug). **cPanel:** padrão de integração = creds em `~/.cpanel/<account>.json` (fora do git, igual a `~/.cloudflare/datalix.json`) + driver genérico UAPI (`cpanel.mjs`, passthrough de qualquer módulo) + skill. Hosting partilhado stableserver: **token UAPI dá ~90% da gestão sem shell**; SSH key autoriza mas shell vem off por defeito (ticket) — SFTP funciona à mesma para ficheiros. Tokens vão para o ficheiro local `~/.cpanel/…`, nunca para o chat (Hard Limit: não guardar/ecoar tokens).
- 2026-06-23 — **Análise do `FUTUROS.md`** (visão 7 fases). Desfasado ~1 mês: fases marcadas "futuras" já parcial/totalmente feitas (Fase 1 Master 1a COMPLETA; Fase 7 memória 3-camadas IMPLEMENTADA; Fase 5 `/know` existe; Fase 3 automações parcial). **Erro factual a corrigir:** o "Modelo económico" (Princípio 4) diz Master=Gemini/Codex/Llama — a decisão real e o código é **Master=Claude Agent SDK na subscrição** (custo zero), Gemini desactivado. Tensões internas: "zero custos extra" ↔ Fase 2 WhatsApp (número/API pagos); "um JOCA por pessoa" (multi-tenant) vs estado single-user. Risco maior: Fase 6 Self-Learning autónomo (auto-`/upgrade-joca` sem supervisão) — recomendado staging+aprovação (padrão 2 fases). Reconciliação do doc fica como pendente opcional.
- 2026-06-28 — **3 god-files do JOCA_OS refactorizados (1 de cada vez, gate tsc+build) + feature Tarefas/Kanban (Fase 8) implementada via workflow.** **God-files:** `server.ts` 1225→81 (routers Express por domínio em `http/*` + `ws/broadcast.ts` fonte-única de clients/broadcast — sem threading de deps; routers importam broadcast directamente) · `App.tsx` 979→751 (`hooks/useSessionSocket.ts` = camada WS inteira, deps lidas via ref → socket criado 1x, devolve `send`; `CommandPalette.tsx` presentacional) · `DashboardView.tsx` 964→69 (router fino; os 2 ramos `project`/`dashboard` nunca montam juntos → cada um vira sub-view dona do seu estado: `dashboard/ProjectDetailView` + `ProjectsOverview` + `icons` + `RateBar` que re-exporta `RateLimits`). **Lição:** componente-hub de estado centralizado (App.tsx) — fragmentar de mais move complexidade; extrair só blocos coesos (camada WS, modal) + ramos de render mutuamente exclusivos. **Tarefas/Kanban (Fase 8):** decisões do Renato = **Master auto-puxa "A Executar"** (cap 2 concorrentes) · **passo de tester** depois de cada tarefa · **4º bloco recolhível** no Master. Construído por **workflow** (1 agente foundation `tasks/store.ts` → 3 streams paralelos: backend engine+rotas / `TasksView` tab / `MasterTasksBoard`) + **integração no main loop** (server.ts, master-tools, App.tsx, SessionSidebar, MasterChatView, useSessionSocket, types). Modelo `Task` (5 colunas a-definir→a-executar→em-execucao→concluida→arquivada), `tasks/engine.ts` (tick 5s → `runMaster(objectivo)` → tester → Concluída), tools Master `create_task`/`move_task`/`list_tasks`. Verificado: tsc backend+frontend + vite build + restart (`[tasks] engine on`) + smoke rotas GET/POST/move/DELETE. Commits `1bbac90`/`b63aaeb`/`53d48cd`/`98aaa7f` pushed. **Padrão validado:** workflow constrói ficheiros NOVOS independentes (foundation→fan-out); ficheiros partilhados (server.ts/App.tsx/etc) integra o main loop sequencial (anti-clobber).

- 2026-06-29 — **Memória de projectos UI→OS portada (REPLACE) + learning do `start.bat`.** Renato escolheu REPLACE (não merge): `JOCA_UI/data/{projects,project-memory}.json` copiados literalmente para `JOCA_OS/data/` (backups `.bak-<ts>`). OS ganhou os 4 projectos só-UI (incl. `tcg` com histórico) e perdeu os 3 só-OS. `data/` é gitignored → não vai a commit (estado runtime). **Learning reutilizável:** o `start.bat` do JOCA_OS (e provavelmente do UI) **não deixa nada a correr** — usa `start /b "" cmd /c <launcher>` e os filhos de background **morrem quando a janela cmd-pai fecha** (partilham consola). Sintoma: lança, "Launched", mas 0 listeners nas portas e logs antigos intactos. Workaround fiável: arrancar os processos **destacados** via `Start-Process` (backend `node dist/server.js` com `PORT`/`JOCA_LOGIC_PATH`; frontend `npx vite --port 7492`) — sobrevivem à sessão. Antes de arrancar, sempre `Get-NetTCPConnection -LocalPort 7491,7492 -State Listen` (regra porta-ocupada).

- 2026-07-01 — **7 bugs do JOCA_OS corrigidos (batch de `Erros.md`, só JOCA_OS).** Ver Estado actual para a lista. **Lições reutilizáveis:** (a) **arranque de PTY Claude Code deve ser guiado por prontidão, não por timers fixos** — esperar o TUI ficar quieto (quiet-gate: buffer>0 + sem output há ~700ms) + aceitar o prompt "trust this folder?" ANTES de enviar `/resume`/brief; timers fixos (1200ms) perdem o comando em boots lentos/prompt de confiança → sintoma "às vezes não faz /resume". (b) **o evento `done` do SessionManager tinha de ser consumido** para fechar o loop do Master — sem watcher, o Master é fire-and-forget (dispara worker, acaba turno, ninguém o re-invoca quando o worker termina). Fix = `master/worker-watch.ts` (done→follow-up runMaster; serializado + adiado enquanto `masterRunsActive>0`; cap por-worker anti-loop; **`notifyOnIdle` TEM de ser armado no brief inicial** senão a 1ª conclusão nunca dispara done). (c) **regra do proxy Vite outra vez**: rota nova no backend consumida por fetch relativo (`/tasks`) TEM de entrar no `server.proxy` senão cai no SPA-fallback (HTML 200)→`r.json()` rebenta→estado vazio ("tarefas não aparecem"; parece bug de persistência, é routing). (d) **browser não dá path de drag do Explorer** (sandbox) — drag só devolve path real de fontes que o dão (file browser interno via text/plain, Finder via uri-list); Ctrl+V é que grava cópia em JOCA_Drops. (e) Ctrl+C do xterm: `attachCustomKeyEventHandler` (com selecção copia, senão ^C). **POR COMMITAR.**

## Pendente
- **JOCA_OS — 7 fixes de `Erros.md` (2026-07-01) POR COMMITAR** (11 mod + 2 novos: `worker-watch.ts`, `notify.ts`; monorepo `JOCA_FINAL`). tsc+build verdes. Não testado e2e ao vivo (app não estava a correr; arranca-se limpa). **Verificar ao vivo (próxima sessão):** (1) worker-watch — spawnar worker via Master, deixar terminar, confirmar que o Master é re-invocado e responde/continua no chat (é o fix de maior valor e não foi exercitado); (2) /resume guiado por prontidão em projecto com prompt de confiança; (3) tarefas aparecem agora (proxy). ⚠ Se o worker-watch fizer ping-pong, ajustar cap `MAX_AUTO_PER_WORKER` (8) ou o gate `masterRunsActive`.
- **AUTOMAÇÃO (Fase 3) + ACÇÕES (Fase 4) — FEITAS (cont.3+5).** Motor cron + nós + criar por NL (Master `create_automation`/`run_automation`/`list_automations`) E por form + agente por-automação + **input runtime + skills picker (`/joca-items`) + requireConfirm** (acções) + botão **Optimizar** + Optimizações com SDK/modelo próprios. `gws` configurado, "Resumo Matinal" e2e. **Editor visual de nós DESCARTADO** (Renato quer só NL). **Próximo:** ~~(1) Tasks (Fase 8)~~ **FEITO 2026-06-28** (ver decisão); (2) **Connections** (ligar site 1x: `mysql`/`sqlite`/`http`/`project`, read-only); (3) Fase 2 = saída WhatsApp; (4) corrigir Princípio 4 do FUTUROS (modelo económico errado). Runtime: nós `master`/`llm`/`shell`/`http`/`message`; UI gera `[master]→[message]`.
- **TAREFAS / KANBAN (Fase 8) — FEITA (2026-06-28).** Quadro 5 colunas + tab "Tarefas" + 4º bloco recolhível no Master + Master auto-puxa "A Executar" (cap 2) + tester após cada. **Por fazer (afinações futuras):** persistência/lock do `running` Set do engine (mesmo double-fire risk do scheduler de automações se 2 backends); UI do tester-result/result no cartão; toggle global de pausa do auto-pull; ligar `projectId` ao cwd do worker (engine passa o objectivo mas não força cwd ainda).
- ~~**JOCA_OS app → repo GitHub próprio (ADIADO)**~~ **RESOLVIDO (2026-06-27): absorvido no MONOREPO `MirrasPT/JOCA-OS`** — NÃO repo separado, a pedido do Renato ("o joca-os deve ter tudo, é privado, é meu"). Passos: `JOCA_OS/.git` removido (history → `~/JOCA_OS-app-history.bundle`), `JOCA_OS/` tirado do `.gitignore` do monorepo, `*.mjs` tirado do `.gitignore` da app (a bridge `codex-master-bridge.mjs` passou a ser rastreada — antes estava fora do git). 76 ficheiros (~1.1MB); `node_modules`/`dist`/`data`/`backend/data` continuam excluídos. Push `5de05e0`. **Nota:** a app já não é repo git próprio — commits futuros da app vão para o monorepo.
- ~~**COMMITAR (repo local-only JOCA_OS):**~~ **FEITO localmente (`6d2e0f7`, 2026-06-25)** — Master UX (full-screen, bento 3-caixas, indicador de actividade, stats rail + limites c/ reset), **selector de provider + ProviderManager + 3 providers** (Ollama funcional, **Codex funcional ao vivo** — bridge a abrir terminais reais, confirmado, Antigravity bloqueado), fixes proxy/spawn/atomic-write/bridge-secret-path, default Sonnet. Inclui o fix `argv[3]` do segredo da bridge Codex, **Master destrava menus de selecção** (`select_in_worker`), **Automações+Acções** (`backend/src/automations/` store+runner+scheduler + 5 nós + nav `AutomationsView` + tools `create_automation`/`run_automation`/`list_automations` + input runtime + skills picker + requireConfirm + provider/modelo por-automação), **botão Optimizar** (`/optimize-objective` + `noTools` no provider) e **Optimizações independente** (settings provider/modelo próprios, Ollama `completeText`).
- ~~**COMMITAR (repo toolkit `JOCA_FINAL`/origin `JOCA-OS`):**~~ **FEITO + PUSHED (`c4f08db`, 2026-06-25)** ao GitHub privado `MirrasPT/JOCA-OS`. (a) `/upgrade-joca` desta sessão — 2 skills novas (`woocommerce-elementor`, `selfhosted-arr`), tailwind/install/init-project/save/orchestration-patterns/workflows-and-tooling reforçados, SKILL_INDEX (115), bridges, CLAUDE.md trigger map, 6 feedbacks arquivados. (b) **JOCA_UI 3 ficheiros staged** (update `MirrasPT/JOCA@main`).
- **Facto de repos (confirmado 2026-06-25):** repo de **distribuição/oficial = `MirrasPT/JOCA`** em branch **`main`** (tem `install.md`/`update.md`, ainda com paths `JOCA_Logic`/branch `master` desactualizados — doc-gap). Origin local do `JOCA_FINAL` = **`MirrasPT/JOCA-OS`** (privado) em `master`. Históricos divergem; sincronizar JOCA_UI faz-se por remote temporário + `git checkout upstream/main -- <path>`, nunca pull.
- **gws / Resumo Matinal — confirmar publicação OAuth:** a app `jocaos` estava em "Testing" (token expira ~7 dias). Renato ia **publicar em Production** na consola. **Confirmar** que passou (se scope restricted bloquear, fica Testing → re-login `gws auth login --scopes <…>` a cada ~7 dias até resolver no deploy VPS). Conta sempre **renatorff93@gmail.com**.
- **JOCA_OS v2 — design DONE (2026-06-21):** workflow `joca-os-v2-design` (9 agentes) escreveu `JOCA_OS/{PRD,TECH_SPEC,TASKS,REPURPOSE}.md`. Arquitectura: Master = camada aditiva sobre a base JOCA_UI (reusar SessionManager/WS/ProjectStore/RateLimits/Toolkit; build-new = ProviderManager/Orchestrator/WorkerRegistry/MemoryManager 3 camadas). **JOCA_OS é ainda cópia crua (package `joca-ui*`, portas 7371/7372 — repurpose NÃO feito).** Avisos verificados: `ANTHROPIC_API_KEY` no env VENCE o token de subscrição e factura → Master tem de fazer strip; "Gemini na subscrição" sem path SDK (fica TODO); Node ≥22.5 (node:sqlite). Crítica `needs-decisions` — 3 must_fix aplicados. **Decisões do Renato (2026-06-21):** provider default = **Claude Agent SDK na subscrição** + fallback Ollama (Gemini desactivado, sem path SDK); workers = **PTY visível** (reusar); defaults MVP aceites (memória %-ctx+session-close, diário não-encriptado local-first, ler cache partilhada joca-ui). **Fase 0 (repurpose) FEITA (`11d6b2b`):** JOCA_OS agora é **repo git próprio** (`git init`, baseline `19b01a8` + repurpose), package names `joca-os*`, portas **7381/7382** (disjuntas do JOCA_UI 7371/7372 → lado-a-lado), banner/LOG_DIR/pidfiles, `PORT||7381`. Preservado: `RL_CACHE_DIR='joca-ui'` (cache partilhada de rate-limits), sentinel TOOLKIT, App.tsx. Backend `tsc` limpo. **Fase 0.7 (extracção de módulos) FEITA (`33ae395`):** `server.ts` 1703→958 linhas; 6 módulos em `backend/src/` (security-fs, cli-capabilities, rate-limits, toolkit-registry, project-store, session-manager). `SessionManager` é EventEmitter singleton com API programática (spawn/input/readBuffer[strip-ANSI]/kill/resize) + evento `done` para o Master. Paridade: WS shapes 1:1, literais preservados. tsc 0 erros + **boot smoke test OK** (banner + liga ao Brain 110/36/22 + escuta :7381). **Fase 1a backend FEITO (`7fe4807`):** cérebro do Master em `backend/src/master/` — provider.ts (ClaudeProvider via Agent SDK), worker-brief.ts, master-tools.ts (MCP), orchestrator.ts; server.ts +WS `master_message`. tsc 0 + boot OK. **Agent SDK VERIFICADO** (`@anthropic-ai/claude-agent-sdk@0.3.185`, .d.ts + doc oficial): `query({prompt,options}):Query extends AsyncGenerator<SDKMessage>`; **`Options.env` SUBSTITUI o env** (não merge) → env-strip = `{...process.env}` menos `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` → CLI cai na subscrição (custo zero); `ANTHROPIC_API_KEY` no env VENCE e factura (por isso strip). `createSdkMcpServer({name,version,tools})` + `tool(name,desc,zodShape,handler)` (zod 4 ok). `SDKAssistantMessage.message`=BetaMessage (content blocks); `SDKResultMessage` (type:'result') traz `result`/`is_error`/`total_cost_usd`. `claude-api` skill cobre o Messages-API SDK (`@anthropic-ai/sdk`), NÃO o Agent SDK — são pacotes diferentes. **Fase 1a COMPLETA (2026-06-21)** — frontend Master chat + e2e + 10 melhorias + memória 3-camadas (ver Decisões 2026-06-21). **AGORA / próximo:** (a) ~~COMMIT~~ FEITO — Fase 1a + melhorias + memória commitados em sessões anteriores; multimédia drag-drop/paste em `0cbafd0`. **Portas agora 7391/7392** (não 7381/7382). (b) Memória LONGA já feita; falta opcional afinar trigger de arquivo (WINDOW/BLOCK) e talvez encriptar diário (FUTUROS decisões-pendentes). (c) **Selector de provider na UI** (Ollama fallback) + troca de modelo do brain — próximo grande. (d) `stop.bat` — confirmar que apanha IPv4+IPv6 da porta. (e) Opcional: reconciliar `FUTUROS.md` com estado real (marcadores de fase + corrigir modelo económico). (f) Opcional: picker nativo p/ caminho original de pastas. **Nota versionamento:** JOCA_OS é repo local-only; o GitHub `JOCA-OS` (privado) tem o *toolkit* (Brain+UI) — clash de nomes; decidir se JOCA_OS-app ganha repo GitHub próprio.
- **JOCA_OS portas = 7491/7492** (eram 7391/7392; antes 7381/7382). Afastadas do JOCA_UI (7371/7372) para o `stop.bat` do OS nunca matar o UI. Refs funcionais todas mudadas; docs históricas (REPURPOSE/PRD/TASKS/TECH_SPEC) ainda dizem 7381→7391 (não-funcionais, baixa prioridade).
- ~~**JOCA_OS god-files (decisão adiada):**~~ **FEITO 2026-06-28** — `server.ts` 1225→81, `App.tsx` 979→751, `DashboardView.tsx` 964→69 (ver decisão 2026-06-28). 1 de cada vez, gate tsc+build, confirmação visual do Renato no App.tsx.
- **JOCA_OS scheduler in-memory double-fire:** o `running` Set do scheduler é só em memória → se houver 2 instâncias do backend (double-bind IPv4/IPv6) a mesma automação dispara 2×. Mitigado pela disciplina single-instance; fix real precisa de persistência/lock. Baixa prioridade.
- **🔴 Brain log mistura sub-projectos no balde `JOCA_FINAL` (DIAGNOSTICADO 2026-06-28, fix POR EXECUTAR).** `joca-brain.mjs` + `joca-checkpoint.mjs` chaveiam o "projecto" por `git rev-parse --show-toplevel → basename` (`currentSlug`, joca-brain.mjs:52-58). Quando se trabalha num projecto cujo cwd reverte para `JOCA_Brain` (ex.: TCG, cujo repo real é `C:\Users\renat\Projetos\tcg`), o toplevel é `JOCA_FINAL` → **TODAS as decisões/aprendizagens/checkpoints de TCG + JOCA_OS + toolkit caem em `memory/{decisions,learnings,checkpoints}/JOCA_FINAL.*`**. Sintoma visível: o recall no arranque ("Brain — decisões activas (JOCA_FINAL)") mostra TCG e JOCA_OS juntos. Contagem actual: decisões 24 (11 TCG), aprendizagens 47 (5 etiquetadas tcg/omniclash). **Plano escolhido pelo Renato = "Separar TCG agora + corrigir raiz":** (1) extrair as entradas TCG de `JOCA_FINAL.*` para balde `tcg.*` (deixar JOCA_OS/toolkit como está); (2) corrigir raiz — `/save` passa `--slug <projecto>` aos comandos Brain/checkpoint + melhorar fallback do `currentSlug`. **Classificação já feita e revista** (11 decisões + 5 aprendizagens TCG; única escolha de julgamento = decisão [12] "demo Unity IMGUI" → incluída no TCG). **Próxima acção: backup dos `.jsonl` originais + executar a migração** (estava à espera do "avança" do Renato quando correu `/save`).
- **cPanel (renatoferreira @ stableserver):** capacidade montada (token UAPI + SFTP). **Opcional:** pedir **shell access** ao stableserver (chave já autorizada, porta 22) para `git`/`mysql`/scripts no servidor. WHM/root não aplicável (só conta de utilizador). Driver `cpanel.mjs` + skill `cpanel`.
- Graph build de `4fa0bcc` — stale; rebuild (paths mudaram com o rename) via Python API.
- Nota: o INDEX.md tracked mistura entradas pessoais — agora ok no repo privado, mas se algum dia o repo voltar a público, `git rm --cached` a memória pessoal primeiro.

- **JOCA_Brain — melhoria sistémica 2026-07-03 (cont.3) POR COMMITAR.** ~90 ficheiros (65 skills + 26 agentes + commands/hooks/README + 19 reference/ novos + 4 componentes novos). Ver Estado actual + Decisões. **Restante deixado de propósito (decisões de gosto/estrutura, não bugs):** (1) **9 skills grandes por dividir** em multi-step (github 433, remotion 445, file-storage 448, horizon 427, social-content 417, blueprint 417, video 415, hyperframes 408, lottie-animator 463) — mesmo padrão dos 6 já feitos. (2) **4 fusões candidatas** (agent-context→rules/, karpathy-guidelines→yagni, html-review rename p/ doc-preview-html, transactional-email router absorção) — precisam de sign-off por mexerem em nomes/routing. (3) **`browser-use` CLI vestigial** no `/install`+`/init-project` (o toolkit usa `browser-automate`/Playwright) — remover se confirmado. (4) triggers largos com colisão (`search`/`deploy`/`cache`) — apertar quando se tocar nas skills.
- **JOCA_OS — 2ª ronda de `Erros.md` (2026-07-03) POR COMMITAR** (ver Estado actual: notificações split masterAwaiting/notifyOnIdle, drag restaurado, close_worker, Model dropdown). tsc backend+frontend verdes. Deferidos #8/#18 (UI grande subjectiva) p/ próxima sessão.

## Recuperar branches arquivados (se preciso)
```bash
git branch v1-legacy archive/v1-legacy          # restaura branch v1
git checkout archive/analyze-project            # inspecciona fork JOCA_Optimized
```

## Última sessão
2026-07-03 (cont. 3) — **Auditoria + melhoria sistémica do JOCA_Brain a partir de 20 posts virais IG/Reddit sobre Claude Code.** Workflow (via D): 3 agentes de auditoria paralelos (skills/agentes/commands) → relatórios no scratchpad → 5 workers de fixes + main loop. Resultado: 28 descriptions reescritas (lixo auto-gerado), `chain:` machine-readable em 36 skills/agentes, 24 skills limpas de paths mortos, tools least-privilege, agentes passam a escrever p/ disco. **4 componentes novos** dos repos analisados: `/tdd` guard-rail (hook ask-não-deny), `stop-checkpoint.js` (auto-memória no Stop), `pack-context.mjs`+skill `context-pack` (repomix), `reference/design-dataset.md` (paletas/fontes/estilos p/ anti-convergence). **6 skills grandes divididas** em multi-step (corpo curto + `.claude/reference/<skill>/`, zero perda). `/sync-questionnaires` corrido (contadores + README Guard-rails/Game Dev). Todos os hooks smoke-tested, bridges recompilados, SKILL_INDEX 133/40. **Tudo POR COMMITAR.** Restante (9 splits + 4 fusões) deixado para sign-off — são gosto/estrutura, não bugs.

2026-07-01 (cont.) — **Restante do `Erros.md` do JOCA_OS implementado por workflow + review geral + 1 bug corrigido.** Análise fundamentada do `Erros.md` no código real (mapa item→ficheiro→causa; #4/#6 confirmados já feitos). Depois **workflow** para "implementar tudo": fase backend (1 agente coerente: #13 cwd no Brain, #14 contrato `origin`+`auto`/`continued`, #12 anexos+rotas) → 4 agentes frontend de **ficheiros disjuntos** em paralelo (DRAG / MASTER-CHAT / TASKS / NOTIFY) → verify (3 builds verdes). Decisão minha no #3 (reversível): drag = path real; Explorer sem-path → dica em vez de copiar; Master consistente (sem upload no drag). No pedido de "teste geral + melhorias": review de integração ponta-a-ponta (contrato WS íntegro, wrapper xterm não colapsa) — **bug encontrado**: `tasks/engine.buildObjective` ignorava `task.attachments` → o worker nunca recebia os ficheiros anexados; corrigido (injecta os paths nas instruções). App a correr na build anterior (7491/7492, sem double-bind) — backend precisa de restart p/ #13/#14/#12. **Tudo POR COMMITAR.**

2026-06-28 (cont.) — **Diagnóstico: Brain log mistura sub-projectos no balde único `JOCA_FINAL`.** Renato reparou que o recall "fala do TCG e do JOCA_OS ao mesmo tempo". Causa: `joca-brain.mjs`/`joca-checkpoint.mjs` chaveiam por `basename(git toplevel)` → trabalho no TCG (cwd revertido p/ `JOCA_Brain`) cai em `JOCA_FINAL.*` em vez de `tcg.*`. Os ficheiros de prosa (`projects/tcg.md`, `projects/joca.md`) estão BEM separados; só o Brain log/checkpoints é que estão num balde só. Classifiquei as entradas (11 decisões + 5 aprendizagens são TCG) e revi com o Renato. Plano aprovado: extrair balde `tcg` + corrigir raiz (`/save` com `--slug`). **Migração ainda POR EXECUTAR** (à espera do "avança"; ver Pendente 🔴). Sem alterações a código/commits nesta volta.

2026-06-28 — **JOCA_OS: 3 god-files refactorizados + feature Tarefas/Kanban (Fase 8) construída por workflow + push de tudo.** Sessão começou pelo toolkit: commit das 58 mudanças pendentes do `/upgrade-joca` 2026-06-27 (3 skills novas `notion`/`social-scheduler`/`unity-gamedev` + 12 melhorias + memória TCG + arquivo de 11 feedbacks) → `d55df26` pushed. Depois **god-files do JOCA_OS, 1 de cada vez com gate tsc+build**: `server.ts` 1225→81 (routers Express por domínio `http/*` + `ws/broadcast.ts` fonte-única) `1bbac90`; `App.tsx` 979→751 (`hooks/useSessionSocket.ts` camada WS + `CommandPalette.tsx`) `b63aaeb` — confirmado ao vivo pelo Renato; `DashboardView.tsx` 964→69 (`dashboard/ProjectDetailView`+`ProjectsOverview`+`icons`+`RateBar`) `53d48cd`. **Feature Tarefas/Kanban (Fase 8)** alinhada com o Renato (auto-pull do Master cap 2 · tester após cada · 4º bloco recolhível) e construída por **workflow** (foundation `tasks/store.ts` → 3 streams paralelos: backend engine+rotas / TasksView / MasterTasksBoard) + integração no main loop (server.ts/master-tools/App.tsx/SessionSidebar/MasterChatView/useSessionSocket/types) → `98aaa7f`. App arrancada eu próprio (PIDs directos — `start.bat` via background tool não segura os processos: cmd-pai reaped leva os `start /b`) e aberta no browser p/ o Renato testar. Verificado: tsc backend+frontend + vite build + restart limpo (`[tasks] engine on tick 5s cap 2`) + smoke das rotas. Tudo pushed ao `MirrasPT/JOCA-OS`.

2026-06-27 (cont.) — **Análise do JOCA_OS + mudança de portas 7391/7392 → 7491/7492 + workflow de fixes + capacidade cPanel (token+SFTP).** **(1) Análise** (2 agentes de review paralelos, backend Master + frontend): arquitectura sólida — `security-fs` sério (defesas symlink/TOCTOU/hardlink), abstracção de providers genuinamente neutra (registo único de tools, 2 consumidores de schema), proxy do Vite COMPLETO. Dívida concentrada em 2 sítios: **`master-memory.ts` não-atómico** (`fs.writeFileSync` directo, ao contrário do `project-store` temp+rename → kill a meio corrompe e reverte memória em silêncio) + **god-files** (`App.tsx` 967, `server.ts` 1225, `DashboardView` 969). **(2) Portas:** a pedido do Renato (segurança — `stop.bat` do JOCA_OS nunca pode matar o JOCA_UI 7371/7372 e terminar a sessão), movido **7391/7392 → 7491/7492** em TODOS os ficheiros funcionais: `start.bat`/`stop.bat`/`start.sh`/`stop.sh`/`JOCA OS.command`, `vite.config.ts`, `server.ts` (×2), `bridge-config.ts`. Docs históricas (REPURPOSE/PRD/TASKS/TECH_SPEC) deixadas (não-funcionais). **(3) Workflow `joca-os-fix-bugs`** (3 streams de ficheiros disjuntos em paralelo → verify tsc/build, SEM arrancar servidor/matar portas): 🔴 memória atómica (`writeFileAtomic` extraído em project-store; 5 escritas migradas; guard de re-entrância `archiving` em `refreshShortMemory`) · 🟡 Ollama `tool_call_id` (verificado contra Ollama real, degrada sem fabricar) · 🟠 rateLimits duplicado (fonte única em App.tsx) + re-render global (`workflowEquals` evita setState no-op) + drag-drop divergente (TerminalView → fileDrop.ts) + bug-irmão do `<input file>` picker. **Build verde backend+frontend.** God-files EXCLUÍDOS do auto-fix (reestruturação ≠ bug). **(4) cPanel** (conta `renatoferreira` @ stableserver): nova capacidade de gestão — `.cpanel/renatoferreira.org.json` (creds fora do git), driver genérico `.claude/scripts/cpanel.mjs` (passthrough UAPI), skill `cpanel` (+trigger). Provado ao vivo: domínios (6 addon), email (9), DNS, ficheiros. **SSH:** chave ED25519 `~/.ssh/cpanel_renatoferreira` autorizada (porta 22) — **shell DESACTIVADO pelo host** (precisa ticket), mas **SFTP funciona** (deploy de ficheiros). **COMITADO + PUSHED ao `MirrasPT/JOCA-OS` privado:** Brain `898ae06` (cpanel+análise) + `388a1ae` (memória) + `57d787b` (/save); e **a app JOCA_OS foi ABSORVIDA no monorepo** `5de05e0` (deixou de ser local-only; `.git` aninhado removido, history em `~/JOCA_OS-app-history.bundle`, 76 ficheiros, bridge `.mjs` agora rastreada). Lembrete de revogar o token removido a pedido do Renato.

2026-06-27 — **Notion CLI (`ntn`) instalado + reorganização do workspace Notion do cliente Luis Gonçalo.** Instalado `ntn 0.18.1` via `winget install Notion.ntn` (Windows x64; macOS/Linux usam `curl -fsSL https://ntn.dev | bash` ou `npm i -g ntn` c/ Node ≥22). Binário em `…\WinGet\Packages\Notion.ntn_…\ntn-x86_64-pc-windows-msvc\ntn.exe` (PATH só na próxima shell). Auth por keychain (`ntn login`; `ntn whoami` OK) → workspace "Renato Fernandes Ferreira's Notion" (design@renatoferreira.org). **API por `ntn api <PATH> [-d @file|stdin|inline]`.** **Gotchas confirmados ao vivo:** (1) **Git Bash converte `/v1/…` em path Windows** → 400 `invalid_request_url`; fix `MSYS_NO_PATHCONV=1` (ou inline inputs). (2) **API nova = data sources, não "database"**: search filtra `value:"data_source"` (não `"database"`); query = `POST /v1/data_sources/{id}/query`; criar página = `parent:{type:"data_source_id",data_source_id}`; editar opções de Status = `PATCH /v1/data_sources/{id}` (re-enviar todas as `options` por id + a nova). (3) **`-d @file` PENDURA no `PATCH …/children`** (timeout) → passar o body por **stdin** (`subprocess input=`). (4) Acentos: forçar UTF-8 (`PYTHONUTF8=1` + `encoding='utf-8'` no subprocess) senão a consola Windows corrompe a saída. **Trabalho:** na BD "Tarefas" do cliente — criei estado "Save", arquivei lá os to-do antigos, e reescrevi a lista em "Não Iniciada" reduzida de 27 → **11 tarefas agrupadas por projecto** (Alkimia ×4, Elite Cozinhas, Divinho Alvarinho, Royal Douro, Restauros, Bracaris, Vinartis, Outros), cada uma com **checklist interno (`to_do` + descrição cinzenta nested)**. Recuperei pendentes embutidos numa página-mãe "Notas" (Restauros/Bracaris/Royal Douro) e integrei nas tarefas certas. ⚠ Erro inicial: editei a cópia backup "Save 27-06-26" (data source `d2369cbb`, criada hoje, dentro de página backup) em vez da Tarefas real (`1e269cbb`, 2025-04-27) — Renato restaurou; lição: **distinguir a data source real da cópia de backup pelo `created_time`/`database_parent` antes de escrever**. Notion CLI = tool externo (padrão gh/graphify), não skill portada. Sem alterações ao repo JOCA.

2026-06-26 (cont. 5) — **`/last30days` setup COMPLETO (10 fontes activas).** Tratei sozinho: yt-dlp (YouTube), Go 1.26.4 + Digg CLI (no PATH, activa após restart), `gh` confirmado (GitHub), `.env` em `~/.config/last30days/` (local, fora de repo, **sem segredos no toolkit**). Renato instalou Firefox+login x.com e deu a chave ScrapeCreators + cookies X (`auth_token`/`ct0`). **Diagnose: 10 fontes** — Reddit·X·TikTok·Instagram·YouTube·HackerNews·Polymarket·GitHub·Web·Threads. Teste ao vivo (`--quick`) devolveu dados reais (Reddit+HN sobre "Claude Code"). ⚠ Windows: X só via Firefox (Chrome/Edge encriptados) → usei AUTH_TOKEN+CT0 manuais. Sessão grande hoje: gstack A+B+C, stop-slop, graphify/UA (extractor + grafo-de-grafos + merge), last30days. Renato a conduzir o uso. **Memória/Brain registados; integração toolkit já commitada (a2ee337).**

2026-06-26 (cont. 4) — **`/last30days` (mvanhorn) instalado + ligado aos workflows de research/marketing.** Analisado o repo `mvanhorn/last30days-skill` — é um **TOOL real** (como o graphify): a skill são 2037 linhas de contrato/LAWs + motor Python `scripts/last30days.py`; sem o motor é inútil → não-portável como conteúdo. Preenche um **gap real** (o `deep-research` do JOCA é web/editorial; o last30days dá **sinal social pontuado por engagement** — Reddit/X/YouTube/TikTok/HN/Polymarket/GitHub, upvotes/likes/dinheiro). **Instalado** via `claude plugin marketplace add mvanhorn/last30days-skill` + `claude plugin install last30days@last30days-skill` (user scope, v3.8.3, HTTPS auto; motor em `~/.claude/plugins/cache/.../scripts/last30days.py`, `--help` OK). Zero-config p/ Reddit/HN/GitHub/Polymarket/web; X/YouTube/TikTok via keys opcionais. **Ligado aos workflows (não reimplementado — referenciado como tool externo, padrão graphify/markitdown/gh):** trigger map (`/last30days`), nota "sinal de mercado" em `competitor-profiling`/`content-strategy`/`launch-strategy` + complemento em `agent deep-research` (correr ambos e fundir), pipeline "Research de mercado/recência" em `rules/pipelines.md`. Útil p/ marketing dos clientes (Bracaris/Bigorna/Bodegas) + manter-se a par de AI. Bridges recompilados. Por commitar.

2026-06-26 (cont. 3) — **Análise graphify + Understand-Anything → mapa de conhecimento JOCA-aware.** Re-analisei o graphify (instalado v0.8.5 = já multimodal: código+docs+PDF+imagens+vídeo, `graph.html`+query/explain; o JOCA só usava a invocação antiga só-código) e o Understand-Anything (plugin Claude pesado Node/pnpm/React, ~80% sobreposto). **Decisão do Renato:** SKIP o UA + construir extractor JOCA-aware. **Achado:** nem graphify nem UA captam as conexões reais do JOCA (skills ligam-se por `chain:`/triggers/prosa, não por links markdown/imports). **Feito:** (1) bug real corrigido no `graphify-deps.py` (lia `.md` sem `encoding='utf-8'` → crash cp1252 Windows); (2) `joca-graph.mjs` — extractor que lê frontmatter (name/chain/triggers) de skills/agentes/comandos/rules/projectos → graph.json compatível graphify (hubs por tipo + edges `chain:`) → **211 nós/232 edges, 26 chains resolvidas, graph.html interactivo** via `graphify cluster-only`; (3) comando `/map-joca`; (4) navegação do CLAUDE.md (top + Brain) actualizada (mapa código + conhecimento, graphify moderno). Output gitignored. **(cont. 3b)** A pedido do Renato — **grafo-de-grafos:** o extractor agora resolve o caminho real de cada projecto (frontmatter `directorio:`/`path:`/`repo:`), e liga cada nó de projecto ao **grafo PRÓPRIO** desse projecto (nó `grafo: <projecto>` com `source_file`→`graph.html` do projecto, edge `has-graph`) quando `<path>/graphify-out/graph.json` existe. **6/13 projectos já ligam** (Bigorna, livro-de-elogios, mediaval-chess, meu-site-github, simao-sina, unimedia); flag `--build-projects` gera os em falta (best-effort, só código; ⚠ saltar ComfyUI=gigabytes; remotos/ausentes saltados). 217 nós/238 edges. **(cont. 3c)** A pedido do Renato — **merge real (graph gigante com tudo):** flag `--merge` em `joca-graph.mjs` funde o `graph.json` de cada projecto no graph do JOCA, **namespaced** (`<proj>::<nó>`) + **bridge** `project:X --project-code--> god-node do projecto` → componente único conectado. **Filtra ruído de libs** (node_modules/vendor/dist/.venv → 280k nós descartados). Escreve SEPARADO (`joca-knowledge-merged/`) p/ não tocar o mapa limpo. Resultado: **75.816 nós / 104k edges / 5663 communities** (JOCA 217 + código real de 6 projectos). ⚠ >5000 → sem viz estática: explorar via `graphify serve` ou `graphify query --graph <merged>`. Fix `directed: false` (graphify exige consistência p/ merge). **2 mapas:** limpo (217, visual/navegável, drill-down) + gigante (query cross-projecto). Nota: livro-de-elogios traz 61k mesmo filtrado (grafo próprio inchado → precisa rebuild limpo na pasta dele). Próximo: quanto mais skills tiverem `chain:`, mais rica a teia. Por commitar.

2026-06-26 (cont. 2) — **`stop-slop` integrada** (de `hardikpandya/stop-slop`, MIT): skill nova de anti-AI-slop em PROSA (o JOCA só tinha slop visual no design-review). Consolidada num ficheiro denso (regras+frases+estruturas+scoring+exemplos+nota pt-PT); `copywriting` encadeia para ela; passo de polish terminal das skills de copy. Index 123/36. gstack A+B+C em `cb4aa5f`/`3886920`/`13b3f06`.

2026-06-26 (cont.) — **gstack → JOCA Fase C (produto): design-shotgun (+UX Principles Krug), design-html, /ship, cso (audit 2-modos).** Index 122/36. Integração gstack COMPLETA (A+B+C). A+B em `cb4aa5f`; C por commitar.

2026-06-26 — **Integração gstack → JOCA (autonomia, Fase A+B).** Analisado o gstack (toolkit-irmão) e roubados 7 mecanismos para o Brain: guard-rails por hook (freeze/careful/guard), Brain event-sourced (decisions/learnings JSONL + recall automático no arranque + `/learn`), chaining automático (`chain:` + `rules/chaining.md`), checkpoints (`/save`+`/resume`), auto-runner de pipelines (`rules/pipelines.md` + master-orchestrator runner mode + `/autoplan`+`/retro`), e loops gstack (test→fix→verify, Iron Law causa-raiz, atomic-fix+screenshot). 11 ficheiros novos + canónicos reforçados; tudo verificado (hooks e2e, CLI e2e, node --check, validate-skill). Index 119/36. Fase C adiada. Por commitar. Detalhe nas Decisões 2026-06-26 + `_improvement/gstack/DESIGN.md`.

2026-06-25 (cont. 5) — **JOCA_OS: Master cria automações por NL + Acções (=automações manuais com input/skills/confirm) + botão Optimizar + Optimizações com SDK próprio + FUTUROS (Fase 8 Tasks).** Tools `create_automation`/`run_automation`/`list_automations`; acções = automação manual + input runtime + skills picker (`/joca-items`) + requireConfirm; bug do Optimizar (Agent SDK executava em vez de reescrever) corrigido com `tools:[]`; Optimizações separado do Master (Claude sem-tools OU Ollama local). FUTUROS resumido + Fase 8 (Tasks/Kanban) adicionada. Tudo verificado e2e. Por commitar.

## Sessão 2026-06-25 (cont. 4 — gws)
2026-06-25 (cont. 4) — **`gws` (Google Workspace CLI oficial) configurado + "Resumo Matinal" a funcionar e2e + envio de email validado.** Instalado `gws 0.22.5` + gcloud 574; auth OAuth (conta SEMPRE renatorff93@gmail.com, projecto `jocaos`); fixes de scopes (login só com `--scopes gmail.readonly`; 403→add test user; depois token alargado a 13 scopes incl. gmail.modify→envia). Automação diária `[shell gws +triage]→[llm resume]→[message Master]` testada (run-now: "2 promoções, nada urgente…"). Envio real OK (id 19f0080e…). ⚠ App OAuth em Testing → token expira ~7 dias; Renato ia publicar em Production (confirmar). Tudo no repo local-only, por commitar.

## Sessão 2026-06-25 (cont. 3 — automações v1)
2026-06-25 (cont. 3) — **Automação (Fase 3) desenhada com o Renato + v1 implementada e verificada, + Master destrava workers presos em menus de selecção.** Modelo n8n com nó agêntico (worker cobre a cauda longa → ~10 nós, não 400); motor cron no backend always-on (alvo VPS 24h); entrega no chat do Master (WhatsApp=Fase 2); Connections como primitivo futuro. Implementado: `select_in_worker`+detecção de menu; módulo `automations` (store/runner/scheduler) + 5 nós + nav AutomationsView. e2e verde (pipeline shell→message→chat; schedule math). Próximo: editor visual de nós + tool create_automation + confirmar gws. Tudo no repo local-only, por commitar.

## Sessão 2026-06-25 (cont. 2 — Codex)
2026-06-25 (cont. 2) — **Codex como Master CONFIRMADO ao vivo.** O Codex fazia o trabalho no background em vez de abrir terminais no JOCA UI porque a bridge MCP morria no arranque a ler o segredo do path errado (`backend/data` vs `JOCA_OS/data` — trap `__dirname ../../`). Fix: passar o caminho exacto do segredo (`MASTER_SECRET_FILE`) à bridge por `argv[3]` + fallback relativo corrigido. Verificado (tsc, control-plane 200, handshake MCP lista 7 tools, backend reiniciado limpo) e confirmado pelo Renato. **Automação (Fase 3): ainda a discutir — o Renato vai explicar as ideias dele primeiro, NÃO COMEÇAR sem alinhar.** Tudo no repo local-only do JOCA_OS, por commitar.

## Sessão 2026-06-25 (cont. providers)
2026-06-25 (cont.) — **JOCA_OS: selector de provider do Master (Claude/Ollama/Codex/Antigravity) + bento boxes + stats/limites + bridge MCP do Codex.** Workflow `master-providers-wire` (fundação registry neutro + 3 providers com sondagem real): **Ollama a funcionar** (tool-calling local verificado), **Codex** = bridge construído + control-plane verificado (confirmado ao vivo na sessão seguinte), **Antigravity bloqueado** (agy não escreve p/ pipe). Master full-screen redesenhado em 3 bento boxes. Selector nas Definições (persiste em ui-settings) + dispatch real no orchestrator. Tudo no repo local-only do JOCA_OS, por commitar.

## Sessão 2026-06-25 (UX/bugs)
2026-06-25 — **JOCA_OS Master: UX redesign (bento) + bugs.** `/install` reconfig (fix MCP playwright: pacote 404 → `@playwright/mcp`; statusline). JOCA_UI actualizado do GitHub `MirrasPT/JOCA@main` (3 ficheiros multimédia, staged). Master full-screen + redesign a partir de 9 refs (coluna centrada, hero editorial, composer com toolbar `+`anexar, rail de stats). Indicador de actividade ao vivo em baixo (em vez do topo). Default brain = **Sonnet**. Header minimal-bento + rail bento + card de limites Claude (5h/7d/sonnet de `/rate-limits`). **2 bugs corrigidos:** chat perdia histórico no refresh (proxy Vite sem `/master-chat`+`/roots`); spawn de worker tirava do Master (agora background). Persistência de projectos endurecida (escrita atómica). Tudo no repo local-only do JOCA_OS, por commitar. **Próximo: selector de provider + commit do JOCA_OS.**

## Sessão 2026-06-23
2026-06-23 — **JOCA_OS: análise (código + FUTUROS.md) → multimédia drag-drop/paste + portas 7391/7392 + v0.4.0** (commit `0cbafd0`). Análise do cérebro do Master (5 módulos, 658 LOC, tsc limpo) e do `FUTUROS.md` (desfasado ~1 mês; modelo económico errado). Mudei portas 7381/7382→7391/7392 (lado-a-lado com JOCA_UI), corrigi o bug "drag põe só o nome" (browser não expõe path no Windows → upload p/ `~/JOCA_Drops`), generalizei o drop a qualquer ficheiro/pasta, adicionei Ctrl+V de imagem no Master E terminais, extraí `lib/fileDrop.ts`, endureci `/upload` (allowlist, x-rel-path, traversal). e2e do endpoint PASS. Gotchas: sed CRLF nos `.bat`, `$PID` reservado PowerShell. **Próximo: selector de provider na UI.**

## Sessão anterior
2026-06-22 (madrugada) — **JOCA_OS Fase 1a frontend + 10 melhorias do Master + memória conversacional 3-camadas**, tudo verificado e2e contra o backend real (probes WS, não só tsc). Master chat (MasterChatView) → workers visíveis na UI (evento spawn) → workers em projecto (projectId→path) → submit fiável (bracketed-paste) → confirmação de envio → concorrência (caixa não bloqueia) → reutilização de terminais (registo singleton + list_workers vê tudo) → create_project (com broadcast) → chat persistente → memória CURTA/LONGA/DIÁRIO (FUTUROS Fase 7) com search_memory/read_diary. Bugs apanhados: SDK duplica texto final; `__dirname` aninhado divergia data-dir (→ exportei DATA_DIR); **2 backends na 7381 (IPv4+IPv6)** por start.bat sobre backend meu → estado dividido, recuperado. Regra de hygiene de portos + data-dir adicionada a `rules/workflows-and-tooling.md`.
